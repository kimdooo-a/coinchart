# 개발 로그 — 2026년 6월

### 2026-06-02 (세션 43) — R16 지휘부 — type-cleanup 마감 (CEO)

**대화 토픽 흐름**: 지휘관 터미널 진입(/kdydispatch) → R15-T04 환각 FAIL 컨텍스트 파악 → 지상 진실 검증(scripts any 실분포 grep) → R16 범위 확정(사용자 질문) → SOT 3종 설계(환각 차단 게이트) → 발사 → 회수·통합 검증 → 세션 종료

**라운드 개요**: R15에서 T04(`scripts/` any 정리)가 **이 프로젝트에 없는 유령 디렉토리**(`scripts/batch/`·`scripts/cron/` 등)를 대상으로 작업했다고 허위 보고(any 11→0 주장, 실제 변경 0건)한 환각 사고를 R16에서 재수행. 핵심 교훈: **SOT에 실파일 목록을 못박고 착수 전 게이트·변경 증거(`git diff --stat`)를 강제**하면 환각 재발을 차단할 수 있다.

**변경 사항** (21파일, 일꾼 산출물):
- **T01 (scripts 엔진류 4파일, any 22→1)**: `alert_engine.ts`(8→0, 로컬 `AlertSnapshot` 인터페이스 도입·catch `any`→`unknown`+캐스팅)·`batch_orchestrator.ts`(6→0, catch 6건 narrowing)·`batch_analysis.ts`(4→1, catch 3건 처리·`result?:any` 1건 정직 보류)·`preflight.ts`(4→0).
- **T02 (scripts 보조류 12파일, any 23→0)**: report_generator·seed_prices_v2·update-market-data·verify_explanation·healthcheck·release_quality_gate·seed_bch·daily_cron·debug_analysis·migrate-blog·seed_prices·weekly_cron. 로컬 `YahooCandle`/`YahooPriceRow`/`BinanceKline` 선언 + 기존 `MarketRegime`·`ConfidenceGrade`·`CandleData` import 재사용. catch `unknown`+`instanceof Error`.
- **T03 (analysis 5파일, any 5→0)**: `app/analysis/[symbol]/_lib/types.ts`에 route-local `Candle`(time:string + OHLCV:number) 신설. `historyData: any[]` 5건(useAnalysisData ×2·AnalysisGrid·ChartSection·PositionStatusCard) → `Candle[]`. 기존 캔들 타입은 전부 `time:number`(Unix)인데 본 라우트는 `time:string`(DB date)이라 재사용 부적합 → 신설.

**주요 논의**:
- **환각 근본원인**: R15-T04 SOT가 실파일 목록을 주긴 했으나 "착수 전 재확인" 게이트가 약해 일꾼이 상상한 디렉토리 구조로 작업. R16 SOT 3종 모두 §1에 **착수 전 게이트**(`Get-ChildItem scripts -Directory`로 평면 확인 + 대상 파일 `Test-Path` + "유령 디렉토리 보이면 환각 감지·중단") + **`git diff --stat` 실출력을 handover 필수 항목**으로 박음.
- **격리 설계**: T01·T02가 둘 다 `scripts/` 안이라 **파일 단위 disjoint**(T01=4파일, T02=12파일)로 분할 + **새 공유 타입 파일 신설 금지**(충돌 회피, 로컬 선언/기존 import만).
- **T01·T02 상충 보고**: T02가 "글로벌 tsc EXIT 2, alert_engine.ts 12건"을 보고했으나 지휘부 최종 tsc EXIT 0. 원인 = 공유 작업트리에서 T02가 본 시점이 T01의 alert_engine 작업 **중간 스냅샷**(옵셔널 체이닝 정리 전). 타이밍 차이지 상충 아님.
- **eslint 베이스라인 대조**: HEAD 58 problems(55 err) → R16 후 7 problems(4 err). 감소 51건 전부 any 해소. 잔여 7건은 git stash 대조로 전부 pre-existing 확정(신규 0).

**보류 1건 (R17 후보)**: `batch_analysis.ts` `result?: any` — union으로 좁히면 `report_generator.ts`(T02 영역)가 union에 없는 `.signals` 접근으로 tsc 에러 3건. 격리+동작보존 원칙상 보류. 소비처 동시 변경 필요한 교차 파일 결합 사안.

**검증**: tsc 0 · build 0 · eslint 58→7(신규0) · 환각 재발 0(디스크 실변경 21파일 ↔ handover diff 일치) · 격리 0(T01∩T02=∅).

**관련 문서**: 통합 보고서 `docs/handover/2026-06-02-R16-_SUMMARY.md`, orchestration `docs/orchestration/2026-06-02-R16-type-cleanup/`, solution `docs/solutions/2026-06-02-dispatch-hallucination-guard.md`.

---

### 2026-06-02 (세션 44) — daily-cron 자체호스팅 런북 + Claude간 메신저 온보딩

**대화 토픽 흐름**: 새 세션 시작·상태 확인 → "GitHub billing 왜 필요?" Q&A(daily-cron 구조·Public≠무료 정정) → 자체 서버 운영 요청서 작성(코드 5개 직독·런북 v1) → 양평 Claude 교차검토 수신(PART A 런북 + PART B 메신저) → 런북 v2 보강(종료코드 유실 등 8건)·A-4 검증 → 메신저 연결(ypmsg list/text/read round-trip)

**성격**: 단독 세션, **코드 변경 0**. 운영 문서 작성 + 외부 메신저 채널 연결.

**변경 사항**:
- **신규 `docs/DAILY_CRON_SELFHOST.md`** (런북 v2): GitHub Actions billing 차단으로 멈춘 daily-cron을 운영자 자체 서버에서 대체 실행하는 자기완결 런북. 3스크립트(update-news→update-market-data→daily_cron) 순서실행·필수 env 2개(`NEXT_PUBLIC_SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY`)·crontab/systemd/Windows 등록·검증 체크리스트·트러블슈팅.
- **`CLAUDE.md`**: 개발 규칙 표에 DAILY_CRON_SELFHOST.md 노드 등록.
- **(글로벌 메모리)** `inter-agent-messenger-coinchart.md` 신규 + `MEMORY.md` 인덱스 2행 추가(레포 밖, git 비대상).

**주요 논의**:
- **코드 직독으로 확인한 런북 사실**(추측 배제): 로그는 stdout/stderr만(`createLogger`=console-only, 파일 미생성) / 알림 stub(`sendAlert`=console.log → SMTP 불필요) / 킬스위치 `NEXT_PUBLIC_DISABLE_AUTOMATION`는 배치 경로 미연결 / 필수 env 2개(TwelveData는 실질 미사용) / 분석은 2단계 적재 데이터를 읽어 순서 의존 / 분석은 5분봉 50개+ 기대인데 적재는 일봉 5일치 → "Insufficient data" 가능(기존 Actions도 동일 구조).
- **양평 Claude 교차검토 반영(v2)**: 🔴 ① 래퍼 종료코드 유실(마지막 `echo`가 종료코드를 0으로 덮음 → `rc=$?`+`exit $rc`로 수정, **명백한 누락**) · ② `NODE_ENV=production` 시 `npm ci`가 devDep `tsx` 누락 → `--include=dev` 가드 · ③ `cd \|\| exit 1` · ⑤ EnvironmentFile/dotenv 비호환 경고 · ⑥ `Wants=network-online.target` · ⑦ Windows PATH+`$LASTEXITCODE` · A-3 nits(로그 chown·UTC/KST 날짜·멱등 TZ).
- **A-4 검증**: dotenv = `path.resolve(__dirname,'../.env.local')` __dirname 기준 확정(CWD 무관) / tsx = devDependency 확정 / NODE_ENV 실값은 운영 서버 소관이라 **코드로 단언 불가 → 운영자 확인 대기**(양평 예시 "production"을 베끼지 않음 — 환각 방지).
- **메신저 온보딩**: `ypmsg` 스킬(`~/.claude/skills/ypmsg/`)·번들(`F:\11_dev\_external-repos\ypmsg-bundle`)로 양평 서버(stylelucky4u.com) 연결. agent-coinchart, DIRECT 방 `5dacf8e0…`·#ops `eef46654…`. env 3개 주입 후 `list`(exit 0)→`text` 송신(messageId 발급)→`read` 게시 확인 = **양방향 round-trip 정상**. 키는 env로만, 파일·커밋·로그 평문 미저장.

**의사결정**: billing 해소 대신 자체 호스팅(사용자) / 런북 NODE_ENV 가드(양쪽 안전) / NODE_ENV 단언 안 함(환각 방지) / 메신저 Skill 방식 연결(MCP 보류).

**알려진 이슈**: 분석 단계 캔들 부족은 구조적(자체 호스팅과 무관, 기존 Actions도 동일) — 별도 과제 분리. billing은 자체 호스팅으로 daily-cron 목적상 우회됨.

**관련 문서**: handover `docs/handover/2026-06-02-session44-daily-cron-selfhost-messenger.md`, solution `docs/solutions/2026-06-02-selfhosted-cron-runbook-exit-code.md`, 런북 `docs/DAILY_CRON_SELFHOST.md`.

---

### 2026-06-03 (세션 45) — 양평 cron 실행 요청 송신 + ypmsg_coinchart 전용 스킬

**대화 토픽 흐름**: 양평 cron 이관 현황 파악 → "양평에 요청사항 적어줘"(메신저 송신) → 키 `.env.local` 저장 → 요청사항 송신 → "ypmsg → ypmsg_coinchart 전용 스킬로"

**성격**: 단독 세션, **앱 코드 변경 0**. 메신저 송신 + 프로젝트 전용 스킬 복제.

**변경 사항**:
- **신규 `.claude/skills/ypmsg_coinchart/`** (6파일): 글로벌 `~/.claude/skills/ypmsg/` 복제 + `SKILL.md` `name: ypmsg_coinchart` + coinchart 특화(agent-coinchart·DIRECT 방 `5dacf8e0…`·#ops·`.env.local` 키 셸주입 원라이너·서버 환경값 단언금지 가드). README 경로 갱신.
- **`.env.local`**: YP 메신저 env 3개 추가(`YP_AGENT_KEY`/`YP_MSG_TENANT`/`YP_MSG_BASE_URL`). git 미추적(`.gitignore` `.env*`) — 커밋 대상 아님.
- **신규 `docs/handover/_index.md`**: handover 탐색 인덱스(부재 WARN 해소, 최근 12세션 + 디렉토리 안내).
- **(글로벌 메모리)** `inter-agent-messenger-coinchart.md`: 키 `.env.local` 저장 + 셸 주입 방식 기록.

**주요 논의**:
- **현황**: 양평 cron 이관은 "런북 v2·양평 교차검토·메신저 채널 100% 완료, 실제 서버 실행만 운영자에 남음". 검토 회신(④dotenv·②NODE_ENV)은 세션44에 이미 송신됨을 `read`로 확인.
- **메신저 송신**: `ypmsg.mjs`가 `.env.local`을 자동 로드하지 않고 셸 `process.env`만 읽음(소스 확인) → PowerShell 한 호출에서 `Get-Content .env.local | ? YP_ | Set-Item env:` 주입 후 실행. DIRECT 방 `read`로 중복 방지 후 **실행 요청사항 신규 송신**(messageId `fa0703ab…`): 셋업(`npm ci --include=dev`·env 2개)·수동검증(3스크립트 순서·종료코드0)·cron(UTC 21:00·systemd·`exit $rc`)·회신 5항목(NODE_ENV·node -v·실행결과·등록방식+TZ·로그경로)·구조이슈(5분봉 부족).
- **전용 스킬**: 글로벌 `ypmsg`는 yangpyeon/silvercare/almanac 공용이라 **보존**(rename 금지) + 프로젝트 전용 복제. 프로젝트 경로 CLI `list` 동작 검증 PASS.

**의사결정**: 키 `.env.local` 저장+셸 주입 / 검토 회신 중복 송신 안 함(`read` 확인) / 글로벌 ypmsg 보존+전용 복제 / 서버 환경값(NODE_ENV) 단언 금지(환각 방지).

**알려진 이슈**: 🟡 양평 cron 실행 회신(5항목) 대기. 🔴 GitHub Billing·watchlist 실로그인 스모크(사용자). 🟡 R17 교차결합. ⚠️ 분석 5분봉 부족(구조적).

**관련 문서**: handover `docs/handover/2026-06-03-session45-ypmsg-coinchart.md`.

### 2026-06-03 (세션 46) — 양평 조치 회신: batch SSOT 정렬 + supabaseAdmin lazy + insert silent failure 차단

**대화 토픽 흐름**: 양평 메시지 확인(ypmsg inbox/read) → 판단 요청+4건 코드 실측 → 코드 3건 일괄 구현(#1·#4·#2) → 양평 회신 → #3 키로테이션 철회

**변경 사항**:
- `lib/constants.ts`: `SUPPORTED_COINS`에 ADA/AVAX/DOT 추가 (6→9종)
- `scripts/batch_analysis.ts`: 분석목록 하드코딩 제거 → `SUPPORTED_COINS.map(symbol)` 참조(SSOT) + batch_runs/batch_analysis_results insert·update `.error` 검사 + 전건 실패 `[CRITICAL]` 로그
- `scripts/alert_engine.ts`: `alert_history` insert `.error` 검사 (warn→error)
- `lib/supabaseAdmin.ts`: module-load createClient → 첫 접근 lazy(Proxy + bind), 호출처 6파일 무수정
- `docs/references/_API_REFERENCE.md`: 지원 코인 6→9종 갱신

**주요 논의**:
- #1 근본원인 = SSOT 위반(분석 하드코딩 8종 vs 수집 SUPPORTED_COINS 6종). ADA/AVAX/DOT 미수집→0 candles, BCH 미분석→낭비. 사용자 선택: 9종 확대.
- #4 = ESM import 호이스팅이 dotenv.config()보다 먼저 실행 → supabaseAdmin module-load createClient가 빈 env로 throw(exit 1). Proxy lazy로 영구 수정. 양평 래퍼 우회 없이도 tsx 직접 실행 생존.
- #2 = supabase-js PostgREST 에러는 throw 아닌 `.error` 반환 → try/catch가 못 잡는 silent failure. `.error` 명시 검사로 loud화. 단 테이블 부재(PGRST205) 자체는 DB 작업이라 throw까지는 안 함(매일 exit 1 방지).
- 검증: tsc exit 0, eslint 통과, lazy 스모크 3/3(env 빈 로드 생존/접근 throw/주입 후 정상).
- #3 service_role 키 로테이션: 사용자 지시로 철회(개발 단계, 운영 활성화 시 직접 처리). 양평에 정정 송신.

**의사결정**: 코인 9종 확대 / supabaseAdmin Proxy lazy(호출처 무수정) / insert 실패 throw 안 함(로그만, 매일 죽는 것 방지) / #3 철회.

**알려진 이슈**: 🟡 PGRST205 3개 테이블 부재(DB작업). 🟡 ADA/AVAX/DOT 5분봉 누적 전 Insufficient 가능. 🟡 5분봉 기대 vs 일봉 적재 구조(양평 §5). 🟡 R17 교차결합.

**관련 문서**: handover `docs/handover/2026-06-03-session46-batch-ssot-lazy-silentfailure.md`, solution `docs/solutions/2026-06-03-supabaseadmin-lazy-and-postgrest-silent-failure.md`.

---

### 2026-06-03 (세션 47) — PGRST205 종료 + R17 교차결합 정리 (kdyswarm 2트랙 worktree 병렬)

**대화 토픽 흐름**: 새 세션 시작 점검(current/handover) → 양평 inbox 확인(미읽음 0, 06:00 관측 미회신) → 작업 결정(모두 진행+kdydispatch/kdyswarm 적극 활용) → kdydispatch 진입 → Explore 2병렬 범위 정찰(PGRST205 테이블 스키마·R17 교차결합) → kdyswarm 2트랙 worktree 병렬 발사 → 통합·전역검증 → 운영 DB 적용(Management API) → cs

**변경 사항**:
- **T1 (PGRST205)**: `supabase/migrations/20260603000001_create_batch_tables.sql` 신규 — 3개 테이블(batch_runs 11컬럼·batch_analysis_results 6·alert_history 10) CREATE TABLE IF NOT EXISTS, FK 2개(batch_id→batch_runs ON DELETE CASCADE), 인덱스 6개, RLS service_role full-access(anon/auth 기본 차단), 멱등 패턴. 코드 insert 컬럼을 SSOT로 역설계. `_SCHEMA_REFERENCE.md` 3테이블 섹션 추가.
- **T2 (R17)**: `scripts/report_generator.ts` — `result.signals`(부재)→`result.probability?.signals` + `signal.type==='buy'/'sell'`→`signal.signal==='BUY'/'SELL'`(IndicatorSignal 실필드, `types/probability.ts` 확인) 정정(107·108·109·110·185행). `scripts/batch_analysis.ts` — `AnalysisRecord.result?:any`→`AnalysisResult|StockAnalysisResult` union + import 2종 추가 + R16 eslint-disable/보류주석 제거. `_TYPE_REFERENCE.md` 배치 분석 타입 섹션 추가.
- **운영 DB 적용**: Management API `database/query`로 마이그레이션 트랜잭션 적용 → 3테이블 생성 검증(컬럼·인덱스·RLS·FK) → `NOTIFY pgrst, 'reload schema'` → `schema_migrations` `20260603000001` backfill. `_SCHEMA_REFERENCE.md` "미적용"→"적용 완료" 정정.
- `.gitignore`에 `.claude/worktrees/` 추가.

**주요 논의**:
- **kdydispatch vs kdyswarm**: 작업 2건이고 본 세션 자율 진행 가능(사용자 복붙 매개 불필요) → kdydispatch 자율 위임 게이트(--subagent→kdyswarm) 패턴에 해당. 외부 N-터미널 대신 본 세션 worktree 병렬 채택. 쓰기 영역 disjoint(T1=migrations/SCHEMA vs T2=scripts/TYPE) 확인 후 발사.
- **T2 추가 버그 발견**: R17은 `result?:any` union화가 목적이었으나, report_generator가 `result.signals`(존재X)를 기대하고 `signal.type`(IndicatorSignal엔 `.signal`)을 비교 → any가 tsc를 무력화해 런타임에 buy/sell 카운트가 항상 0이던 잠복 버그. 경로(`result.probability.signals`)+필드(`signal.signal`)+대소문자(BUY) 3중 정정으로 함께 교정.
- **Management API UTF-8 함정**: PowerShell `ConvertTo-Json` + `Invoke-RestMethod` 기본 전송이 한글 SQL 주석을 CP949로 인코딩 → 서버가 malformed JSON(400, position 774=한글 지점). `[Text.Encoding]::UTF8.GetBytes()` + `charset=utf-8`로 바이트 전송하여 해소.

**의사결정**: PGRST205 운영 DB 지금 적용(내일 cron 즉시 저장) / R17 방안 B(최소 침해, 동작보존) / 세션 종료(cs).

**알려진 이슈**: 🟡 내일 06:00 KST cron 저장 관측(양평에 요청). 🟡 `alert_history.status`에 코드가 insert 안 하는 'pending'을 `shouldSendAlert`가 필터에 사용하는 불일치(CHECK 미적용으로 안전 처리, 추후 정리 후보). 🟡 5분봉 기대 vs 일봉 적재 구조(양평 §5).

**관련 문서**: handover `docs/handover/2026-06-03-session47-batch-tables-r17.md`, solution `docs/solutions/2026-06-03-management-api-utf8-korean-sql.md`.

### 2026-06-03 (세션 48) — alert 'pending' 정리 + 일봉 적재 깊이 확대

**대화 토픽 흐름**: 새 세션 점검(current/handover/next-dev) → 양평 inbox 확인(미읽음 0·06:00 관측 미회신·06-04 첫 정상실행이라 시점 미도래) → 작업 결정(#2·#3 코드 과제) → 코드 정찰(alert_engine/update-market-data/batch_analysis/마이그/crypto/signals 직독) → brainstorming(설계 확정) → 설계 doc 작성 → 구현 4파일 → tsc/eslint/vitest 검증 → cs

**변경 사항**:
- **#2 (alert 'pending')**: `scripts/alert_engine.ts` `shouldSendAlert` — `.in('status', ['sent','pending'])`→`.eq('status','sent')`(코드가 insert 안 하는 죽은 'pending' 제거) + `.single()`→`.order('triggered_at', desc).limit(1).maybeSingle()`(24h 내 동일 알림 다중행 시 `.single()` 에러→fail-open 중복발송 잠재버그 차단). `supabase/migrations/20260603000001_*.sql` L63-64 주석 정합(SQL 객체 무변경). `docs/references/_SCHEMA_REFERENCE.md` alert_history 설명 정정.
- **#3 (일봉 적재)**: `scripts/update-market-data.ts` `fetchYahooData` Yahoo URL `range=5d`→`range=1y`(crypto·stock 공용, 일봉 ~250봉). `scripts/batch_analysis.ts` "Last 24 hours (5min * 288)" 환각 주석 → "최근 288 거래일(일봉, ~1년)" ×2(crypto·stock).
- 설계 doc `docs/superpowers/specs/2026-06-03-alert-pending-daily-candle-design.md` 신규.

**주요 논의**:
- **#3 진짜 원인 규명**: 양평 §5가 "5분봉 288 vs 일봉" 미스매치로 분류했으나, 코드 실측 결과 분석 파이프라인 전체가 일봉 가정(`timeframe:'1d'`·VWAP `'daily'`·`market_prices` symbol+date PK·`fetchCryptoMarketPrices`가 `date` 조회). "5min × 288" 주석이 실동작과 불일치하는 환각 주석. 진짜 원인은 `update-market-data`가 `range=5d`로 일봉 5봉만 적재 → `generateSignals` 최소 50봉 미달. **적재 깊이만 1년으로 확대**하면 근본 해소.
- **방향 결정(brainstorming)**: #3=A(일봉 유지+range 확대, B 5분봉 전환=스키마/엔진 대공사 YAGNI 기각). #2=A(코드 정합 정리, B pending 상태머신=sendAlert stub이라 YAGNI 기각).
- **운영 영향**: 순수 코드 변경. 운영 DB 직접 적용 불요 — #3는 양평 다음 06:00 cron의 `upsert`가 일봉 자동 백필, #2는 스키마 무변경.

**의사결정**: #3 일봉 유지+range=1y / #2 죽은 'pending' 제거+maybeSingle 다중행 안전화 / 세션 종료(cs).

**검증**: 전역 `npx tsc --noEmit` exit 0 · `npx eslint`(3 scripts) exit 0 · `npx vitest run` 27/27 passed(회귀 0) · `git diff --stat` 4파일 일치(환각 0). 기존 테스트는 이 4파일 미커버 → 신규 단위테스트 가성비 낮음 판단.

**알려진 이슈**: 🟡 2026-06-04 06:00 KST cron 관측(range=1y 일봉 ~250봉 적재 + Insufficient 해소 + batch 저장) → 양평 관측 요청 필요. 🟡 5분봉 인트라데이는 비목표(YAGNI)로 명시 기각.

**관련 문서**: handover `docs/handover/2026-06-03-session48-alert-pending-daily-candle.md`, 설계 `docs/superpowers/specs/2026-06-03-alert-pending-daily-candle-design.md`, 저널 `docs/logs/journal-2026-06-03.md`.

---

### 2026-06-03 (세션 49) — 양평 통지 송신 + R17 잔여 마감 (watchlist sync E2E 스모크)

**대화 토픽 흐름**: 세션시작(세션48 cs 보고) → 양평 통지 송신 → R17 잔여 정의 + §4 DB레이어 재실증 → brainstorming(Playwright 스모크 설계) → 구현+풀검증 → cs

**변경 사항**:
- 신규 `e2e/watchlist-member.spec.ts` (TC1 §3 익명적재→sync200·손실0 / TC2 §5 빈컨텍스트→DB복원 / TC3 §6 reorder PATCH+clear DELETE?all+잔여복원없음)
- 수정 `e2e/playwright.config.ts` (`watchlist-member` project 추가 + chromium testIgnore에 spec 제외)
- 신규 `docs/superpowers/specs/2026-06-03-watchlist-sync-e2e-smoke-design.md` (설계 doc)
- 신규 `docs/solutions/2026-06-03-watchlist-sync-playwright-smoke.md` (패턴 솔루션)
- 커밋 `89dae05`(설계)·`8200e6a`(spec+config) — 로컬, cs에서 push

**주요 논의**:
- **양평 통지**: 세션48 인계 ①. ypmsg_coinchart 스킬로 DIRECT 방 직전 맥락 read → 양평이 §5에서 "5분봉 288개 기대"로 이해 중임 발견. 멘탈모델 정정(분석=일봉, '5min×288'은 환각 주석) + git pull 필요(커밋 3929e9b 반영 조건) + range 5d→1y + 06-04 관측요청을 묶어 송신(msgId b0451b85). inbox 미읽음 0.
- **R17 잔여 정의**: next-dev-prompt 직독 → R17 교차결합·PGRST205는 세션47 마감. 실질 잔여 = watchlist 실로그인 sync 스모크(R12부터 PENDING). 절차서 `docs/db/R14-watchlist-sync-smoke.md`로 2축(§4 DB레이어=Claude가능 / §3·5·6 실브라우저=자격증명필요) 파악.
- **§4 재실증**: `scripts/smoke/watchlist-sync-smoke.ts` --dry-run(PASS3/0) + --write(PASS4/0, 라운드트립 INSERT→UNIQUE충돌→reorder→DELETE 잔여0). user_watchlist 운영 적용 후 계약 정상 확인.
- **brainstorming**: 컨텍스트 탐색으로 auth.setup.ts magiclink fallback(경로B) 재사용 가능 확인. **핵심 발견 — reorder는 WatchlistView 구조분해 미포함=UI 진입점 없음**(절차서 §6-1 "드래그" 가정 stale) → PATCH API 직접검증으로 결정. 질문 4건: 범위=§3+§5+§6 전체·통합=watchlist-member project·계정=E2E_ADMIN_EMAIL 재사용·트리거=접근법A(addInitScript+storageState로 페이지 로드 시 getUser→runSync 자동발동).
- **구현/검증**: 합성 심볼 SMOKEE2E*(normalizeWatchlistInput 1~32자 허용)·afterAll service_role 정리. 풀실행(E2E_ADMIN_EMAIL=smartkdy7@gmail.com, 운영DB 합성행 임시쓰기·자동정리 동의) **4 passed**·운영DB 잔여0·미주입 graceful skip 3 skipped(회귀0)·eslint0.

**알려진 이슈**: 🟡 2026-06-04 06:00 KST cron 관측(양평 git pull 선행 필수 — range=1y 반영 조건). 🟢 R17 완전 마감(교차결합·PGRST205 세션47 + watchlist sync 스모크 세션49). watchlist reorder UI 부재 = 별도 기능 과제(스모크는 PATCH API만 검증).

**관련 문서**: handover `docs/handover/2026-06-03-session49-watchlist-sync-e2e.md`, 설계 `docs/superpowers/specs/2026-06-03-watchlist-sync-e2e-smoke-design.md`, 솔루션 `docs/solutions/2026-06-03-watchlist-sync-playwright-smoke.md`, 저널 `docs/logs/journal-2026-06-03.md`.
