# 2026-05 작업 로그

---

### 2026-05-23 (세션 10) — R1/T07 일꾼: 익명 bcrypt + IP 마스킹 + middleware 머지

**대화 토픽 흐름**: R1 dispatch 일꾼 T07 발사 → 작업 명세서(T07-auth-middleware.md) 정독 → SOT(package.json, PROJECT_DIRECTION §3, ENV/TYPE 레퍼런스, 기존 `middleware.ts`) read → 산출물 5종 생성 → 기존 supabase 미들웨어와 머지 → tsx 런타임 검증 → tsc/eslint → handover

**역할**: R1 라운드(mainpage) **단일 일꾼** 세션. 다른 일꾼(T01/T05/T08/T14)은 별도 터미널에서 동시 작업 중. 본 세션은 자체 산출물(T07)만 책임. R1 통합은 컨덕터 영역. (세션 번호 9는 T01/T14/T05/T08 일꾼들이 logs를 동시 갱신하며 충돌 — 본 일꾼은 10으로 채택)

**변경 사항 (T07 한정, 5개 파일)**:

신규:
- `lib/community/auth.ts` — `bcryptjs` 기반 해시/검증 + `validateGuestNickname` (2~12자)
- `lib/community/ip-mask.ts` — `extractClientIp` / `maskIp` / `hashIp` (HMAC-SHA256)
- `docs/handover/2026-05-23-R1-T07-auth-middleware.md` — 일꾼 인수인계

수정:
- `middleware.ts` — 기존 Supabase 세션 새로고침 + 보호 경로 리다이렉트 로직 보존. IP 헤더 3종(`x-client-ip`/`-masked`/`-hash`) 주입 로직을 supabase 호출 앞에 배치. matcher에 `/api/board/:path*`, `/api/community/:path*` 추가. `/api/` 경로는 supabase getUser 스킵
- `docs/references/_ENV_REFERENCE.md` — `IP_HASH_SECRET` append
- `docs/references/_TYPE_REFERENCE.md` — `auth · ip-mask 모듈` 섹션 append (T12가 의존할 헤더 명세 포함)

**주요 논의**:

1. **bcryptjs 미설치 — PARTIAL 처리** — `package.json` Read 결과 `bcryptjs` 부재 확인. 본 일꾼은 `package.json` 수정 권한 없음(작업 명세서 안티패턴). 코드는 정상 작성하고 handover의 "미해결" 섹션에 `npm install bcryptjs @types/bcryptjs`를 메인 터미널 후속 조치로 명시. TS 에러는 bcryptjs 1건만 발생, 설치 즉시 해결.

2. **middleware 머지 전략** — 기존 `middleware.ts`가 supabase 세션 + 보호 경로 리다이렉트를 수행 중. 작업 명세서는 "신규" 라고 적시했으나 실제 존재하여 머지 결정:
   - IP 헤더 주입 → supabase 호출 → 보호 경로 리다이렉트 순서
   - `/api/` 경로는 supabase getUser 스킵 (커뮤니티 API는 자체 인증 사용)
   - `setAll` 콜백에서 `request.headers` 대신 IP 주입된 `requestHeaders` 사본 전달
   - matcher 기존 페이지 패턴 + `/api/board` + `/api/community` 추가

3. **IP 마스킹 검증 우회** — 세션 8(T03)과 동일하게 `tsx -e import('./lib/community/ip-mask')`가 named export를 `default` 아래로만 노출하는 ESM/CJS 변환 이슈 재발생. `m.default ?? m`로 우회 → `maskIp('211.34.123.45') → 211.34.*.*`, hash 64자 hex PASS.

4. **T12 의존성 인터페이스 명시** — handover §4에 헤더 3종, matcher 경로, auth 헬퍼 시그니처, 라우트 헤더 읽기 패턴까지 코드 예시 포함. T12 일꾼이 별도 컨텍스트에서 발사돼도 본 산출물만으로 board API 작성 가능.

5. **다른 일꾼 산출물 격리** — `git status`에 T01(community migrations) / T05(news classifier) / T08(chart theme) / T14(translations) 산출물 잔존. 본 일꾼 commit 대상은 T07 5개 파일만.

**검증**:
- IP 마스킹 런타임 — `maskIp('211.34.123.45') → '211.34.*.*'`, `maskIp('invalid') → '0.0.*.*'`, `hashIp('1.2.3.4').length === 64` PASS
- `npx tsc --noEmit` — bcryptjs 1건 (예상됨, 설치 후 해소). `ip-mask.ts`/`middleware.ts` PASS
- `npx eslint lib/community/auth.ts lib/community/ip-mask.ts middleware.ts` — 0 errors (1 warning: 기존 supabase setAll의 unused `options`, 본 작업 무관)
- `node -e "require('bcryptjs')"` — MODULE_NOT_FOUND (예상됨)

**다음 일꾼/컨덕터에게 메모**:
- 메인 터미널: `npm install bcryptjs @types/bcryptjs` 실행 후 R1 통합 진행
- 운영 배포 전: `IP_HASH_SECRET=<32바이트 hex>` 환경변수 설정 권장 (`openssl rand -hex 32`)
- T12(board API): 헤더 3종(`x-client-ip-masked`, `x-client-ip-hash`)을 `community_posts.guest_ip_masked` / `community_post_likes.ip_hash` 컬럼에 저장. T01 시드 스키마(`guest_ip_masked` regex `^\d+\.\d+\.\*\.\*$`)와 본 마스킹 출력이 일치 확인됨

---

### 2026-05-23 (세션 9) — R1/T14 일꾼: 번역 키 정리 + 헤더 인라인 분기 제거

**대화 토픽 흐름**: R1 dispatch 일꾼 T14 발사 → 작업 명세서(T14-translations-cleanup.md) 정독 → SOT 2개(lib/translations.ts, components/global-header.tsx) read → menu 그룹에 신규 키 9개(ko/en) append → 헤더의 인라인 한/영 분기 8건을 `t.menu.*` 호출로 교체 → 잔여 분기 4건 식별 → tsc 검증 → handover 작성

**역할**: R1 라운드(mainpage) 단일 일꾼 세션. 다른 일꾼(T01/T05/T07/T08 등)은 별도 터미널에서 동시 작업 중이며 워킹트리에 산출물 잔존. 본 세션은 T14 산출물만 책임지며 다른 일꾼 파일은 미터치(컨덕터 통합 영역).

**변경 사항 (T14 한정, 3개 파일)**:

수정 (append-only):
- `lib/translations.ts` — `translations.ko.menu` / `translations.en.menu` 각각 끝에 9개 키 신규 append: `best`, `boardFree`, `boardMarket`, `boardInfo`, `coinRoom`, `tools`, `write`, `search`, `login`. `news`는 기존 존재로 중복 회피. 기존 키 0건 수정
- `components/global-header.tsx` — JSX 구조·아이콘·className 무변경. 인라인 분기 8건만 `t.menu.{key}` 형식으로 교체 (L43~46 1차 메뉴 5개 중 4개, L52 coinRoom 라벨, L65 tools 라벨, L153 데스크탑 글쓰기, L207 모바일 글쓰기)

신규:
- `docs/handover/2026-05-23-R1-T14-translations-cleanup.md` — 일꾼 인수인계

**잔여 인라인 분기 4건 (의도적)**:
- `lang === "ko" ? "알트코인" : "Altcoin"` (L58) — 스펙 키 목록 외
- `lang === "ko" ? "김치프리미엄" : "Kimchi Premium"` (L59) — 스펙 키 목록 외
- `lang === "ko" ? "EN" : "KR"` (L143, L213) — 언어 토글 라벨은 본질적으로 lang 반대를 표시해야 하므로 단일 키 환원 불가능

**검증 결과**:
- 번역 키 추가 검증: `grep -c "best:|boardFree:|...|write:" lib/translations.ts` = 14 (= 7키 × ko/en, 사양 "14 이상" 충족)
- 인라인 분기 잔여 검증: `grep -c "lang === \"ko\" ?" components/global-header.tsx` = 4 (의도적 잔류와 일치)
- `npx tsc --noEmit` — 본 작업 수정 파일 관련 에러 0건. 사전 에러 3건(`lib/chart/theme.ts` × 2 'solid' 할당, `lib/community/auth.ts` bcryptjs 모듈 미존재)은 본 T14 무관 (다른 일꾼 책임 또는 컨덕터 통합 단계 책임)

**커밋**: 본 일꾼은 커밋하지 않음 (컨덕터가 R1 일꾼 전체 산출물을 통합 커밋)

---

### 2026-05-23 (세션 9) — R1/T08 일꾼: 차트 라이트 테마 + BlogEditor `tone` prop

**대화 토픽 흐름**: R1 dispatch 일꾼 T08 발사 → 작업 명세서(T08-chart-theme-editor-tone.md) 정독 → SessionStart hook 범위(T15)와 task 범위(T08) 불일치 인지 → BlogEditor 실제 경로 확인(`components/Blog/editor/`) → 디자인 토큰 검증(globals.css) → lightweight-charts 버전 확인(v5.0.9) → `lib/chart/theme.ts` 작성 → BlogEditor/EditorToolbar tone prop 추가 → tsc에서 ColorType enum 이슈 발견·해결 → 검증 통과 → handover 작성

**역할**: R1 라운드(mainpage) 단일 일꾼 세션. 1차 발사(의존 없음). 본 세션 산출물에 **T09(signal)·T10(analysis)·T11(market) 후속 일꾼이 의존**. 페이지 적용은 후속 일꾼 영역, 본 일꾼은 인프라 모듈만 책임.

**변경 사항 (T08 한정, 4개 파일)**:

신규:
- `lib/chart/theme.ts` (+71) — `LIGHT_CHART_THEME` / `DARK_CHART_THEME` / `KR_CANDLE_COLORS` / `US_CANDLE_COLORS` + `getChartTheme(tone)` / `getCandleColors(scheme)` 헬퍼
- `docs/handover/2026-05-23-R1-T08-chart-theme-editor-tone.md` — 일꾼 인수인계 (T09·T10·T11 사용법 안내)
- `docs/solutions/2026-05-23-lightweight-charts-v5-colortype-enum.md` — compound knowledge

수정:
- `components/Blog/editor/BlogEditor.tsx` — `tone?: 'light' | 'dark'` prop 추가 (default `'light'`), `EditorTone` 타입 export, EditorContent prose 클래스 분기(다크 시만 `prose-invert`), containerClass 분기, EditorToolbar에 tone 전파
- `components/Blog/editor/EditorToolbar.tsx` — `tone?` prop 추가(default `'light'`), wrapper className만 light/dark 분기 (`bg-surface-container border-outline-variant text-on-surface` ↔ 기존 다크)

**주요 논의**:

1. **Hook 범위 vs Task 범위 불일치** — SessionStart hook은 "이 터미널 범위: 일꾼 T15. 작업 허용 파일: app/page.tsx;lib/community/*;docs/*"를 안내했으나 사용자 명시 지시는 T08. T08 산출물 경로(`lib/chart/`, `components/Blog/editor/`)는 hook 허용 목록에 없었지만 사용자 지시 우선 원칙으로 진행 → 실제 hook 차단 미발생. (dispatch에서 발사 프롬프트의 hook payload가 다른 일꾼 것이 잘못 주입된 듯)

2. **EditorToolbar tone 전파 — spec vs 안티패턴 충돌** — spec line 122-124("toolbar 분기")와 안티패턴 line 168("BlogEditor의 다른 부분 수정 금지")이 충돌. 해석 B(안티패턴의 "다른 부분"은 TipTap extensions/이미지 업로드 같은 기능 로직) + 절충안 채택 — EditorToolbar에 tone prop 추가하되 wrapper div className만 분기, ToolButton 내부 색상은 minimal diff 원칙으로 미수정. 후속 R2에서 ToolButton 자체에 tone-aware 색상 분기 추가 권장.

3. **lightweight-charts v5 ColorType enum 호환성** — spec의 `background: { type: 'solid' as const }` 코드를 그대로 작성 시 `TS2322: Type '"solid"' is not assignable to type 'ColorType | undefined'` 발생. v5에서 `ColorType`이 string literal union이 아닌 enum으로 강화됨. `import { ColorType }` value import + `type: ColorType.Solid`로 교체. 별도 솔루션 문서로 기록(`docs/solutions/2026-05-23-lightweight-charts-v5-colortype-enum.md`).

4. **BlogEditor 경로 케이스 차이** — spec은 `components/blog/BlogEditor.tsx`, 실제는 `components/Blog/editor/BlogEditor.tsx`(대문자 Blog + editor 하위). Windows case-insensitive라 동일 경로처럼 보이지만 Vercel(Linux)은 case-sensitive — 실제 경로로 작성 + handover 명시.

**검증**:
- `npx tsc --noEmit`: T08 영역(`lib/chart`, `components/Blog/editor`) 에러 0. ColorType 이슈 수정 후 T14/T07 등 다른 일꾼이 기록한 "사전 에러 2건(lib/chart/theme.ts 'solid' 할당)"은 본 세션에서 해소됨
- `grep theme.ts 심볼` = 8 (기준 6+)
- `grep BlogEditor "tone"` = 6 (기준 4+)
- `npx eslint`: T08 파일 3개 에러 0
- `npm run build`: `lib/community/auth.ts`의 bcryptjs 누락(T07 영역)으로 격리 검증 불가 → tsc로 대체

**커밋**: 본 일꾼은 커밋하지 않음 (컨덕터가 R1 일꾼 전체 산출물을 통합 커밋). /cs 5단계에서도 R1 다른 일꾼들과의 정합성 유지를 위해 자동 커밋 보류 권장.

**다음 일꾼/컨덕터에게 메모**:
- T09(signal)·T10(analysis)·T11(market): 기존 차트 컴포넌트(`components/Chart/CryptoChart.tsx`, `components/Chart/StockChart.tsx`, `components/DetailedChart.tsx`, `components/hero-chart.tsx`)에서 차트 옵션 하드코딩을 `getChartTheme('light')` + `getCandleColors('kr')` 호출로 교체. CandlestickSeries는 `addSeries(CandlestickSeries, { ...getCandleColors('kr') })` 형태.
- 블로그 라이트화 일꾼: `<BlogEditor tone="light" />` 또는 default 그대로 사용.

---

### 2026-05-23 (세션 9) — R1/T01 일꾼: community_* 4테이블 마이그레이션

**대화 토픽 흐름**: R1 dispatch 일꾼 T01 발사 → 작업 명세서(T01-community-migrations.md) 정독 → SOT 9개 병렬 read → 산출물 2종(SQL + MD append) 생성 → SQL 키워드 grep 카운트 검증 → tsc 무영향 확인 → handover 작성

**역할**: R1 라운드(mainpage) **단일 일꾼** 세션. 다른 일꾼(T05/T07/T08/T14 등)은 별도 터미널에서 동시 작업 중이며 워킹트리에 산출물 잔존. 본 세션은 T01 산출물만 책임지며 다른 일꾼 파일은 미터치(컨덕터 통합 영역).

**변경 사항 (T01 한정, 3개 파일)**:

신규:
- `supabase/migrations/20260523_create_community_tables.sql` — 4 테이블 + 시드 9행 + 인덱스 6(일반 4 + UNIQUE 부분 2) + 트리거 4 + RLS 15
- `docs/handover/2026-05-23-R1-T01-community-migrations.md` — 일꾼 인수인계

수정 (append-only):
- `docs/references/_SCHEMA_REFERENCE.md` — `## community_*` 섹션 추가 (기존 줄 0건 수정)

**주요 논의/설계**:

1. **익명 XOR 회원 CHECK 제약** — `author_id IS NOT NULL OR (guest_nickname/password_hash/ip_masked 3요소 NOT NULL)`로 DB 레벨에서 강제. 컬럼별 추가 CHECK: `guest_nickname` 2~12자, `guest_password_hash` ≥60자(bcrypt 출력 가정), `guest_ip_masked` regex `^\d+\.\d+\.\*\.\*$`로 "앞 2옥텟만" 형식 강제.

2. **추천/비추 식별 단위 분리** — `community_post_likes`는 회원=`user_id`, 익명=`ip_hash`(sha256 전체 IP, 마스킹 X) 두 축으로 dedup. **작성용 `guest_ip_masked`와 별개** — dedup 정확도가 필요하므로 표시용 마스킹과 분리. `UNIQUE` 부분 인덱스 2종으로 NULL 안전 처리(`WHERE user_id IS NOT NULL` / `WHERE ip_hash IS NOT NULL`).

3. **트리거 채택 vs 비채택** — `updated_at`·`comment_count`·`like_count` 자동 동기화는 채택 (3종 트리거 함수 + 4개 트리거). `is_hot` 자동화는 임계치 미정으로 비채택(배치 스크립트 권장). `like_count`는 비추(-1) 합산이라 **음수 가능** — 화면 단독 표시 시 별도 집계 필요(차후 결정).

4. **RLS 익명 수정/삭제 미허용** — 의도된 설계. 익명 글 UPDATE/DELETE 정책은 `author_id = auth.uid()`만 통과 → 익명은 본 세션 RLS로 못 통과. 서버 라우트(T12 영역)가 비번 검증 후 service_role 키로 처리하는 패턴. INSERT만 전체 허용(CHECK 제약이 강제).

5. **시드 9행 범위** — `free`/`market`/`info` + 코인룸 6종(`coin-btc/eth/xrp/sol/altcoin/kimp`). 게시판 메타만 — 게시글 더미는 T02 영역으로 격리(안티패턴 회피).

6. **다른 일꾼 산출물 격리** — `git status`에 T05(news-classifier), T07(auth-middleware), T08(chart-theme-editor-tone), T14(translations-cleanup) 산출물이 워킹트리에 잔존. 본 일꾼은 자기 3개 파일만 명시적 staging → commit → push. R1 통합은 컨덕터 영역.

**검증**:
- `grep -c "CREATE TABLE"` = 4 (기대 4) ✓
- `grep -c "CREATE INDEX"` = 4 일반 + UNIQUE 부분 2 = 총 6 (기대 ≥4) ✓
- `grep -c "CREATE POLICY"` = 15 (기대 ≥8) ✓
- `grep -c "CREATE TRIGGER"` = 4 ✓
- `npx tsc --noEmit` — 본 산출물(SQL/MD)은 TS 영향 0. 워킹트리의 다른 일꾼 파일(`lib/community/auth.ts`)의 `bcryptjs` 모듈 미설치 에러 1건은 본 작업 무관(T07 영역).
- `psql` dry-run은 로컬 Supabase 미가동으로 명세에 따라 SKIP.

**다음 일꾼에게 메모**:
- T02(게시글 시드): `board_slug` enum 9종 + 익명 IP 형식은 `211.34.*.*` (마스킹 적용된 형태로 INSERT)
- T12(작성 API): `community_post_likes`는 부분 UNIQUE 2종 + `ON CONFLICT ... DO UPDATE` 패턴 권장, value 부호 변경은 트리거가 자동 차분 반영
- T13(코인룸 라우팅): `coin_symbol` NULL = 코인 무관 일반 글. 코인룸 페이지는 `board_slug='coin-btc' OR coin_symbol='BTC'` 양 축 OR 쿼리 권장

---

### 2026-05-23 (세션 8) — R1/T03 일꾼: Binance ticker SSOT + `/api/coins/ticker`

**대화 토픽 흐름**: R1 dispatch 일꾼 T03 발사 → 작업 명세서 정독 → SOT 6개 병렬 read → 산출물 5종 생성 → tsc/eslint 통과 → Binance 응답 정규화 실 호출 검증 → handover 작성

**역할**: R1 라운드(mainpage) **단일 일꾼** 세션. 다른 일꾼(T01/T07/T14 등)은 별도 터미널에서 동시 작업 중. 본 세션은 자체 산출물(T03)만 책임지며, 글로벌 상태(R1 통합)는 컨덕터 영역.

**변경 사항 (T03 한정, 6개 파일, +90/-0)**:

신규:
- `types/coins.ts` — `CoinTicker` / `CoinSnapshot` 타입
- `app/api/coins/ticker/route.ts` — `GET /api/coins/ticker?symbols=...`
- `docs/handover/2026-05-23-R1-T03-ticker-ssot.md` — 일꾼 인수인계

수정 (append-only):
- `lib/supabase/crypto.ts` — `fetchBinanceTickers(symbols)` + `fetchCommunityTickers()` + 60s 메모리 Map 캐시. 기존 `fetchCryptoMarketPrices` 미변경
- `docs/references/_API_REFERENCE.md` — `GET /api/coins/ticker` 섹션 append
- `docs/references/_TYPE_REFERENCE.md` — `CoinTicker / CoinSnapshot` 섹션 append

**주요 논의**:

1. **이중 캐시 정책** — Binance ticker는 호출 비용이 있지만 60s 단위로도 신선도가 충분. Next fetch `revalidate: 60` + 메모리 `Map` 60s 이중. 캐시 키는 `symbols.slice().sort().join(",")`로 정렬 일관성 확보.

2. **base symbol 정규화** — `BTCUSDT` → `baseSymbol: "BTC"`로 자동 분리. `mock-coins.ts`의 코인 키(`btc`/`eth`/...)와 1:1 매핑 가능.

3. **검증 우회** — 작업 명세서의 `npx tsx -e "import('./lib/supabase/crypto')..."` 검증이 path alias `@/`를 tsx ESM 변환이 처리하면서 named export를 `default`로만 노출하는 이슈 발생. Binance API 호출/정규화는 동일 로직의 ESM 스크립트로 우회 검증 → BTC price=75414.25 (>0), baseSymbol=BTC, count=10 모두 PASS. 우회 솔루션은 `docs/solutions/`에 별도 기록.

4. **다른 일꾼 산출물 격리** — `git status`에 T01(community-migrations) / T07(auth-middleware) / T14(translations) 산출물이 워킹 트리에 잔존. 본 일꾼은 자기 6개 파일만 명시적 staging → commit → push. 컨덕터의 R1 통합 시점에 다른 일꾼들 산출물이 일괄 처리될 예정.

**검증**:
- `npx tsc --noEmit` PASS (exit 0)
- `npx eslint types/coins.ts app/api/coins/ticker/route.ts` PASS (exit 0)
- Binance 24h ticker 실 호출 PASS (count=10, BTC price>0)

---

### 2026-05-10 (세션 6+7) — v2.0 커뮤니티 피벗: 방향성 정의 + 디자인 의뢰서 + Stitch 시안 코드 적용

**대화 토픽 흐름**: 프로젝트 점검·git 최신화 → 정체성 피벗 결정(코인판×네이버 커뮤니티) → 의뢰서 6종 작성 → Stitch 시안 수령 확인 → 적용 계획 수립(Plan Mode) → Step 1~3 코드 구현 → 빌드 검증 → 커밋·푸시

**변경 사항** (56개 파일, +9,917 / −656):

신규:
- `docs/PROJECT_DIRECTION.md` — v2.0 방향성 정의 (코인판×네이버 커뮤니티 피벗)
- `docs/design-brief/00-overview.md` ~ `05-coin-room.md` + `README.md` — Stitch 의뢰서 7종
- `docs/design-brief/stitch_attachment_file_checker/` — Stitch 반환 시안 (HTML 6 + 스크린샷 6 + DESIGN.md)
- `app/board/[slug]/page.tsx`, `[postId]/page.tsx`, `write/page.tsx` — 게시판 3페이지
- `app/coin/[symbol]/page.tsx` — 코인룸 페이지
- `components/community/` 13개 (Badge, Tabs, Pagination, SidebarWidget, BoardRow, NewsRow, NewsHeadlineCard, CoinHero, 위젯 5종)
- `lib/community/mock-{posts,news,coins}.ts` — 더미 데이터 모듈

수정:
- `app/globals.css` — Material 3 토큰 + 한국식 빨/파 시맨틱 + Noto Sans KR 폰트 변수 (전면 교체)
- `app/layout.tsx` — Geist → Noto Sans KR 교체, lang ko, 라이트 톤 body
- `app/page.tsx` — 홈 전면 리디자인 (시세 마퀴 + 베스트30 + 뉴스10 + 게시판 3컬럼 + 코인룸 6카드)
- `app/news/page.tsx` — 4차원 필터 + 헤드라인 3분할 + 표 형식 리디자인
- `components/global-header.tsx` — 메뉴 5+2 구조 (베스트/자유/시세/정보/뉴스 + 코인룸▼/도구▼) + 글쓰기 CTA
- `components/footer-section.tsx` — 라이트 톤 + 커뮤니티/정보 그룹
- `CLAUDE.md` — 풀뿌리 트리에 PROJECT_DIRECTION.md, design-brief/ 등록
- `docs/status/current.md` — 세션 6, 7 진행 상태 반영

**주요 논의**:

1. **정체성 피벗 (세션 6)** — 사용자 요청: "코인판 사이트처럼 정보공유의 장을 만들고 싶다." → 4가지 핵심 결정:
   - 정보 공유 주체: **유저 커뮤니티** (코인판식)
   - AI 차트 분석: **부가 기능**으로 격리 ("도구" 메뉴), URL은 그대로
   - 뉴스 분류: **AI API 미사용**, 룰베이스 키워드 사전 + 점수
   - 디자인 톤: **네이버 스타일** (흰 배경, 정보 밀도, 표 위주)

2. **권한 모델·MVP 범위** — 익명+회원 혼용 (코인판식 IP 앞 2옥텟 마스킹), MVP는 4개 페이지(메인/게시판/상세+작성/뉴스+코인룸)

3. **Stitch 시안 분석 (세션 7 진입 시)** — 시안 HTML 직접 검사로 정확 매핑:
   - `_4`=홈, `_3`=게시판 리스트, `_2`=게시글 상세, `_5`=작성, `_1`=뉴스, `btc`=코인룸
   - Material 3 토큰 (Primary `#0050CB` + Secondary `#006E2E`) + Noto Sans KR
   - 시안 미국식 색상(녹=호재, 빨=악재) → 한국식(빨=호재, 파=악재)으로 강제 적용

4. **3가지 핵심 결정 (Plan Mode)**:
   - 컬러: 시안대로 블루+그린 (Stitch 결정 채택)
   - 라이트/다크: **전체 사이트 라이트 통일** (다크 프리미엄 톤 폐기)
   - 1차 구현 범위: 디자인 토큰 + 공통 컴포넌트 + 더미 페이지 5개 (DB는 다음 세션)

5. **단계별 실행 (Step 1~3)** — Tailwind v4 `@theme` 디렉티브로 토큰 정의, 시안 HTML의 `text-primary` `bg-surface-container` 등 클래스를 그대로 사용 가능하게 매핑. BlogEditor(TipTap) 작성 페이지에서 재사용.

6. **블로커 처리** — 빌드 전 `node_modules` 일부 누락(@tiptap, @radix-ui 등) 발견 → `npm install`로 208 패키지 추가 → tsc·next build 통과

**검증 결과**:
- TypeScript (`npx tsc --noEmit`) — 통과
- Next.js 빌드 — 41개 라우트 정상 컴파일 (Turbopack)
- 신규 코드 lint 에러 0 (기존 `scripts/` any 타입 경고는 알려진 항목)
