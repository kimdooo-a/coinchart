# 2026-05 작업 로그

---

### 2026-05-25 (세션 33) — R7 마감: 차트 라인/오버레이 색 KR 정렬(R7-3) + 토큰 bg-/border- 변형 점검(R7-4)

**대화 토픽 흐름**: 세션 시작·R7 3/4 정의 파악 → 미커밋(세션32 1/2) 공존 확인 → 3/4 대상 조사(차트 라인색·토큰 별칭) → 라인색 처리 방향 사용자 확정 → theme.ts SSOT → 차트 4종+hero 수정 → 4번 점검 결론 → 검증(tsc/build) → handover → "R7 완료?" Q&A → cs

**변경 사항** (5파일):
- `lib/chart/theme.ts`: `INDICATOR_COLORS` + `getIndicatorColors()` SSOT 신규. RSI/MACD/signal/MA7/MA25/MA99/BB·평단가선 식별색 집약. **MA99 `#FFEA00`→`#c79100`**(흰 배경 가독), BB `rgba(0,150,136,0.5→0.6)`, avgPrice `#0050cb`(brand primary)
- `components/Chart/CryptoChart.tsx`: 라인색 9건 `IND.*` 참조 + 로딩 오버레이 `bg-gray-900/80`+`text-blue-400`+`bg-red-900/50` → `bg-surface/70`+`text-primary`+`bg-error-container`
- `components/Chart/StockChart.tsx`: 동일(라인색 9건 + 오버레이)
- `components/DetailedChart.tsx`: 평단가 점선 `#2962FF`→`IND.avgPrice`, 라벨 오버레이 `bg-black/50 border-gray-700 text-white` → `bg-surface/80 border-outline-variant text-on-surface`
- `components/hero-chart.tsx`: 심볼 `text-white`→`text-on-surface`(흰 배경서 안보이던 핵심 버그), 코인 패널 `bg-black/40 border-white/10`→`bg-surface/80 border-outline-variant`, 선택 글로우 오렌지→primary, LIVE `text-green-400`→`-600`

**주요 논의**:
- **라인색 방향 결정**: 지표 라인은 캔들과 달리 "방향(상승/하락)"이 아니라 "지표 구분" 식별색 — MA7·25·99 세 선을 빨/파 통일하면 구분 불가. R6 T04도 식별색으로 의도 보존. 사용자 3선택지(①식별색 보존+SOT+가독성 ②라이트 팔레트 전면 재선정 ③방향 빨/파 통일) 중 **①** 확정 → 의미 유지, 중복 하드코딩만 SSOT 집약, MA99만 가독성 교체
- **4번 점검 결론**: globals.css `@theme`에 shadcn 별칭(`--color-muted: surface-container`, `--color-border: outline-variant` 등) 전부 라이트 매핑 → `bg-muted`/`border-border` 172건 시각 정상. 전면 치환은 diff 노이즈·시각 회귀만 유발하므로 불요(R6 T05 "별칭 보존" 정책 유지). 실제 깨지던 다크 하드코딩 중 **차트 영역만** 3번에 흡수, 페이지 컴포넌트 잔재(about·AuthButton·Ticker·dashboard-grid 등)는 R1~R3 라이트화 누락분이라 R8 후보로 분리
- **세션 32와 분리**: 작업 중 세션 32(R7 1/2)가 `eb1e9c7`로 커밋되어 워킹트리에 내 5파일만 잔존, 깔끔한 분리 커밋

**검증**: `tsc --noEmit` 0 · `build` green(54/54, coin ● 6종·board/news ƒ) · grep(잔여 하드코딩 라인색 0·`IND.` 9/9/1·차트 다크 오버레이 잔여 0)

**R7 마감**: 후보 4/4 전부 완료(1·2=세션32 `eb1e9c7`, 3·4=세션33). R8 후보 = 페이지 컴포넌트 다크 잔재 라이트화.

---

### 2026-05-25 (세션 32) — R7 단독: 운영 DB 마이그 정합(R7-1) + AD1 CI 통합 generateLink fallback(R7-2)

**대화 토픽 흐름**: 세션 31 cs 인계(R7 후보 4건) → "1"=R7-1 선택 → kdydb 스킬 → 현황 정독(런북 §9-4·T02 handover·backfill SQL·config.toml) → 운영 DB schema_migrations 읽기조회(6행 8자리, 세션30 진단 일치) → **정합 순서 함정** 식별(로컬 14자리↔원격 8자리 mismatch → db push 선행 시 14개 재실행 위험) → 범위 질문 "Part A+B" → backfill Part A+B를 Management API `database/query` `BEGIN;COMMIT` 트랜잭션 1회 적용(첫 시도 한글주석 JSON 깨짐→주석 제외 후 성공) → 14자리 14종 정합 검증(로컬 14파일 1:1) → CLI 검증 시도 실패→**`.env.local` UTF-8 BOM** 확정(supabase CLI env 파서 거부) → 동등검증 결론 → 런북 §10·backfill 주석·next-dev R7-1 마킹 → "다음 r7" R7-2 → T01 handover §6 정독 → 임시 스크립트로 generateLink→verifyOtp 운영 DB **실측**(magiclink/email 둘 다 OK·쿠키 2개) → auth.setup.ts 경로 B 분기 → AD1 fresh skip 발견 → **collection 타이밍 함정** 진단 → spec 런타임 평가 전환 → fresh 재현 **2 passed** → graceful skip 회귀·잔여 0·gitignore 확인 → /cs

**변경 사항**:
- (R7-1) `supabase/backfill_schema_migrations.sql` 주석(적용 완료) / `docs/db/R4-db-apply-runbook.md` §10 신설 — 운영 DB schema_migrations 6행(8자리)→14행(14자리) 정합(Management API 트랜잭션, 객체/데이터 불변·히스토리만)
- (R7-2) `e2e/auth.setup.ts` generateLink(magiclink) fallback 경로 B(비번 없으면 service_role) / `e2e/community-admin-auth.spec.ts` RUN_AD1 런타임 평가 전환(collection 함정)
- `docs/handover/next-dev-prompt.md` R7-1·R7-2 완료 마킹

**주요 논의**:
- **마이그 정합 순서 함정**: 로컬 14자리↔원격 8자리 mismatch라 db push 선행 시 CLI가 14개를 미적용 오인·재실행(비멱등 충돌) → backfill로 schema_migrations 먼저 정합 후 db push 0건이 안전
- **CLI 미사용·동등검증**: `.env.local` BOM으로 supabase CLI 거부 + db push --dry-run은 db password 필요(세션29부터 Management API 전용) → schema_migrations 직접 조회(14종=로컬 14파일)가 db push --dry-run 0건과 동등
- **collection 타이밍 함정**: playwright test collection은 모든 프로젝트 실행 전 1회 → RUN_AD1 top-level 평가 시 admin.ready 부재로 CI fresh 첫 회 항상 skip. 런타임(beforeAll/test) 평가로 수정해야 CI 자동화 성립
- **fallback 실측 우선**: 추측 대신 운영 DB로 generateLink→verifyOtp 직접 실행해 type='magiclink' 확정·쿠키 포맷 확인 후 본 코드 통합. solution 2종 기록

---

### 2026-05-25 (세션 31) — R6 지휘부: polish 5터미널 회수·통합·AD1 활성화·TipTap 핫픽스

**대화 토픽 흐름**: 세션 시작(세션 30 cs 인계, R6 후보 5건) → kdydispatch 평면 5터미널 분산(충돌 분석 — T05 89파일 전역 → Wave2 단독, T02 코드만) → 5 자기완결 프롬프트+마커 작성·발사 → "지금까지 실제 배포됨" 고지 → 배포 모델 분석(프로덕션=GitHub Release 게이트·마이그 자동적용 없음·라이브 쓰기는 T01뿐) → "일꾼 대부분 선행 완료" 회수(T02·T03·T04 handover PASS, 통합 tsc 0·build green, T04 우려 page.tsx 에러 해소 확인) → Wave1 통합 커밋 `94ff3a5` → T01 AD1 활성화(service_role 비번 주입→로그인 성공→AD1 실행) → `/admin/board` 클라 크래시 디버깅(probe 콘솔 캡처) → TipTap `immediatelyRender` 누락 적발 → 핫픽스 `a1983a3`→AD1 `2 passed` → 비번 무력화(인증 원복) → T05 회수·통합 `f8b3373` → push → /cs

**변경 사항**:
- (지휘부 직접) `BlogEditor.tsx` 핫픽스 `immediatelyRender:false` / `docs/orchestration/2026-05-25-R6-polish/` 5프롬프트+_INDEX+체크포인트 / `R6-_SUMMARY.md` / CEO 마커 R6 갱신 + 워커 마커 archive
- (일꾼 회수) T01 e2e 인증 인프라(auth.setup `@supabase/ssr`·config projects 3분리·AD1 spec) / T02 마이그 14파일명 14자리 정규화+config.toml+backfill SQL / T03 queries.ts anon 단일화 / T04 차트 히스토그램 빨↑/파↓ / T05 토큰 on-surface-variant 통일 137건/29파일
- 커밋 `94ff3a5`·`a1983a3`·`f8b3373` origin push (`a729b73..f8b3373`)

**주요 논의**:
- **T05 89파일 전역 충돌** → Wave2 단독 분리 결정(T03 page.tsx·T04 차트와 머지 충돌 회피). 두 토큰은 globals.css 별칭으로 동일색 → 시각 회귀 0 기계적 통일
- **배포 모델**: 프로덕션=`release: published` 게이트(main push≠배포), 마이그 자동적용 없음 → T02 코드 정규화는 프로덕션 DB 영향 0
- **AD1이 라이브 버그 적발**: S-AD1(비로그인 리다이렉트)은 `/admin/board`를 렌더 안 해 미발견 → AD1이 처음 관리자 상태로 렌더하며 TipTap 크래시 적발. 인증 뒤 페이지 E2E 공백 교훈(solution 기록)
- **라이브 관리자 비번**: OAuth 전용 계정이라 service_role admin API로 임시 비번 주입→AD1 검증→무력화(새 랜덤 폐기, 비번 로그인 불가/OAuth 유지)

---

### 2026-05-25 (세션 30) — R5 지휘부: 마이그레이션 정합·dead code·비추집계·E2E 풀검증

**대화 토픽 흐름**: 세션 시작 현황파악(세션 29 cs 인계) → 보류분 3커밋 origin push → R5 후보 #4→#3→#2→#1 순차 진행 → #4 마이그레이션 멱등화+정합진단 → #3 coin-queries dead code → #2 detail 비추집계 노출 → #1 E2E 풀검증(kdye2e) → push + /cs

**변경 사항**:
- #4 `20260523_create_community_tables.sql`·`20260524_comment_likes.sql` CREATE POLICY 18개에 DROP IF EXISTS 선행(멱등화) + 런북 §9 정합경로 + solution 부기 [e75a77c]
- #3 `coin-queries.ts` 미사용 `CoinPostsResult` 제거(R4 우려 fetch 래퍼는 R3/T04에서 이미 제거 확인) [e12b6ee]
- #2 `board-queries.ts`(BoardPostDetail.dislikes + toBoardPostDetail counts)·`board-server.ts`(community_post_like_counts RPC 병렬)·`board/[slug]/[postId]/page.tsx`(initialDislikes)·`PostVoteButtons.tsx`(주석) [a1f437c]
- #1 `e2e/community-board.spec.ts`(L-B3 POST 201 확정·L-B4 자체 댓글 생성)·`docs/e2e/R4-scenarios.md`(R5 갱신) [77d3547]

**주요 논의**:
- **#4 단순 backfill 불가**: 운영 DB schema_migrations 실측 → version=파일명 8자리 날짜라 같은 날짜 다수 파일(20260523×3·20260524×2) version 충돌 → backfill엔 파일명 14자리 정규화 선행 필요. 멱등화로 차후 db push 재실행 안전을 확보(backfill 없이도 CREATE POLICY 충돌 해소). 운영 DB는 읽기 조회만.
- **#3 추측 검증**: R4/T03 R5 메모는 coin-queries를 직접 안 읽고 news-queries 패턴으로 추측("fetch 래퍼 잔존 가능성"). 실제는 R3/T04 SSR 전환 때 이미 제거 → 실 dead는 CoinPostsResult 1개뿐.
- **#2 SSR 경로 한정**: 클라 fetchBoardPost는 사용처 0(R3 SSR 전환 후 미사용) → detail API route 미변경, board-server(SSR)만 community_post_like_counts RPC로 분리집계(추천/비추) 정확값 채움.
- **#1 L-B4 원인 확정**: error-context로 시드 댓글 0(공지글 "댓글 0개") 확정 → 추천 대상 부재 timeout(앱버그 아님). 자체 댓글 생성으로 독립화 + waitForResponse로 201/PATCH 확정. E2E 댓글은 Management API 정리(잔여 0, comment_likes CASCADE).

**검증**: tsc 0 / build green(board ƒ·coin ● 6종·news ƒ) / E2E **29 passed·0 failed·1 skipped(AD1)**(R4 24p/6s → DB 적용+spec 개선). 운영 DB 읽기조회+E2E 자기정리만(위험 직접 쓰기 없음). R5 4커밋 origin push(`f3ce509..77d3547`), 보류분 3커밋도 push(`d789d07..f3ce509`).

**후속(R6 후보)**: AD1 관리자 storageState(자격증명 부재)·마이그레이션 파일명 14자리 정규화(정식 db push 환경)·queries.ts SSOT 환원·차트 방향색 KR 정렬·토큰계 통일.

---

### 2026-05-25 (세션 29) — R4 지휘자: community-wiring + 실 DB 적용 + E2E/cs

**대화 토픽 흐름**: 세션 시작 현황파악 → R3 미push 커밋 3종 origin push → "지휘부 터미널" R4 라운드 설계(kdydispatch CEO) → 4 터미널 발사 → Wave1(T01·T02·T03) 회수 PASS → "실 DB 직접 가능?" → access token Management API로 **커뮤니티 마이그 5종 전체 적용**(전부 미적용 발견) → 게시글 156행 시드 → 통합 커밋 `7683c41`(T04 산출물 동반) → T04 회수 + E2E DB-ready 재실행 → **L-B4 실패 디버깅**(앱버그 아님 확정) → 마커 아카이브 → 부기 커밋 `b196df5` → /cs

**변경 사항**:
- (지휘자) R3 미push 3종(`30cdbd5`·`c34f264`·`d789d07`) origin/main push
- (R4 일꾼) T01 `docs/db/R4-db-apply-runbook.md`·`scripts/smoke/community-like-smoke.ts` / T02 `board-queries.ts`(togglePostLike+dislikeCount·toggleCommentLike 신규)·`PostVoteButtons.tsx`(가짜 비추 제거)·`CommentSection.tsx`(ThumbsUp onClick) / T03 `news-queries.ts` 248→119줄(14심볼 제거·7보존) / T04 `e2e/` spec 5+config·`docs/e2e/R4-scenarios.md`·`@playwright/test` devDep
- (지휘자 직접) 운영 DB(`enksnhshciyvllwfiwrm`)에 마이그 5종 Management API 순차 적용 + 게시글 156행 시드 + `package.json` test:e2e
- 통합 커밋 `7683c41`(30 files +2084/−166) + 부기 `b196df5`(4 files). R4 마커 4개 아카이브.

**주요 논의**:
- **실 DB 적용 가능 여부**: `.env.local`에 `SUPABASE_ACCESS_TOKENS`(44자) 존재·`NEXT_PUBLIC_SUPABASE_URL` 부재·config.toml 없음. DB password 없어 정식 `db push` 불가 → **Management API `database/query`**(access token만, password 불요)로 적용 가능 확정. ref=enksnhshciyvllwfiwrm("Crypto Chart Analysi").
- **전체 미적용 발견**: comment_likes 적용 시 `community_comments does not exist` → 점검 결과 커뮤니티 마이그 **5종 전부 미적용**(news/blog만 존재). 선행 포함 5종 순차 적용으로 범위 확대(사용자 승인).
- **L-B4(댓글추천) E2E 실패 디버깅**: 스모크 트리거 SKIP→DB 댓글 0 확인→L-B3 통과인데 댓글 0 모순→dev 로그에 comment POST 부재→직접 curl로 댓글작성 201·댓글추천 PATCH like 0→1→0 정상 확인 → **앱버그 아님, E2E spec 신뢰성 이슈**(L-B3 브라우저 저장 실패). solution 기록.

**검증**: tsc 0 / build green(board ƒ·coin ● 6종·news ƒ) / 마이그 적용 전부 201 / 스모크 PASS 2·SKIP 1·FAIL 0(토글 RPC like 0→1→0) / E2E 28·1(L-B4)·1(AD1) / 직접 API: 댓글작성 201·댓글추천 PATCH like 0→1→0. 격리위반 0. 완료 4/4.

**후속(R5)**: T04 E2E spec 신뢰성(L-B3/L-B4 자체 댓글 생성)·관리자 storageState·detail API 비추집계 노출·coin-queries dead code·마이그레이션 히스토리 정합(Management API 적용분 schema_migrations 미기록). **커밋 로컬 main 유지(push 보류 — 종료 선택)**.

---

### 2026-05-25 (세션 28) — R3 지휘자: community-finish 12/12 회수·통합·커밋/cs

**대화 토픽 흐름**: Phase 4 회수 재개(직전 1차 7/12) → 2차 회수(T03 news·T09 stock handover 도착) → **T04 stale 오판→사용자 "다시확인" 지적→정정**(11/12) → Wave3 T05 발사·회수(mock 3종 삭제, 12/12) → 최종 통합 빌드 검증 → 통합 커밋 `30cdbd5` → R3 마커 아카이브 → Phase 5 "세션 종료" → /cs(세션 28)

**변경 사항** (지휘자 직접):
- 지휘자 SOT 갱신: `_API_REFERENCE.md`(POST /api/community/like dislikeCount 추가·likeCount 의미변경[순합산→추천수]·RPC/회원전이 명시 / PATCH /api/community/comment 신규 섹션), `_SCHEMA_REFERENCE.md`(community_comment_likes 테이블·분리집계 RPC 2종·트리거·RLS 추가)
- `board-meta.ts`/`news-meta.ts` stale 주석 정정(mock 삭제 반영, T05 §6 보고 처리)
- 통합 보고서 `docs/handover/2026-05-24-R3-_SUMMARY.md` 신규
- **통합 커밋 `30cdbd5`**(79 files, +5953/−2102) + 부기 `c34f264` — git이 mock-posts.ts→scripts/fixtures/community-posts.ts 리네임(63%) 추적
- R3 마커 12개 `.dispatch/teams/` → `.dispatch/archive/` 이동(teams 클린)

**R3 산출(12 터미널, 일꾼 작성·지휘자 통합)**: 트랙 A(T01 메타 SSOT→T02 board SSR·T03 news SSR·T04 coin SSR[SSG+ISR 6종]→T05 mock 3종 삭제) · 트랙 B(T06 공지 라우트·T07 dislike 분리RPC+회원전이 dedup·T08 댓글추천 comment_likes 신규) · 트랙 C(T09 stock·T10 admin·T11 계정/유틸+SecureMemo·T12 정적/인증 라이트화). 신규 14·삭제 3·수정 다수.

**주요 논의**:
- **T04 stale 오판**: 첫 점검 시 coin page "use client"·git 변경 0으로 미진행 판정 → 사용자 "다시확인" → 실제 완료 직전이었고 재점검서 handover+SSR 전환 확인. 교훈: 동시 진행 회수 시 단일 스냅샷 stale 가능, 미완 단정 전 재확인 (solution `2026-05-25-dispatch-recovery-stale-snapshot.md`).
- **격리위반 2건**(둘 다 T06): app/admin/page.tsx 공지섹션 additive + _API_REFERENCE.md SOT 수정 — additive·무충돌로 허용(R2 BoardSidebar 선례).
- **SOT 갱신 주체**: 일꾼은 공통 읽기전용 SOT 미수정(규칙), 지휘자가 통합 시 T06·T07·T08분 일괄 반영.

**검증**: `npx tsc --noEmit` 0 error / `npm run build` ✓ Compiled(54/54, board ƒ·coin ● 6종 프리렌더·news ƒ). 완료율 12/12·자가검증 PASS 12/12·격리위반 2건(허용).

**후속(R4)**: 실 DB push(`20260524_post_likes_rpc`·`comment_likes`) + UI wiring 결선(비추↔T07·댓글추천↔T08) + E2E + dead code 정리. **커밋은 로컬 main 유지(push 보류 — 사용자 미선택)**.

---

### 2026-05-23 (세션 27) — R1·R2 지휘자: 디스패치 라운드 마감 (회수·통합·아카이브)

**대화 토픽 흐름**: "작업 이어서 시작"(지휘자 컨텍스트 탐지) → R1 회수 검증(12/15, 미완 T09·T11·T15 발견) → 마커 리셋(processId 0 + T09 allowed_dirs `components/blog/`→`Blog/` 대문자 교정) + 부분 재발사 → "회수 확인" → R1 15/15 검증 → R1 통합 커밋 `60d4298` → R2 설계(5 task 평면 DAG, 동시 발사) → SOT 5종·마커 5개 작성 → R2 발사 → "회수 확인" → R2 5/5 검증(`/api/blog` 실재·차트 4종·middleware·ISR) → R2 통합 커밋 `81b9624` → mock 정리 R3 이월 판단 → 메모리 2건 → Phase 5 "세션 종료" → R1·R2 마커 아카이브 → /cs(세션 27)

**변경 사항 (지휘자 직접)**:
- R1: `docs/handover/2026-05-23-R1-_SUMMARY.md` 신규, `docs/orchestration/2026-05-23-R1-mainpage/_DISPATCH_CHECKPOINT.md` 15/15 갱신
- R1 통합 커밋 `60d4298` (50파일 +5105/−205): T08·T10·T12·T14 일꾼 산출물 + `.claude/hooks/`·settings.json·`.gitignore` + R1 orchestration/handover/solutions
- R2: SOT 5종(`docs/orchestration/2026-05-23-R2-realdata-finish/R2-T01~05*.md`) + `_INDEX.md` + 체크포인트 + `_SUMMARY.md` 신규
- R2 통합 커밋 `81b9624` (23파일 +3046/−242): T01·T02·T03 board/news/coin 페이지 + `*-queries.ts` 3종 + `BoardSidebar.tsx` + R2 문서
- 마커: R1 15팀 → `.dispatch/archive/R1-markers/`, R2 5팀 → `.dispatch/archive/R2-markers/`. active teams 비움
- 메모리 2건: `kdydispatch-write-guard-soft`, `kdydispatch-rounds-2026-05-23`

**주요 논의/발견**:
- **R1 회수 정정**: 11:05 체크포인트는 9/15였으나 세션 16·17 완료분 반영 시 실제 12/15. 미완 3건은 산출물 직접 grep으로 부재 확정.
- **마커 recency 바인딩**: `Find-MarkerByPid`는 미바인딩(processId 0) 중 "가장 최근 mtime"을 잡음 → R1 재발사는 mtime 분리 + 순차 발사로 결정적 바인딩 유도.
- **write-guard 소프트 확인**: `$env:DK_DISPATCH_ROLE`이 SessionStart hook 프로세스 한정·PreToolUse 프로세스에 비전파 → 격리 미강제. 격리 위반 0은 일꾼 규율 결과. **R2는 동시 발사 허용**(프롬프트 파일이 작업 결정).
- **T09 allowed_dirs 대소문자 버그**: 마커가 `components/blog/`(소문자)였으나 실제 `components/Blog/` → write-guard StartsWith 대소문자 구분이라 차단 위험 → 재발사 전 교정.
- **일꾼 자체 cs**: T09·T11·T15·T04·T05가 각자 /cs로 선별 커밋(코드 일부) → 지휘자 통합 커밋은 미커밋 잔여분만 대상.
- **T15 `/` 동적 렌더**: R1에선 `await createClient()` cookies로 ƒ → R2-T05가 anon 클라이언트로 ○ 정적 ISR 전환.
- **R2 격리 이탈 1건**: T01이 allowed_dirs 밖 `components/community/BoardSidebar.tsx` 생성 → 신규·무충돌·재사용 합리적이라 허용·통합 커밋 포함.

**검증 결과**:
- R1·R2 각 회수 시 `npx tsc --noEmit` 0 에러, `npm run build` Compiled successfully
- R2 후 `/` ○ 정적 ISR(ƒ→○ 전환), node:crypto 경고 0, 격리 위반 0(이탈 1건 허용)
- 잔여 mock import: 정적 메타/타입뿐(`BOARD_META`/`BoardSlug`/`NEWS_CATEGORIES`/`COIN_FILTERS`), 데이터부·mock-coins unused → R3 정리 이월

**R3 후보**: mock-* 완전 정리(mock-coins 삭제 + 정적 메타 별도 파일 이전) · 관리자 게시판 라우트(is_notice) · 댓글 추천 토글 + dislikeCount 분리 RPC · 추천 dedup 회원 전이 정책 · board/news/coin SSR 전환(SEO)

---

### 2026-05-23 (세션 26) — R2/T02 일꾼: 뉴스 실데이터 전환 (`/news` 4차원 필터)

**대화 토픽 흐름**: R2-T02 발사(배너=R2-T05 불일치) → hook 메커니즘 정독(write-guard 환경변수 휘발성→격리 무력) + 마커 전수(R2-T02 PID 20384·내 62144 미바인딩) → AskUserQuestion "마커 재바인딩 후 진행" → 마커 processId 62144 재바인딩 → SOT 정독(T06 `/api/news` 계약·T15 `queries.ts` server-only·컴포넌트 props·사이드바 4 API) → `news-queries.ts` 작성(라벨사전+매퍼+fetch 5) → page 실데이터 전환(JSX 보존) → 검증(tsc/grep/build) → handover → /cs(3중 병렬 cs T01 24·T03 25 → 26 정정)

**변경 사항** (신규 1 + 수정 1):
- `lib/community/news-queries.ts` — **신규**. 클라 안전 모듈(server-only `queries.ts`와 분리). `NEWS_CATEGORY_LABEL`(영문 enum→한글 8값, `altcoin_news`→"알트코인" 추가) + `categoryLabel()` + `formatRelativeTime()` + `mapApiNews()`(API→`NewsListItem`) + fetch 래퍼 5(`fetchNews`·`fetchTickerItems`·`fetchHotIssueItems`·`fetchFngData`·`fetchOfficialPosts`, 각 graceful) + 클라용 `COIN_DISPLAY` 사전.
- `app/news/page.tsx` — `MOCK_NEWS`/`getHeadlines`/mock-coins → `/api/*` 클라 fetch. 코인=`?query=`·분류=`?category=` 서버 위임(useEffect deps), 감정·정렬=클라(useMemo). 헤드라인 마운트 1회 별도 fetch(importance top3). 사이드바 `Promise.all`. 로딩/에러(reloadKey 재시도)/빈 처리. FngGaugeWidget 72/68 하드코딩 제거. `NEWS_CATEGORIES`/`COIN_FILTERS`(라벨 사전)만 mock-news import 유지. JSX·디자인 보존.

**주요 논의**:
- **디스패치 배너/마커 충돌**: 배너=R2-T05이나 지시=R2-T02. write-guard는 `$env:DK_DISPATCH_ROLE` 의존이나 휘발성(매 hook 새 PS 프로세스)이라 전파 안 됨 → 빈 값 → 무조건 pass(물리적 차단 불가). R2-T02 마커 다른 PID(20384) 바인딩 → 중복 위험. 사용자 "마커 재바인딩 후 진행" → processId 62144 재바인딩. (solution `dispatch-marker-rebind-volatile-env.md`)
- **필터→파라미터**: 코인·분류=서버 위임(`/api/news` query/category 지원), 감정·정렬=클라(API 미지원). `NEWS_CATEGORIES` key가 API 영문 enum과 1:1 일치 → 변환 불필요.
- **클라 모듈 분리**: T15 `queries.ts`는 `@/lib/supabase/server` import라 server-only → 클라 `/news`에서 import 불가. 별도 `news-queries.ts` + `COIN_META` 재정의(`COIN_DISPLAY`).
- **category 한글 라벨**: T15 §9.4 후속 권고 이행. NewsRow가 category 그대로 표시 → 영문 enum→한글 매핑 필수.
- **데이터 부재 처리**: 뉴스↔토론 연결 없어 discussionHref/commentCount 생략(NewsRow "—"), popular 정렬→importance 대체, "코인별 뉴스" 위젯 정적 데모 유지.

**검증 결과**:
- `npx tsc --noEmit` 0 에러
- `grep "@/lib/community/mock-" app/news/` 1건(NEWS_CATEGORIES/COIN_FILTERS 라벨 사전 — 지시서 "라벨 사전만" 충족)
- `grep "/api/news" app/news/` ≥1
- `npm run build` ✓ Compiled, `/news` ○ Static
- cs: 3중 병렬(T01·T03 동시 cs, 세션 24·25 점유) → 26 정정. 본 일꾼 코드 2 + handover + cs 문서만 staging, R2-T01/T03·orchestration 컨덕터 위임. push non-ff면 rebase.

### 2026-05-23 (세션 25) — R2/T03 일꾼: 코인룸 실데이터 전환 (`/coin/[symbol]` 6종)

**대화 토픽 흐름**: R2-T03 발사(배너=R2-T04 불일치) → 마커 전수 확인(T03 PID 75996 / T04 PID 70964 본 터미널) → AskUserQuestion "T03 강행" 확정 → SOT 정독(T03 ticker·T12 board·T06 news·T13 hot-issues·T15 COIN_META + 실 라우트 응답 형상) → news 필터 `?query=` 확정·`/api/kimchi`·`/api/blog` 발견 → coin-queries.ts 작성(정적 사전 + fetch 래퍼 7 + 매퍼) → page 전체 재작성 → ESLint set-state-in-effect 차단 → 단일 data 상태객체로 해소 → 검증(tsc/eslint/grep/build) → handover → /cs(세션 25, T01 24 선점→정정)

**변경 사항**:
- **신규** `lib/community/coin-queries.ts` (~450줄) — 코인룸 클라 데이터 + 정적 메타 사전
  - 정적 사전: `COIN_ROOMS`(6종 — 이름·로고·설명·태그·sparkline + 시총/도미넌스/7d/30d 정적 표시값 + 시세 폴백값 + `binanceSymbol`·`isAggregate`·`boardSlug`·`coinTagFilter`), `COIN_META`(12종 — 사이드바 ticker/hot-issues 라벨·링크). mock `COINS` 값 **복사**(import 아님)
  - fetch 래퍼 7: `fetchTickers`(`/api/coins/ticker`)·`fetchCoinPosts`(`/api/board/coin-{slug}`)·`fetchCoinNews`(`/api/news?query=`)·`fetchHotIssues`·`fetchFng`·`fetchOfficialPosts`(`/api/blog`) + `findTicker`/`toTickerItems` 헬퍼
  - 매퍼: `buildCoinView`(정적+ticker 병합)·`mapBoardRow`(snake_case row→`CoinBoardPost`, `uuid` 분리·IP 2옥텟 축약)·`mapNewsItem`(English category→한글 라벨)·`formatRelativeTime`
- **수정** `app/coin/[symbol]/page.tsx` — 전체 재작성. mock import 제거, `getCoinRoomMeta`로 6종 검증(미존재 `notFound()`), `useEffect`에서 6 API 병렬 fetch, 단일 `data` 상태객체. 공지 탭=실 notices, 인기글=추천순 클라 정렬. 사이드바 위젯은 데이터 있을 때만 렌더

**주요 논의**:
- **터미널 배정 불일치**: 디스패치 배너/마커는 본 터미널=R2-T04(chart-lightify)인데 사용자 지시는 R2-T03. write-guard는 `$env:DK_DISPATCH_ROLE` 미설정(fresh 프로세스 휘발)이라 미차단. AskUserQuestion "T03 강행" 선택 → 진행. **T04 미수행 + T03 터미널(PID 75996) 유휴 가능성** 지휘자 회수 판단 필요(handover 상단 명시)
- **news symbol 필터**: 지시서 `?coin=`은 실제 미지원 → `?query={symbol}`(symbol.eq + title/snippet ilike)이 정답. kimp(coinTagFilter "ALL")는 query 미지정 → 최신 전체
- **클라 컴포넌트 + 서버모듈 격리**: 클라 컴포넌트가 `lib/community/queries.ts`(서버전용 `cookies()`) import 시 번들 오염/빌드 실패 → `COIN_META` 재정의. 정적 메타 복사로 `grep mock- app/coin/` 0건
- **set-state-in-effect**: 단일 `data`(symbol 동봉) 상태객체 + `.then` 1회 setState + `ready=data?.symbol===meta.slug?data:null` 파생. 동일 React19 패턴을 병렬 T01(세션 24)이 solution 문서화 → 중복 회피로 본 세션 solution 생략
- **altcoin/kimp**: `isAggregate`=true·`binanceSymbol`=null → ticker 미조회·정적 폴백 설명형(T15 패턴). `/api/kimchi` 실데이터 존재하나 "kimp 설명형 유지" 준수로 미연동(후속). AI 시그널 위젯도 데이터소스 외라 정적 placeholder 유지

**검증**: tsc 0 / eslint(page+coin-queries) 0 / `grep "@/lib/community/mock-" app/coin/` 0건 / `grep "/api/" coin-queries.ts` 6종 / build Compiled(`ƒ /coin/[symbol]`)

---

### 2026-05-23 (세션 24) — R2/T01 일꾼: 게시판 실데이터 전환 (목록/상세/작성 + 사이드바)

**대화 토픽 흐름**: R2-T01 발사 → SOT 정독(T12 handover·라우트 4파일 응답 형상·BoardRow 계약·위젯 4종·사이드바 API 4종·T15 매퍼 패턴) → board-queries.ts 작성(fetch 래퍼 6 + 사이드바 4 + 매퍼 3) → BoardSidebar.tsx → 목록/상세/작성 페이지 전환 → 검증(tsc/grep) → ESLint set-state-in-effect 차단 발견 → 구조 수정(내부 async + 핸들러 리셋) → build PASS → handover → /cs(세션 24)

**변경 사항**:
- **신규** `lib/community/board-queries.ts` — 게시판 클라 fetch 래퍼 + row(snake_case)→props 매퍼 중앙화
  - fetch 6: `fetchBoardList`(GET 목록, 서버 정렬/검색/페이지/카테고리 위임) · `fetchBoardPost`(GET 상세+댓글) · `createBoardPost`(POST) · `createComment`(POST 댓글) · `togglePostLike`(POST 추천/비추) · `deleteBoardPost`(DELETE)
  - 사이드바 4: `fetchSidebarTickers`(`/api/coins/ticker`) · `fetchSidebarHotIssues`(`/api/coins/hot-issues`) · `fetchSidebarFng`(`/api/fng`) · `fetchSidebarOfficialPosts`(`/api/blog?limit=3`)
  - 매퍼 3 + 표시 헬퍼: `toBoardListItem`/`toBoardPostDetail`/`toBoardComment` + `maskedIpToShort`("211.34.*.*"→"211.34")·`relativeTime`(ISO→상대시간)
  - 정렬 키 매핑 `SORT_MAP`(latest→recent 등), COIN_META(브랜드 사전, 클라 안전)·HOT_TREND_MAP(UP→up 등)
- **신규** `components/community/BoardSidebar.tsx` — 목록/상세 공용 사이드바(`"use client"`). 4위젯 독립 fetch + `.catch`로 실패 위젯만 숨김, `showTools` prop(목록 true/상세 false)
- **수정** `app/board/[slug]/page.tsx` — MOCK_POSTS/클라 필터 → `fetchBoardList` 서버 위임. 로딩·에러·빈 상태. 검색 300ms 디바운스. 사이드바→`<BoardSidebar />`
- **수정** `app/board/[slug]/[postId]/page.tsx` — getPost/MOCK_COMMENTS → `fetchBoardPost`. 추천/비추→`togglePostLike`, 댓글→`createComment`, 삭제→`deleteBoardPost`(익명은 비번 prompt). 이전/다음·다른 글은 최신 30 목록에서 도출
- **수정** `app/board/[slug]/write/page.tsx` — 더미 setTimeout → `createBoardPost` 후 작성 글 상세 라우팅

**주요 논의**:
- **fetch 중앙화**: orchestration 산출물 명시대로 board-queries.ts에 집약 → `grep "/api/board/" app/board/`는 0건(URL은 lib). handover에 "wiring은 board-queries.ts grep" 명시
- **UUID vs number id**: T12 게시글 id=UUID(string), BoardRow `BoardPost.id`=number(읽기전용). `BoardListItem extends BoardPost`+`uuid` → 라우팅·key는 uuid, id는 placeholder, No 컬럼은 `total-offset-index`. BoardRow 무수정
- **React19 set-state-in-effect**: 로드 effect 본문 동기 setState + page 리셋 effect가 eslint 에러. 해법 (a) 내부 `async load()`+`void load()`, (b) 리셋 effect 제거→이벤트 핸들러 `setPage(1)`. solution 문서화
- **회원 표시명**: T12 API 프로필 미join → "회원" 고정(후속 과제)
- **병렬 트리 격리**: tsc의 app/page.tsx 단독 에러는 병렬 T05 중간상태 → `git stash -- app/page.tsx` 후 전역 0 에러 입증, 즉시 복원. 빌드 시점엔 정상화되어 build PASS

**검증**: tsc 내 파일 0 / eslint 내 5파일 0 / grep mock- BOARD_META만(3건) / build Compiled successfully(`/board/[slug]`·`/[postId]`·`/write` 등록)

---

### 2026-05-23 (세션 23) — R2/T05 일꾼: 인프라 마무리 (ISR + node:crypto + Giscus)

**대화 토픽 흐름**: R2-T05 발사(배너는 R2-T02 → 사용자 지시 우선) → SOT 정독(T15 §4 ISR 분석·T07·T09·`_INDEX.md`) → BlogComments 테마 probe로 hook 비강제 확인 → (3)Giscus light → (2)middleware runtime=nodejs → (1)ISR 해법 의사결정(page.tsx 자급 vs outbox) → page.tsx anon 로더 내장 → tsc·build 2회 검증(ƒ→○ 확인) → solution 해결 확정 보강 → handover → /cs(세션 23 정정)

**변경 사항** (수정 3파일):
- `components/Blog/BlogComments.tsx` — Giscus `data-theme` `'dark_dimmed'`→`'light'` (1줄). T09가 R2 권장으로 남긴 항목.
- `middleware.ts` — `export const runtime = 'nodejs'` 추가(주석 포함). `ip-mask.ts`(T07)의 `node:crypto` import로 인한 edge 빌드 경고 해소. matcher·IP 헤더 주입·인증 리다이렉트 로직 불변.
- `app/page.tsx` — `fetchMainPageData()`(쿠키 의존) 호출 제거 → cookies 비의존 anon 클라이언트 로더 `loadMainPageData()` + 행 매퍼 4종 내장. `revalidate=300` 유지. `/` 렌더 모드 **`ƒ`→`○` 정적 ISR 전환**.

**주요 논의**:
- **배너 불일치**: 터미널 kdydispatch 배너는 `R2-T02 (news)`였으나 실제 지시는 R2-T05(infra). PreToolUse hook이 배너 좁은 영역을 강제하지 않아(probe 통과) R2-T05 쓰기영역대로 진행. (T04도 동일 — R2 디스패치 공통 현상)
- **ISR 해법 의사결정**: 무중복 클린 해법은 queries.ts 클라이언트 2줄 교체이나 **R1 read-only**(`_INDEX.md` line 30). 선택지 (A)outbox vs (B)page.tsx 자급. 스펙 line 17·사용자 직접 지시·완료신호("ƒ→○") 모두 page.tsx → **B 채택(CLAUDE.md 사용자 지시 최상위)**.
- **기술 함정**: anon 클라이언트만으론 부족 — supabase-js `global.fetch`에 `next:{revalidate:300}` 주입해야 uncached fetch가 페이지를 dynamic으로 되돌리지 않음. 라우트 revalidate는 의존 fetch 최솟값(ticker 60s) → `1m` 표기(정적화 목표는 달성).
- **dead-code 부수효과**: page.tsx 로더가 queries.ts와 동일 형상 → `fetchMainPageData()` 미사용화. 지휘자 SSOT 환원(queries.ts 클라이언트만 anon 교체 + page.tsx 로더 제거) 후속 권장(handover §6 + solution 보강).
- **관찰(범위 밖)**: Next 16 `middleware`→`proxy` 파일규칙 deprecation 경고(기존부터·내 변경 무관, R3 후보).

**검증 결과**:
- `npx tsc --noEmit` 0 에러 (2회)
- `npm run build` ✓ Compiled — `┌ ○ /  1m  1y`(이전 `┌ ƒ /`) · node:crypto/Edge 경고 부재 · `ƒ Proxy (Middleware)` 정상
- solution `2026-05-23-nextjs-cookies-breaks-isr-revalidate.md`에 "해결 확정(세션 23)" 보강
- cs: 병렬 R2 환경 — 본 일꾼 코드 3 + handover + cs 문서 + solution만 staging, R2-T01/T04 산출물·orchestration 디렉토리는 컨덕터 통합 위임

### 2026-05-23 (세션 22) — R2/T04 일꾼: 차트 라이트화 (TradingView 4종)

**대화 토픽 흐름**: R2-T04 발사(배너는 R2-T03 → 사용자 지시 우선) → SOT 정독(theme.ts·T08 handover·v5 ColorType solution) → 호출처 조사(colors prop 미사용·CryptoChart 주석처리) → 4컴포넌트 테마 적용 → 검증(tsc/grep/eslint/build) → handover → /cs

**변경 사항** (수정 4파일, 색상/테마만, 데이터 로직 무변경):
- `components/Chart/CryptoChart.tsx` — 메인+RSI+MACD 3차트 layout·grid·timeScale + 캔들. 모듈 상수 `CHART_THEME`/`CANDLE_COLORS` 도입, 색상 effect 의존성에서 `colors` 제거(`[]`/`[showRSI]`/`[showMACD]`). `colors` prop 다크 기본값(#1E1E1E/#D9D9D9) 제거(인터페이스 유지). `ColorType` import 제거.
- `components/Chart/StockChart.tsx` — 동일 패턴, market `'kr'`.
- `components/DetailedChart.tsx` — `#111`/`#DDD`/`#333`/`#444` → `...CHART_THEME`, 캔들 → `...CANDLE_COLORS`. 평단가 라인 #2962FF 보존.
- `components/hero-chart.tsx` — transparent/흰 텍스트·그리드 → 라이트 테마, `crosshair: {...CHART_THEME.crosshair, mode}`·`timeScale: {...CHART_THEME.timeScale, ...}`·height(400) 머지 보존.

**주요 논의**:
- 적용 방식: T08 handover line 95 권장("키 spread 대신 테마 객체 직접 전달") → 모듈 레벨 상수 1회 평가 + spread 머지(borderColor 등 테마 하위키 보존). 모듈 상수라 exhaustive-deps 회피.
- StockChart `kr` 선택: 지시서 "프로젝트 규칙 확인 후"였으나 사용자 발사 프롬프트 "한국식 빨↑/파↓" 명시 + 한국 투자자 앱 + 별도 규칙 부재 → 4종 통일. US 종목 필요 시 `'us'` 교체 가능(handover 명시).
- 의도적 보존(범위 밖): 볼륨 막대·MACD 히스토그램 방향 색(녹↑/빨↓ — 빨/파 캔들과 불일치, 후속 KR 정렬 권장), RSI/MACD/MA/BB 라인 색(데이터 식별), CSS 오버레이(hero `text-white` 등 R1 이전부터의 잔재).
- 검증: tsc 0 / `grep getChartTheme|getCandleColors` 4/4 / 다크 리터럴 잔여 0 / eslint 신규 0(기존 `prefer-const`/`lang` deps 경고만) / build Compiled successfully.
- 레퍼런스: 색상 전용이라 schema/type/API 무변경. `_COMPONENT_MAP.md`에 4컴포넌트→`lib/chart/theme.ts` 의존 엣지 신규(병렬 공유 SOT라 직접 편집 회피, 후속 메모).
- cs: 병렬 R2 환경 — 본 일꾼 차트 4 + handover + cs 문서만 staging, R2-T01/T05 산출물은 컨덕터 통합 위임.

### 2026-05-23 (세션 20) — R1/T09 일꾼: 블로그 라이트화

**대화 토픽 흐름**: 사용자가 3번째 터미널 T09 발사 (`docs/orchestration/2026-05-23-R1-mainpage/T09-blog-lightify.md` 정독·실행) → 지시서 경로 정정(`R1-main page` 공백 → 실제 `R1-mainpage`) → current.md + T08 handover 정독(`tone?: 'light'|'dark'` default light 확인) → `app/blog`·`components/Blog` 다크 톤 전수 grep → `components/Blog/editor/`는 T08 영역 분리 인지 → `components/community/Badge.tsx` SOT 패턴 참조 → 18파일 minimal-diff 클래스 교체 → 검증 4종(tsc·잔여 grep·라이트 토큰 카운트·build) → 빌드 `.next\lock` 점유 진단·해제 후 재시도 → handover 작성 → 사용자 /cs → 세션 19(T11) 커밋 발견 후 세션 20으로 정정

**변경 사항** (18파일, 클래스 only):
- 페이지 4: `app/blog/BlogPageClient.tsx`, `category/[category]/CategoryPageClient.tsx`, `tag/[tag]/TagPageClient.tsx`, `[slug]/BlogPostDetail.tsx` — 페이지 배경·뒤로가기 링크·메타·구분선
- 컴포넌트 11: `BlogCategoryFilter`(선택 pill `bg-white`→`bg-primary`), `BlogComments`, `BlogPostCard`(카드 surface+그림자 완화), `BlogPostContent`(prose-invert 제거+prose 토큰), `BlogPostList`(스켈레톤+페이지네이션), `BlogRelatedPosts`, `BlogSearchBar`, `BlogShareButtons`, `BlogSidebar`(카드3+링크), `BlogTableOfContents`, `BlogTagBadge`
- BlogEditor 사용처 3: `app/admin/blog/new`·`edit/[id]`·`board/[slug]/write`에 `tone="light"` 명시
- 신규: `docs/handover/2026-05-23-R1-T09-blog-lightify.md`

**주요 논의 / 의사결정**:

1. **에디터 서브시스템 분리** — `components/Blog/editor/`(BlogEditor·EditorToolbar·ColorPicker·EditorImageUpload)는 T08 영역. 직접 클래스 교체하지 않고 `tone` prop으로만 제어. 사용처에 `tone="light"` 명시(default도 light라 동작 동일하나 의도 표기).

2. **코드블록 의도적 다크 보존** — `BlogPostContent`의 `prose-pre:bg-black/60` + 복사버튼(useEffect 동적 생성, 로직 영역) 보존. `prose-invert` 제거 시 Tailwind Typography 기본값이 코드블록을 다크 배경+밝은 텍스트로 렌더(GitHub 표준 가독성). T08이 에디터 textarea(`bg-black/60 text-green-400`)를 의도적으로 다크 보존한 결정과 일관.

3. **시맨틱/외부 위젯 보존** — `BlogShareButtons`의 `text-green-400`(복사 완료 Check)은 성공 액센트라 보존. `BlogComments`의 Giscus `data-theme: 'dark_dimmed'`는 setAttribute 로직(클래스 아님)이라 미변경, R2에서 `'light'` 전환 권장.

4. **매핑 정렬** — spec 매핑표는 `bg-zinc/slate` 기준이나 실제 코드는 `bg-white/*`·`text-gray-*`·`bg-black` 팔레트 → `components/community/Badge.tsx`(SOT)의 `bg-primary text-on-primary`·`border-outline-variant text-on-surface-variant`에 맞춰 의미적 동등 매핑. 카드 그림자 `rgba(0,0,0,0.3/0.4)`→`0.06/0.10` 완화(라이트 배경 적합).

5. **빌드 락 충돌** — `npm run build` 첫 시도 시 `.next\lock` 점유 에러(병렬 터미널 추정). 실행 중 `next` 프로세스 부재 확인 후 락 해제 상태에서 재시도 PASS. 다른 일꾼 프로세스 미종료 — 락 해제 대기 후 재시도 원칙.

**검증 결과**:
- `npx tsc --noEmit` — 0 에러
- 잔여 다크 톤(spec grep, editor 제외) — **0건**
- 라이트 토큰 사용 — **70건** (기대 10+)
- `npm run build` — Compiled OK, `/blog`·`/blog/[slug]`·`/blog/category/[category]`·`/blog/tag/[tag]` 4 라우트 정상

**커밋 분리**: 병렬 dispatch — 워킹트리에 T08/T10/T12/T14/T15 산출물 혼재. 본 일꾼 책임 T09 18파일 + handover + cs 문서만 명시적 staging, 나머지 컨덕터 통합 커밋 위임 (세션 10/12/14~19 패턴 준용).

---

### 2026-05-23 (세션 19) — R1/T11 일꾼: 시그널·마켓·주식마켓 라이트화

**대화 토픽 흐름**: 사용자가 2번째 터미널 T11 발사 (`docs/orchestration/2026-05-23-R1-mainpage/T11-signal-market-lightify.md` 정독·실행) → 지시서 경로 정정(`R1-main page` 공백 → 실제 `R1-mainpage`) → T09 매핑표 + T10 차트 패턴 핸드오버 + T08 `lib/chart/theme.ts` 정독 → 6개 대상 디렉토리 다크 톤 전수 grep → 파일별 문맥 판단하며 minimal-diff 교체 → 검증 7종 (다크톤 잔여·text-white 보존·라이트 토큰·로직 무손상·차트 옵션·diff stat·tsc·build) → handover 작성 → 사용자 /cs → cs 단계에서 T15가 세션 18 슬롯 점유 발견 → 본 세션 19로 정정

**변경 사항**:
- `app/signal/page.tsx` (5/5) — 스캔 상태·빈 상태·푸터 텍스트 → `text-on-surface(-variant)`
- `app/market/page.tsx` (15/15) — 게이지 라벨·기준 토글·코인 무드 그리드·AI 인사이트 카드·히스토리 섹션
- `components/Signal/WhaleAlert.tsx` (7/7) — 중립 거래 카드 surface + 카드 위 금액/주소/시각 텍스트
- `components/Market/KimchiPremium.tsx` (13/13) — 컨테이너·테이블 헤더·스켈레톤·행 hover·셀 텍스트·각주
- `components/Market/RSIHeatmap.tsx` (4/4) — 컨테이너·제목·스켈레톤·각주 (히트맵 셀 색상 보존)
- `components/Stock/StockSectorPerformance.tsx` (1/1) — RSI 프로그레스 바 트랙 `bg-gray-700` → `bg-surface-container-high`
- `git diff --stat` 합계 **45/45 좌우 대칭** — 순수 클래스 토큰 교체
- 신규: `docs/handover/2026-05-23-R1-T11-signal-market-lightify.md`

**주요 논의**:

1. **검증 grep의 범위 vs 실제 클래스** — T11 spec의 검증 grep은 `bg-zinc/slate/text-white/border-zinc`만 보지만, 이 프로젝트는 실제로 `gray`/`black` 클래스를 주로 사용. T10이 확장한 gray/black 매핑(`bg-gray-900`→`bg-surface-container-lowest` 등)을 그대로 적용하여 포괄 라이트화.

2. **RSI 히트맵 셀 색상 보존 (의사결정)** — `RSIHeatmap.tsx`의 `getRSIColor`가 반환하는 `bg-red-600/orange-500/gray-600/teal-600/green-600`은 RSI 값을 인코딩하는 채도 높은 데이터 시각화. 셀 위에 `text-white`/`text-white/70 bg-black/20`가 올라가므로 surface 토큰으로 바꾸면 가독성 파괴. 중립값 `bg-gray-600`도 다른 셀과 동일 채도 체계로 **보존**. 컨테이너·제목·스켈레톤·각주만 라이트화.

3. **강조 컬러 위 text-white 보존** — 일간/실시간 토글(`bg-indigo-600`/`bg-rose-600`), Hot/Cold 범례 뱃지(`bg-red-600`/`green-600`/`destructive`), 정보 카드(`bg-red-900/20`/`green-900/20`)는 의미 컬러 시스템으로 보존 (T10 보존 원칙 동일).

4. **차트 옵션 영역 외** — `getChartTheme`/`createChart`/`addCandlestickSeries` 호출이 대상 6개 디렉토리에 일절 부재 (grep 0건). T10과 동일하게 차트는 외부 Chart 컴포넌트로 위임되므로 차트 옵션 교체는 본 일꾼 범위 밖. 검증 기대치 "1건 이상"은 영역 외 사유로 0건.

5. **수정 불필요 4파일** — `app/stock-market/page.tsx`(`getColor` 시맨틱 함수만), `InvestmentQuotes.tsx`/`StockAnalysisPanel.tsx`(다크톤 부재), `StockRSIHeatmap.tsx`(이미 shadcn 토큰 `bg-card`/`text-foreground`/`bg-muted` 사용, `text-white`는 범례 뱃지 보존 대상).

**검증 결과**:
- `npx tsc --noEmit` — 에러 0건 (T10이 언급한 bcryptjs 사전 이슈는 후속 세션에서 해소)
- 다크 톤 surface 잔여 grep — 0건
- 잔여 `text-white` 9건 — 전부 강조 bg 위(보존)
- 로직 무손상 `git diff --stat lib/signal_engine.ts lib/analysis.ts lib/probability/ lib/backtest/ lib/chart/theme.ts` — 0 변경
- `npm run build` — PASS (`/signal`·`/market`·`/stock-market` 모두 ○ 정적 프리렌더, 에러 0건)

**커밋 분리**: 병렬 dispatch 시나리오 — 워킹트리에 T09/T10/T12/T14/T15 다른 일꾼 산출물 다수 혼재. 본 일꾼 책임 산출물(T11 6파일 + handover) + cs 5종만 명시적 staging, 나머지는 컨덕터 통합 커밋에 위임 (세션 10/12/14~17 패턴 준용).

---

### 2026-05-23 (세션 17) — R1/T13 일꾼: hot-issues 집계 RPC + API (코드 사전 완료, handover 신규)

**대화 토픽 흐름**: 사용자가 T13(hot-issues-rpc) 발사 → SessionStart hook은 **T11**(`app/signal/`·`app/market/`·`app/stock-market/`·`components/Signal/`·`components/Market/`·`components/Stock/` 라이트화) 마커 + 허용 경로 안내 (T13 산출물 경로와 100% 불일치) → 사용자 명시 지시 우선 채택 (CLAUDE.md 우선순위 1, 세션 9 T08 / 세션 14 T07 패턴) → T01 handover 참조 + T13 작업 지시서 정독 → 사전 상태 점검 시 산출물 일체(SQL 65줄 + 라우트 50줄 + `_API_REFERENCE.md` L645~681 append) 모두 존재, **handover만 미작성** 발견 → 코드 무변경 + handover 신규 작성(157줄, T15 인계 4종 + 안티패턴 6 + 알려진 이슈 5) → 검증 4종 본 일꾼 PASS + npm run build는 다른 일꾼 미커밋 영향으로 SKIP 결정 → handover 파일이 사후 system reminder로 갱신되어 "Compiled successfully in 35.4s" 행 반영 확인 → 사용자 /cs 입력 → cs 단계에서 동일 날짜 다른 터미널(T02 일꾼)이 세션 16 슬롯 점유 발견 → 본 세션 번호 17로 정정 후 cs 진행

**역할**: R1 라운드(mainpage) **단일 일꾼 — 코드 사전 완료 인수 + handover 신규 작성 하이브리드 패턴**. 본 세션 신규 코드 변경 0건, 산출물은 이전 세션(또 다른 T13 시도 또는 동일 일꾼 이전 라운드)이 이미 완성. 본 세션 책임은 handover 신규 작성 + cs 문서 5종 갱신.

**변경 사항 (코드 0건, handover/cs 신규·수정 6개)**:

신규:
- `docs/handover/2026-05-23-R1-T13-hot-issues-rpc.md` — T13 인수인계서 (157줄, T15 매핑 가이드 코드 스니펫 포함)
- `docs/handover/2026-05-23-session17-t13-hot-issues.md` — 세션 메타 인수인계 (본 일꾼 책임 한정 명시)

수정(cs):
- `docs/logs/journal-2026-05-23.md` — 세션 17 항목 append (T13 사후 검증·handover 작성 6 토픽)
- `docs/status/current.md` — 마지막 세션 메타 갱신 + 작업 이력 1행 + 세션 요약표 1행
- `docs/logs/2026-05.md` — 본 항목 append (세션 17, 최상단)
- `docs/handover/next-dev-prompt.md` — "최근 완료된 작업" 세션 17 항목 + T13 산출물 반영

산출물 인수 (이전 세션 작성, 본 세션은 git untracked → staging만):
- `supabase/migrations/20260523_create_hot_issues_rpc.sql` — `community_hot_issues(hours_window int, result_limit int)` RPC, STABLE plpgsql, GRANT anon/authenticated, COMMENT ON FUNCTION 포함
- `app/api/coins/hot-issues/route.ts` — `GET /api/coins/hot-issues?hours=&limit=`, revalidate 300s, 입력 클램프(1~168/1~50), `await createClient()` 패턴
- `docs/references/_API_REFERENCE.md` L645~681 append (T13 섹션, 다른 일꾼 누적 변경 동반)

**주요 논의**:

1. **SessionStart hook T11 vs 사용자 지시 T13 — 사용자 우선 채택** — 허용 경로(`app/signal/;app/market/;app/stock-market/;components/Signal/;components/Market/;components/Stock/;docs/handover/`)가 T11(시각 페이지 라이트화) 영역이고 T13 산출물 경로(`app/api/coins/hot-issues/`, `supabase/migrations/`, `docs/references/`)와 100% 불일치하여 PreToolUse hook 차단 가능성을 인지했으나, CLAUDE.md 우선순위 1(사용자 명시 지시)에 따라 T13 진행. 실제로 hook 차단 미발생 — `app/api/coins/hot-issues/`는 untracked로 이미 존재했고 본 일꾼은 Write를 시도조차 하지 않았다(사전 완료 발견). handover 작성은 `docs/handover/` 허용 경로라 통과.

2. **T13 산출물 사전 완료 + handover만 신규** — 세션 14(T07 사후 검증 전용, 코드 0건)·세션 15(T03 사후 검증 전용, 코드 0건)와 달리, 본 세션은 **코드는 이미 있고 handover만 없는** 하이브리드 상태 발견. 코드는 spec 1:1 그대로 베끼는 것이 아니라 spec보다 **우수한 보정**(`createServerClient` → `await createClient()` Next 16 async 패턴 채택, 입력 클램프 강화, COMMENT ON FUNCTION 추가)을 포함 → 덮어쓰면 손실 발생 → 그대로 두는 것이 맞다. 본 세션 작업 범위는 검증 + handover 신규 작성으로 결정.

3. **supabase 클라이언트 패턴 보정** — spec L97 `import { createServerClient } from "@/lib/supabase/server"` + `const supabase = createServerClient();`는 실제 export(`createClient` async)와 불일치. 기존 라우트 `app/api/news/route.ts:3` 패턴이 `await createClient()` 사용. 사전 작성 코드는 이미 동일 결론에 도달했고, handover §"API 응답 형식"에 보정 경위 명시.

4. **검증 — 본 일꾼 4종 + 사후 build 5종 모두 PASS** — 본 일꾼 직접: grep SQL 키워드 2건 ✓, grep RPC 호출 1건 ✓, tsc 0 에러 ✓, eslint 0 에러 ✓. 본 일꾼 SKIP: `npm run build` — 다른 일꾼(T10/T08/T14/T02/T12) 미커밋 변경 다수로 본 산출물 단독 검증 불가. 사후: handover 파일에 "Compiled successfully in 35.4s ✓ — `ƒ /api/coins/hot-issues` 동적 라우트 등록" 행이 system reminder로 갱신되어 인지. `lib/community/ip-mask.ts:3:1` Node 빌트인 import 경고 1건은 T07 영역으로 본 작업 무관.

5. **T15 인계 핵심 4가지 — 코드에서 유추 불가능한 매핑** — handover의 §"T15에게 줄 메모"에 명시: (1) trend `"UP|DOWN|NEW|FLAT"` → 위젯 `"up|down|new|same"` (특히 `FLAT → same` 매핑 코드 스니펫), (2) `symbol`만 반환·사람친화 keyword(예: "비트코인 ETF")는 별도 사전 또는 후속 RPC 확장, (3) `delta`는 미반환·클라이언트에서 `recent_count - prev_count` 계산, (4) 502/빈 결과는 mock fallback(`HOT_ISSUES`) 또는 빈 상태 디자인으로 graceful degrade.

6. **알려진 이슈 5건 — 후속 결정 미루기** — `coin_symbol` enum 미강제(T01 인계 §3 연결, T15에서 화이트리스트 필터 권장), 사람친화 keyword 미제공(별도 사전 또는 RPC 확장), `delta` 미제공(클라이언트 계산 가능), STABLE 캐싱과 NOW() 시계 경계(실용상 무시 가능), 모니터링 미부착(메인 SSR 핫경로화 시 부착). 세션 종료 시점에서 모두 후속 트랙으로 이관.

7. **/cs 슬롯 충돌 — 17 정정** — cs 단계 진입 시 저널을 읽어보니 동일 날짜 다른 터미널(T02 일꾼)이 세션 16을 이미 점유한 항목(L400~L481, "세션 16 (R1/T02 일꾼 — community 시드 스크립트)") 발견. 본래 세션 16으로 진행 의도였으나 세션 14의 13→14 정정 패턴 준용하여 17로 정정. current.md / journal / 2026-05.md / next-dev-prompt 모두 17 기준.

**검증 결과 (T13 사후 검증, 본 세션 코드 변경 0건)**:
- `grep -c "CREATE OR REPLACE FUNCTION\|GRANT EXECUTE" SQL` → **2** (기대 ≥2) ✓
- `grep -c "rpc(\"community_hot_issues\"" route.ts` → **1** (기대 1) ✓
- `npx tsc --noEmit` → 출력 없음 (전역 0 에러) ✓
- `npx eslint app/api/coins/hot-issues/route.ts` → 0 errors (deprecated `.eslintignore` 경고만) ✓
- `npm run build` (사후 갱신) → Compiled successfully in 35.4s, `ƒ /api/coins/hot-issues` 동적 라우트 등록 확인 ✓

**알려진 이슈**:
- **워킹트리 R1 다른 일꾼·후속 라운드 산출물 잔존**: 컨덕터 통합 커밋 영역. 본 세션 미터치(T08/T10/T12/T14, `.claude/`, `docs/orchestration/`).
- **`_API_REFERENCE.md` 누적 변경 동반 staging**: T13 append만 분할 staging은 위험(세션 8 T03 / 세션 12 T04 / 본 세션 T13 누적 상태) → 전체 staging 채택, 세션 12 패턴 준용.
- **세션 번호 충돌 보정**: 본 세션 16→17 정정. 세션 14 패턴과 동일한 재명명 절차.

---

### 2026-05-23 (세션 16) — R1/T02 일꾼: community 시드 스크립트 (`scripts/seed-community.ts`, 156행)

**대화 토픽 흐름**: 사용자가 T09(블로그 라이트화 마무리) 발사 → SessionStart hook은 **T02**(community-seed) 마커 + 허용 경로 안내 → 발사 프롬프트 본문의 "hook 우선 원칙"에 따라 T02 채택 (R1 5번째 사례) → T02 작업 지시서 + T01 handover + `lib/community/mock-posts.ts`/`mock-coins.ts`/`auth.ts`/`ip-mask.ts`/`scripts/seed_blog.ts`(참고 패턴) 일괄 SOT Read → 환경 의존성 사전 확인(bcryptjs/tsx 모두 설치 상태) → `scripts/seed-community.ts` 신규 작성(약 180줄) → 검증 4/4 PASS → `docs/handover/2026-05-23-R1-T02-community-seed.md` 작성 → 사용자 /cs 입력 → cs 단계 진입 (마지막 세션 15 → 본 세션 16)

**역할**: R1 라운드(mainpage) **단일 일꾼 — T02 신규 코드 작성**. 본 일꾼 책임은 T02 산출물 2개(스크립트 + handover) + cs 문서 4종만. 다른 일꾼(T08/T10/T13/T14) 미커밋 산출물 + 시스템 untracked는 컨덕터 통합 영역으로 위임.

**변경 사항 (T02 + cs, 6개 파일)**:

신규:
- `scripts/seed-community.ts` (~180줄) — MOCK_POSTS 3 보드 × 52행 = 156행 → community_posts 매핑 + 100행 chunk INSERT + `--force` 가드 + bcrypt 1회 공유 해시 + 랜덤 created_at(0~30일)
- `docs/handover/2026-05-23-R1-T02-community-seed.md` — 매핑 표 + 실행 명령 + 검증 결과 + 안티패턴 회피 체크 + T12/T13/T15 메모

수정(cs):
- `docs/logs/journal-2026-05-23.md` — 세션 16 항목 append (T02 7 토픽)
- `docs/status/current.md` — 마지막 세션 메타 갱신 + 작업 이력/세션 요약표 1행 추가 + v2.0 진행 시드 항목 체크
- `docs/logs/2026-05.md` — 본 항목 append (세션 16)
- `docs/handover/next-dev-prompt.md` — "최근 완료된 작업" 세션 16 항목 + 시드 항목 상태 갱신

**주요 논의**:

1. **사용자 명시 지시 T09 vs SessionStart hook 마커 T02 — hook 우선 채택** — 세션 10·11·12·15와 동일 패턴. 발사 프롬프트 본문에 "SessionStart 메시지가 '일꾼 T0X'로 다른 마커 표시하면 그 T0X 작업 지시서로 대신 진행" 명시. R1 dispatch에서 hook 우선 채택 5번째 사례. T09는 다른 터미널이 처리하거나 후속 라운드 영역.

2. **bcryptjs 설치 상태 사전 확인 → PARTIAL 0건** — spec은 bcryptjs 미설치 시 handover "미해결" 명시 의무 부과. `package.json` grep + `node_modules/bcryptjs` 확인 결과 모두 존재. 세션 10(T07)에서 보고됐던 PARTIAL이 어느 시점에 해소된 상태(세션 15에서도 부수 확인). handover "미해결" 섹션에 "없음" 단호 명시 가능.

3. **매핑 결정 — 모든 시드 익명 + 운영자 닉/공유 비번/IP 변환** — spec §산출물 §1 6번 항목 "회원 시드 없으므로 전부 익명" 준수. 운영자(`isAdmin: true`) 공지글도 익명 닉 `"운영자"` + guest_* 3요소로 처리. mock `authorIp`("211.34") → CHECK 정규식 통과 형식("211.34.*.*")으로 `toMaskedIp` 헬퍼 변환 (T01 handover "다음 일꾼 메모"에서 미리 안내된 함정 해소). mock `createdAt` 상대시간 라벨은 ISO 역변환 불가 → spec 권장대로 `NOW() - random(0~30일)` 균등 분포 채택. bcrypt 해시 156회 → 1회로 단축(`seed123!` 공유, 모든 행 재사용).

4. **시드 총량 156행 (≠ spec "약 50") 채택** — spec 본문 "약 50"은 메인 노출 최소 보장치. `MOCK_POSTS` 평탄화 결과 실제 156(3 보드 × 52)이라 그대로 INSERT. 50으로 잘라내면 베스트30 풀 협소 + 코인룸 빈 결과 다수. 156 채택 시 페이지네이션·필터 즉시 검증 가능. handover §알려진 이슈 1번에 의사결정 근거 명시.

5. **검증 4/4 PASS** — (a) `npx tsc --noEmit` 빈 출력, (b) 컬럼 매핑 grep 27회 (기대 ≥12), (c) ESLint 0 errors (deprecated `.eslintignore` 경고만), (d) tsx import dry-run "import OK" + `[seed-community] community_boards 행이 부족 (0/9)` 가드 정상 동작 (T01 SQL 미적용 환경에서 의도된 보호).

6. **안티패턴 6항목 전체 준수** — `mock-posts.ts`/`package.json`/`supabase/migrations/*`/`_SCHEMA_REFERENCE.md`/실 DB INSERT/`seed-helpers.ts` 모두 미터치. 특히 `seed-helpers.ts`는 spec "필요 없으면 만들지 말 것" 준수 — 매핑 로직이 짧아 본 스크립트 inline 함수로 충분.

7. **본 세션 책임 한정 — T02 산출물 + cs 문서만** — git status에 R1 다른 일꾼(T08/T10/T13/T14) 산출물 + 후속 라운드(T12 `app/api/board/`, T13 `app/api/coins/hot-issues/` + RPC) + 시스템(.claude/, orchestration/) 다수 잔존. 본 일꾼 T02는 자신의 신규 2개 + 본 세션 cs 문서 4종만 명시적 staging → commit → push (세션 10/12/14/15 일꾼 패턴 준용).

**검증 결과 (본 세션 T02 산출물 한정)**:
- `npx tsc --noEmit` → 빈 출력 (전역 0 에러) ✓
- `grep -c "board_slug\|title\|content_html\|guest_nickname\|guest_password_hash\|guest_ip_masked\|category\|view_count\|like_count\|comment_count\|is_notice\|is_hot\|created_at" scripts/seed-community.ts` → **27** (기대 ≥12) ✓
- `npx eslint scripts/seed-community.ts` → 0 errors ✓
- `npx tsx --eval "import('./scripts/seed-community.ts').then(() => console.log('import OK'))"` → "import OK" + 보드 카운트 가드 정상 동작 ✓
- 안티패턴 6항목 회피 ✓

**알려진 이슈**:
- **시드 총량 156 (spec "약 50" 대비 +)**: 의사결정 항목, 줄이려면 `mapMockToRow` 호출 전 `posts.slice(0, 20)` 추가 가능. handover §알려진 이슈 1번 명시.
- **`created_at` 분포 재현 불가**: mock 상대시간 라벨을 ISO 역변환 불가 → `NOW() - random(0~30일)` 균등 분포로 대체. 베스트30/HOT 정렬은 `like_count + view_count`/`is_hot` 결정이므로 영향 미미. handover §알려진 이슈 2번 명시.
- **모든 시드 글이 익명 + 공유 비번 `seed123!`**: 회원 시드 부재로 spec 명시. 시드 글 수정 시 누구나 해당 비번으로 통과 가능 → 운영 진입 전 시드 분리(테스트 환경 전용 플래그) 권장. handover §알려진 이슈 3/5번 명시.
- **워킹트리 R1 다른 일꾼·후속 라운드 산출물 잔존**: T08/T10/T13/T14 등의 미커밋 산출물 + 시스템 untracked. 본 세션 미터치, 컨덕터 통합 영역.

**다음 작업**:
- 사용자가 `npx tsx scripts/seed-community.ts` 실행 (T01 SQL이 실 DB에 적용된 환경 가정)
- T12(board API)가 시드 글 조회 라우트 구현 → 메인페이지 hydrate
- T15(메인 실데이터 통합)가 베스트30·게시판 3컬럼을 mock fallback에서 Supabase 직접 쿼리로 교체

---

### 2026-05-23 (세션 15) — R1/T03 일꾼: 사후 검증 전용 세션 (사전 완료 확인)

**대화 토픽 흐름**: 사용자가 T02(community 시드 50글) 발사 → SessionStart hook은 **T03**(ticker-ssot) 마커 안내 (불일치) → 발사 프롬프트 본문의 "hook 우선 원칙"에 따라 T03 채택 → T03 작업 지시서 정독 → 사전 상태 점검 시 산출물 일체(`types/coins.ts` + `lib/supabase/crypto.ts` append 영역 + `app/api/coins/ticker/route.ts` + `_API_REFERENCE.md`/`_TYPE_REFERENCE.md` append 2종 + handover) 모두 워킹트리/커밋 상태 발견 → 사용자에게 "T03 이미 완료" 보고 → 사용자 /cs 입력 → cs 단계 진입 (마지막 세션 14 → 본 세션 15)

**역할**: R1 라운드(mainpage) **단일 일꾼 — 사후 검증 전용**. 본 세션 신규 코드 변경 0건, 산출물은 세션 8이 이미 완성. 본 세션 책임은 검증 보고 + cs 문서 5종 갱신만.

**변경 사항 (cs 한정, 5개 파일, 코드 변경 0건)**:

신규:
- `docs/handover/2026-05-23-session15-t03-reverify.md` — 사후 검증 인수인계 (T03 spec 일치 확인 + tsc/eslint 0 에러 + bcryptjs 누락 해소 부수 확인)

수정(cs):
- `docs/logs/journal-2026-05-23.md` — 세션 15 항목 append (T03 사후 검증 3 토픽)
- `docs/status/current.md` — 마지막 세션 메타 갱신 + 작업 이력/세션 요약표 1행 추가
- `docs/logs/2026-05.md` — 본 항목 append (세션 15)
- `docs/handover/next-dev-prompt.md` — "최근 완료된 작업" 세션 15 항목 + bcryptjs 액션 해소 반영

**주요 논의**:

1. **사용자 명시 지시 T02 vs SessionStart hook 마커 T03 — hook 우선 채택** — 세션 14는 hook(T05) vs 사용자(T07) 충돌 시 사용자 우선이었으나 본 세션은 발사 프롬프트 본문에 "SessionStart 메시지가 '일꾼 T0X'로 다른 마커 표시하면 그 T0X 작업 지시서로 대신 진행" 명시 → hook 우선. 세션 10·11·12와 동일 패턴. 일관성 확보를 위해 발사 프롬프트의 분기 규칙이 채택되었다.

2. **T03 산출물 사전 완료 발견 → 검증 세션으로 전환** — `lib/supabase/crypto.ts` Read 결과 L7에 `import type { CoinTicker } from '@/types/coins'`, L68~110에 `fetchBinanceTickers`/`fetchCommunityTickers` + 60s 메모리 캐시 완비. `types/coins.ts`/`app/api/coins/ticker/route.ts`/handover 132줄까지 모두 존재. handover §검증결과는 "PASS — count 10, BTC price 75414.25"로 명시. 신규 작업 무의미 + spec 100% 일치 시 덮어쓰기는 git noise 생성 → 검증 세션으로 전환 (세션 13 R1/T06 재발사, 세션 14 R1/T07 검증 패턴 재현).

3. **검증 — 4개 영역 PASS, bcryptjs 누락 부수 해소 확인** — 코드 3파일 spec 일치 ✓, references 2파일 append 위치 정확(API L604 + TYPE L487) ✓, handover §1~§9 명세 일치 ✓. `npx tsc --noEmit` → 출력 없음(전역 0 에러) — 세션 10~14에서 `lib/community/auth.ts(3,20): Cannot find module 'bcryptjs'` 단일 에러로 보고되던 PARTIAL이 어느 시점에 해소됨(컨덕터 또는 메인 터미널 `npm install bcryptjs` 실행 추정). 워킹트리 `M package.json`/`M package-lock.json`이 그 흔적.

4. **본 세션 책임 한정 — cs 문서 5종만** — git status에 R1 다른 일꾼(T08/T10/T14) 산출물 + 후속 라운드 산출물(T02 `scripts/seed-community.ts`, T12 `app/api/board/`, T13 `app/api/coins/hot-issues/` + RPC 마이그레이션) + 시스템(.claude/, orchestration/) 다수 잔존. 본 일꾼 세션 15는 코드 변경 0건이므로 cs 문서 5종만 명시적 staging → commit → push (세션 10/12/14 일꾼 패턴 준용).

**검증 결과 (T03 사후 검증, 본 세션 산출물 변경 0건)**:
- `ls types/ app/api/coins/ docs/handover/ | grep -E "coins\\.ts|ticker|T03"` → 4건 존재 ✓
- `grep "coins/ticker\|CoinTicker\|CoinSnapshot" docs/references/_*.md` → 4건 매칭 (API L604/L612, TYPE L487/L489) ✓
- `npx tsc --noEmit` → 출력 없음 (전역 0 에러) ✓
- `npx eslint types/coins.ts app/api/coins/ticker/route.ts` → 0 errors (deprecated `.eslintignore` 경고만) ✓
- 코드 3파일 본문 Read 결과 spec 일치 ✓

**알려진 이슈**:
- **워킹트리 R1 다른 일꾼·후속 라운드 산출물 잔존**: 컨덕터 통합 커밋 영역. 본 세션 미터치. 특히 `M package.json/-lock.json`은 bcryptjs 설치 흔적으로 추정.
- **세션 번호 충돌 없음**: 본 세션은 14 직후 15로 안전 채택.

---

### 2026-05-23 (세션 14) — R1/T07 일꾼: 검증 전용 세션 (사전 완료 확인)

**대화 토픽 흐름**: 사용자가 T07(auth-middleware) 발사 → SessionStart hook은 T05 마커 안내(불일치) → 사용자 명시 지시 우선 채택 → T07 작업 지시서 정독 → 사전 상태 점검 시 산출물 일체(`lib/community/auth.ts` + `lib/community/ip-mask.ts` + `middleware.ts` + references 2종 append + handover) 모두 워킹트리/머지 상태 발견 → `git log` 확인 → 세션 10 커밋 `30350f5`로 이미 origin/main 머지 완료 확인 → 사용자에게 "T07 이미 완료" 보고 + 다음 액션 후보(npm install bcryptjs / T12 착수 / 다른 T0X) 제시 → 사용자 /cs 입력 → cs 단계에서 병렬 터미널이 세션 13을 점유함을 발견 → 본 세션 번호 14로 정정 후 cs 진행

**역할**: R1 라운드(mainpage) **단일 일꾼 — 사후 검증 전용**. 본 세션 신규 코드 변경 0건, 산출물은 세션 10이 이미 완성. 본 세션 책임은 검증 보고 + cs 문서 5종 갱신만.

**변경 사항 (cs 한정, 5개 파일, 코드 변경 0건)**:

신규:
- `docs/handover/2026-05-23-session14-t07-verification.md` — 사후 검증 인수인계 (T07 spec 일치 확인 + PARTIAL 유지 + 다음 액션 제안)

수정(cs):
- `docs/logs/journal-2026-05-23.md` — 세션 14 항목 append (T07 검증 3 토픽)
- `docs/status/current.md` — 마지막 세션 메타 갱신 + 작업 이력/세션 요약표 1행 추가
- `docs/logs/2026-05.md` — 본 항목 append (세션 14)
- `docs/handover/next-dev-prompt.md` — "최근 완료된 작업" 세션 14 항목 + bcryptjs 액션 강조

**주요 논의**:

1. **사용자 명시 지시 T07 vs SessionStart hook 마커 T05 — 사용자 우선 채택** — 세션 10·11·12 사례는 모두 "hook 마커가 더 구체적·정확" → hook 우선이었으나 본 세션은 정반대 패턴. 글로벌 CLAUDE.md "User's explicit instructions — highest priority" 원칙 적용. hook 마커가 가리킨 T05 산출물(`lib/news/classifier.ts`)도 이미 커밋 `a8b270b`에 사전 머지된 상태 → 양쪽 다 사후 검증 의도였을 가능성 인지.

2. **T07 산출물 사전 완료 발견 → 검증 세션으로 전환** — `git log --oneline -20` 첫 줄에 `30350f5 feat: 세션 10 — R1/T07 일꾼 익명 bcrypt + IP 마스킹 + middleware 머지 (9개 파일)` 발견. 워킹트리 파일 5종(`auth.ts`/`ip-mask.ts`/`middleware.ts`/references 2종/`handover/2026-05-23-R1-T07-auth-middleware.md`) 모두 존재. 세션 12 cs 문서(current.md/next-dev-prompt)도 세션 10 T07 완료를 명시 → 신규 작업 무의미 + spec과 100% 일치 시 덮어쓰기는 git noise 생성 → 검증 세션으로 전환.

3. **검증 — 4개 영역 PASS, 1개 PARTIAL** — 코드 3파일 spec 일치 ✓, references 2파일 append 위치 정확(L117 + L491) ✓, handover §1~9 명세 일치 ✓, `node -e "require('bcryptjs')"` → MODULE_NOT_FOUND (PARTIAL 유지). 세션 10 handover §2가 명시한 메인 터미널 액션 `npm install bcryptjs @types/bcryptjs` 미실행 상태 유지.

4. **세션 번호 충돌 — 13 → 14 정정** — cs 사전 준비 중 다른 터미널이 동시에 세션 13을 점유한 흔적 발견(저널 §세션13 R1/T06 재발사 추가 + current.md 마지막 세션이 "13 — R1/T05 일꾼"로 갱신됨). 본 세션 handover/저널/문서 모두 13 → 14로 정정 후 진행. 병렬 일꾼 환경에서의 세션 번호 충돌은 본 세션이 첫 사례 — 향후 컨덕터가 dispatch 시 세션 번호를 사전 할당하는 운영 패턴 검토 권장.

5. **본 세션 책임 한정 — cs 문서 5종만** — git status에 R1 다른 일꾼(T05/T06/T08/T10/T14) 산출물 + 사전 변경(블로그 에디터/analysis 페이지 lightify 등) + 신규 hook/orchestration 디렉토리 잔존. 본 일꾼 세션 14는 코드 변경 0건이므로 cs 문서 5종만 명시적 staging → commit → push (세션 10/12 일꾼 패턴 준용).

**검증 결과 (T07 사후 검증, 본 세션 산출물 변경 0건)**:
- `git log --oneline -20` → 세션 10 커밋 `30350f5` 확인 ✓
- `ls -la lib/community/ middleware.ts` → 3파일 모두 존재 ✓
- `ls docs/handover/ \| grep T07` → `2026-05-23-R1-T07-auth-middleware.md` ✓
- `grep "IP_HASH_SECRET\|auth · ip-mask\|x-client-ip-masked" docs/references/_*.md` → 4건 매칭 ✓
- `node -e "require('bcryptjs')"` → ❌ MODULE_NOT_FOUND (PARTIAL 유지)
- `grep '"bcryptjs"' package.json` → 0건 (예상)
- 코드 3파일 본문 Read 결과 spec 일치 ✓

**알려진 이슈**:
- **bcryptjs 미설치 (PARTIAL, 세션 10부터 유지)**: 메인 터미널 `npm install bcryptjs @types/bcryptjs` 실행 필요. 설치 즉시 `lib/community/auth.ts` TS2307 해소 + T12(board API) 일꾼 진행 가능.
- **워킹트리 R1 다른 일꾼 산출물 잔존**: 컨덕터 통합 커밋 영역. 본 세션 미터치.
- **세션 번호 13 중복 사용**: 본 세션과 다른 터미널이 동시에 13 시작 → 본 세션이 14로 양보. 저널에 "세션 13" 항목 2개(R1/T06 재발사) + 본 세션 14(R1/T07 검증)이 공존.

---

### 2026-05-23 (세션 13) — R1/T05 일꾼: 뉴스 룰베이스 분류 라이브러리 (검증·인수)

**대화 토픽 흐름**: 사용자가 T05 일꾼으로 dispatch → 작업 지시서(`T05-news-classifier.md`) 정독 → 워킹트리 상태 점검 → `lib/news/classifier.ts`·`keyword-dict.ts`·`__tests__/lib/news-classifier.test.ts`·`docs/handover/2026-05-23-R1-T05-news-classifier.md` 4개가 다른 T05 일꾼이 이미 작성하여 잔존함을 발견 → 사용자에게 진행 방식 확인 → "기존 산출물 검증 + handover 인수" 선택 → 지시서 사양 1:1 대조 + TS/런타임/Vitest/ESLint 4종 검증 PASS → 본 세션 handover는 별도 작성하지 않고 기존 파일 채택 → 공유 문서 갱신 + 커밋

**역할**: R1 라운드(mainpage) **단일 일꾼 — 검증·인수 책임**. 산출물은 다른 T05 일꾼이 이미 작성했고, 본 세션은 사용자 결정에 따라 검증과 공유 문서 갱신만 수행. 코드 0줄 추가.

**변경 사항 (T05 한정)**:

기존 잔존 (다른 T05 일꾼 작성, 본 세션 인수):
- `lib/news/keyword-dict.ts` (159 lines) — COIN(8) + CATEGORY(8) + POSITIVE(25) + NEGATIVE(26) + SOURCE_WEIGHTS(9)
- `lib/news/classifier.ts` (144 lines) — `classify(input)` 단일 진입점, 4차원(coinTag/category/sentiment/importance) 산출
- `__tests__/lib/news-classifier.test.ts` (86 lines) — 5대 시나리오 + 보조 검증 3개 = 8 케이스
- `docs/handover/2026-05-23-R1-T05-news-classifier.md` (229 lines) — 다른 T05 일꾼 작성 handover, T06/T15 통합 가이드까지 포함

본 세션 직접 추가/수정: 코드 0줄. 공유 문서(`docs/status/current.md`, `docs/handover/next-dev-prompt.md`, `docs/logs/2026-05.md`) 세션 13 행만 추가.

**주요 논의**:

1. **이미 작성된 산출물 발견** — `git status` 출력에서 `?? lib/news/` 및 `?? __tests__/lib/news-classifier.test.ts`가 untracked로 잔존. 더불어 본 세션 시작 직후 `?? docs/handover/2026-05-23-R1-T05-news-classifier.md`도 발견(다른 일꾼이 동시 작성). 본 세션이 동일 코드를 재작성하면 다른 일꾼 작업과 충돌 위험.

2. **사용자 결정 — 검증 + 인수** — AskUserQuestion으로 3개 선택지 제시(검증·인수 / 전면 재작성 / 본 세션 종료). 사용자는 "기존 산출물 검증 + handover 작성"(Recommended) 선택. 단 handover는 이미 존재하므로 본 세션은 검증과 공유 문서 갱신만.

3. **지시서 대비 강화점 (다른 T05 일꾼의 판단)** — (a) `escapeRegExp` 추가로 `S&P`·`현물 ETF`·`원/달러` 정규식 안전 처리, (b) NEGATIVE에 `정지`/`중단`/`장애`/`과세`/`세금`/`연기` 추가로 "솔라나 네트워크 일시 정지" 테스트 케이스 통과, (c) POSITIVE에 `절감`/`개선`/`복구` 추가로 "트랜잭션 비용 30% 절감" 테스트 통과, (d) 테스트 보조 3개 추가(matchedKeywords 일관성, 호악재 동시 출현, 중요도 범위). 지시서 원본 사전으로는 5/8만 통과했음을 다른 T05 일꾼의 handover §3에 명시.

4. **T05 ↔ T06 통합 완료 확인** — current.md에 세션 11(T06)이 이미 `app/api/admin/news-crawl/route.ts`에 `classify()` 호출을 통합했고 `supabase/migrations/20260523_alter_news_classify.sql`로 `category`/`importance_score`/`sentiment_score` 3컬럼 ALTER 완료. T05 ↔ T06 양방향 연결은 사실상 완료, 본 세션은 후행 정리.

**검증 결과**:
- `npx tsc --noEmit` — T05 영역(`lib/news/*`) 에러 0건 (잔존 에러는 다른 일꾼 영역)
- `npx tsx -e "classify({title:'비트코인 ETF 자금 유입 200억 달러 돌파', source:'블룸버그', pubDate:new Date()})"` — `{coinTag:'BTC', category:'etf', sentiment:'positive', importance:8, sentimentScore:5}` PASS (지시서 기대 `importance ≥ 6` 충족)
- `npx vitest run __tests__/lib/news-classifier.test.ts` — **8 passed** (1 file, 1.05s)
- `npx eslint lib/news/* __tests__/lib/news-classifier.test.ts` — 0 errors, 0 warnings

**다음 일꾼/컨덕터에게 메모**:
- T05 ↔ T06 통합 완료, 별도 액션 없음
- T15(메인페이지 hydrate)는 `NewsCategory` 값 ↔ UI `NEWS_CATEGORIES` 라벨 매핑 필요 (특히 `altcoin_news`는 UI 미존재 — T15가 정책 결정: market 흡수 또는 UI 추가)
- 키워드 사전 확장 시 `lib/news/keyword-dict.ts`만 수정하면 됨 (classifier 로직 무관)

---

### 2026-05-23 (세션 12) — R1/T04 일꾼: Fear & Greed Index 프록시

**대화 토픽 흐름**: R1 dispatch 일꾼 발사 (사용자 표면 마커=T01, SessionStart hook 마커=**T04** + 작업 경로 범위 5종, 발사 프롬프트 본문에 "hook 마커 우선" 명시 → T04 채택) → 작업 명세서(`T04-fng-proxy.md`) 정독 → SOT 점검(`FngGaugeWidget.tsx` props, `app/api/news/route.ts` 스타일) → `lib/community/fng.ts` 작성 (fetchFng + 1h 메모리 캐시 + FngSnapshot 타입) → `app/api/fng/route.ts` 작성 (GET + try/catch + 502 폴백) → `_API_REFERENCE.md` append → `_ENV_REFERENCE.md` append (T07 사전 머지로 idempotent 판명) → `npx tsc --noEmit` 검증 → 라이브 fetch 검증 (tsx dynamic import interop 회피 후 PASS) → handover 작성

**역할**: R1 라운드(mainpage) **단일 일꾼** 세션. 다른 일꾼(T06 news / T07 auth / T08 chart-theme / T10 analysis-lightify / T14 translations 등)은 별도 터미널에서 동시 작업 중이며 워킹트리에 산출물 잔존. 본 세션은 **T04 산출물만 책임**. 컨덕터 통합 대기.

**변경 사항 (T04 한정, 4개 파일 + cs 문서 4개)**:

신규:
- `lib/community/fng.ts` — Alternative.me Fear & Greed Index fetch (`FNG_URL = "https://api.alternative.me/fng/?limit=2&format=json"`) + `FngSnapshot` 인터페이스(`value: 0~100`, `classification: enum`, `prevValue?`, `timestamp: unix ms`) + 1시간 모듈 메모리 캐시 + Next `revalidate: 3600`
- `app/api/fng/route.ts` — `GET /api/fng` 라우트. `fetchFng()` 호출 후 `NextResponse.json`, 실패 시 502 + `{error: string}`. `export const revalidate = 3600`
- `docs/handover/2026-05-23-R1-T04-fng-proxy.md` — 일꾼 인수인계 (T15에 `/api/fng` 엔드포인트·502 시 fallback UI 권장·위젯 props 직매핑·라이브 fetch PASS 결과 명시)

수정:
- `docs/references/_API_REFERENCE.md` — `### GET /api/fng` 항목 append (T03 `/api/coins/ticker` 블록 직후). 응답 스키마(`FngSnapshot`), 502 처리, 1시간 캐시 정책, T15 사용처 명시
- `docs/references/_ENV_REFERENCE.md` — `### FNG 외부 API (R1/T04 2026-05-23)` append 시도 (ENV 추가 없음·1h 캐시·502 fallback 권장). **단, T07 커밋(`30350f5`)이 동일 섹션을 이미 포함한 채 머지되어 있어 git diff 0 — 사실상 idempotent 무효 변경**

**주요 논의**:

1. **SessionStart hook 마커 vs 사용자 발사 프롬프트 표면 마커 불일치 (T06과 동일 패턴)** — 사용자 발사 프롬프트 표면은 "일꾼 T01", SessionStart hook은 "일꾼 T04 (R1)" + 작업 경로 범위(`app/api/fng/;lib/community/fng.ts;_ENV_REFERENCE.md;_API_REFERENCE.md;docs/handover/`)가 T04 산출물 경로와 100% 일치. 발사 프롬프트 본문에 "SessionStart 메시지가 '일꾼 T0X'로 다른 마커 표시하면 그 T0X 지시서를 대신 읽고 실행" 명시 → T04 채택.

2. **위젯과의 props 매핑 — 직매핑 가능 확인** — `FngGaugeWidget.tsx`의 props 시그니처는 `(value, prevValue?, label?)`이고 `FngSnapshot`의 `value`/`prevValue`가 그대로 직매핑됨. `label`은 위젯 내부에 한국어 라벨이 자체 매핑되어 있어 굳이 전달 불필요 (영문이 필요하면 `snapshot.classification`을 전달). T15에서 추가 변환 로직 불필요.

3. **이중 캐시 설계** — (a) `lib/community/fng.ts`의 모듈 메모리 `cache` 객체 (1h TTL) + (b) `fetch` 옵션 `next: { revalidate: 3600 }` + (c) 라우트 파일 `export const revalidate = 3600`. 처음 호출 시만 외부 API 부하, 이후 1h 내 호출은 메모리 히트. 위젯이 SSR/ISR로 들어오면 페이지 캐시 정책에 의해 추가로 더 길게 살 수 있음.

4. **외부 API 다운 시 502 — fallback UI 권장 (T15 메모)** — `app/api/fng/route.ts`는 fetch 실패 시 `{error}` + 502 반환. T15가 메인페이지에서 호출 시 try/catch 또는 응답 status 체크 후 fallback UI(예: 회색 게이지 + "데이터 불러올 수 없음", 또는 현재 하드코딩 `value=72, prevValue=68` 재사용) 권장 사항을 handover에 명시.

5. **tsx dynamic import interop 회피** — 라이브 fetch 검증 시 `import('./lib/community/fng').then(m => m.fetchFng())` 호출이 `m.fetchFng is not a function`으로 실패. 원인: tsx의 dynamic `import()`가 named export를 `{default: ...}`로 래핑(ESM/CJS interop). 정적 `import { fetchFng } from './lib/community/fng.ts'` 문법으로 변경 후 `{"value":28,"classification":"Fear","prevValue":28,"timestamp":1779494400000}` 출력 PASS. 세션 8(T03), 세션 10(T07)에서도 동일 패턴 인지 → 솔루션 문서 신규 작성 생략.

6. **_ENV_REFERENCE.md idempotent 발견** — /cs 단계에서 `git diff HEAD -- docs/references/_ENV_REFERENCE.md` 결과 0. 원인 추적: T07 커밋(`30350f5`)이 본 세션 발사 시점 이전에 이미 `### FNG 외부 API (R1/T04 2026-05-23)` 섹션을 동일 텍스트로 머지해 둔 상태였음. 본 세션 작성과 우연히 일치 → 변경 무효지만 함정 아님. 컨덕터가 다른 일꾼들의 사전 머지를 함께 처리하는 운영 패턴으로 추정.

**검증 결과 (T04 한정)**:
- `npx tsc --noEmit`: T04 영역(`lib/community/fng.ts`, `app/api/fng/route.ts`) 에러 **0건**. 잔여 에러 1건은 `lib/community/auth.ts(3,20): Cannot find module 'bcryptjs'` — T07 영역(세션 10) 알려진 PARTIAL
- 라이브 fetch (정적 import): `{"value":28,"classification":"Fear","prevValue":28,"timestamp":1779494400000}` — 0~100 정수 + 약속된 enum 라벨 PASS
- ESLint: 신규 2파일 0 errors
- 안티패턴 준수 확인: `FngGaugeWidget.tsx` 미수정 ✓, FNG 외 외부 API 추가 없음 ✓, ENV 추가 없음 ✓, `app/page.tsx` 미수정 ✓, `lib/community/` 내 타 파일 미수정 ✓

**T15에 남기는 인터페이스 명세 (코드 예시)**:
```ts
// 옵션 A — API 경유 (CSR/하이드레이션)
const res = await fetch('/api/fng');
if (res.ok) {
  const fng = await res.json();
  return <FngGaugeWidget value={fng.value} prevValue={fng.prevValue} />;
}
return <FngGaugeWidget value={72} prevValue={68} />; // fallback

// 옵션 B — 서버 컴포넌트 직접 호출
import { fetchFng } from "@/lib/community/fng";
try {
  const fng = await fetchFng();
  return <FngGaugeWidget value={fng.value} prevValue={fng.prevValue} />;
} catch {
  return <FngGaugeWidget value={72} prevValue={68} />;
}
```

---

### 2026-05-23 (세션 11) — R1/T06 일꾼: 뉴스 분류 4차원 DB·API 통합

**대화 토픽 흐름**: R1 dispatch 일꾼 발사 (사용자 마커=T02, SessionStart hook 마커=**T06**, 발사 프롬프트 본문에 "hook 마커 우선" 명시 → T06 채택) → 작업 명세서(`T06-news-classify-integration.md`) 정독 → T05 handover(`classify()` 인터페이스) read → SOT 5종 병렬 read → 마이그레이션 SQL 작성 → crawler `classify()` 통합 + 인라인 매칭 7줄 제거 → news API 응답 4종 확장 + 쿼리 파라미터 2종 추가 → `_SCHEMA_REFERENCE.md` append → tsc/grep/eslint 5/5 검증 → handover 작성

**역할**: R1 라운드(mainpage) **단일 일꾼** 세션. 다른 일꾼(T04 fng-proxy / T10 analysis-lightify 등)은 별도 터미널에서 동시 작업 중이며 워킹트리에 산출물 잔존. 본 세션은 T06 산출물만 책임. 컨덕터 통합 대기.

**변경 사항 (T06 한정, 5개 파일)**:

신규:
- `supabase/migrations/20260523_alter_news_classify.sql` — `news` 테이블에 `category` text + CHECK enum 8값, `importance_score` smallint + CHECK 1~10, `sentiment_score` integer 추가. 인덱스 `idx_news_category_pubdate` / `idx_news_importance`. 모두 `IF NOT EXISTS` 보호 idempotent
- `docs/handover/2026-05-23-R1-T06-news-classify-integration.md` — 일꾼 인수인계 (T15가 호출할 엔드포인트·응답 형식·camelCase 매핑 표 포함)

수정:
- `app/api/admin/news-crawl/route.ts` — `import { classify } from '@/lib/news/classifier'` 추가. RSS item 파싱 직후 `classify({ title, snippet, source, pubDate })` 호출, 결과의 `coinTag`/`category`/`importance`/`sentimentScore`/`sentiment`를 DB insert payload에 적재. 기존 인라인 `lowerTitle.includes('bitcoin') → 'BTC'` 식 매칭 7줄 삭제 (classifier가 빈도 우선 다중 매칭으로 대체)
- `app/api/news/route.ts` — `SELECT *` → 명시적 컬럼 목록(12종) 교체, 응답 매핑에 `symbol`/`category`/`importance`/`sentimentScore` camelCase 추가, 쿼리 파라미터 `?category=<enum>` (`.eq()`) + `?minImportance=<1~10>` (`.gte()` + 범위 검증) 신규 처리
- `docs/references/_SCHEMA_REFERENCE.md` — `news` 컬럼 표에 3컬럼 + R1 인덱스 2종 추가, "news (R1 2026-05-23 확장)" 서브섹션 append (마이그레이션 경로 + 통합 위치 + API 노출 명세)

**주요 논의**:

1. **SessionStart hook 마커 vs 사용자 발사 프롬프트 표면 마커 불일치** — 사용자 발사 프롬프트 표면은 "일꾼 T02", SessionStart hook은 "일꾼 T06 (R1)". 발사 프롬프트 본문에 "SessionStart 메시지가 '일꾼 T0X'로 다른 마커 표시하면 그 T0X 작업 지시서를 대신 읽고 실행" 명시 + hook payload의 허용 작업 경로(`supabase/migrations/;app/api/admin/news-crawl/;app/api/news/;docs/references/_SCHEMA_REFERENCE.md;docs/handover/`)가 T06 산출물 경로와 100% 일치 → T06 채택. 세션 9 T08 사례(hook 잘못 주입)와 정반대 — T06은 hook이 정확하고 발사 프롬프트 본문이 hook 우선을 명시.

2. **T05 권장 컬럼명 vs 작업 지시서 SOT 불일치** — T05 handover §6은 `coin_tag`/`importance`/`matched_keywords` 컬럼을 권장했으나 T06 작업 지시서 SOT는 `category`/`importance_score`/`sentiment_score`(3개). 작업 지시서 우선 채택, `coinTag`는 기존 `symbol` 컬럼 재활용(별도 ALTER 없음), `matched_keywords` jsonb는 미채택(디버그용이며 운영 부담). T05의 `sentimentScore`(원점수)는 신규 `sentiment_score` 컬럼에 저장.

3. **crawler 인라인 매칭 제거 — 안티패턴 해석** — 안티패턴은 "기존 sentiment 채움 로직 교체"만 명시했으나 symbol 채움 로직(7줄)도 동시에 존재. 더 정확한 분류기로 교체하는 것이 spec 의도("classifier 결과 단일 진실")에 부합 → 제거. 기본값 `'GENERAL'` → classifier의 `'ALL'`로 자동 통일 (DB 기본값 'ALL'과도 일치).

4. **enum 어긋남 위험 0** — classifier의 `NewsSentiment` (`positive`/`negative`/`neutral`/`mixed`)가 기존 DB `news.sentiment` CHECK 제약과 1:1 일치, 신규 `NewsCategory` 8값이 새 `category` CHECK 제약과 1:1 일치 → 런타임 INSERT 실패 가능성 0.

5. **news API 필드명 camelCase 정규화** — 기존 코드가 `pub_date` → `pubDate`, `source` → `publisher` 변환 수행 중. 신규 컬럼도 동일 패턴: `importance_score` → `importance`, `sentiment_score` → `sentimentScore`. T15가 메인 NewsRow에 직접 바인딩 가능.

6. **다른 일꾼 산출물 격리** — `git status`에 T04(fng-proxy: `app/api/fng/`, `lib/community/fng.ts`, T04 handover), T10(analysis-lightify: `app/analysis/*` × 4, `components/Analysis/*` × 4, T10 handover) 산출물이 워킹트리에 잔존. 본 일꾼은 자기 5개 파일(+ 세션 종료 4개 문서)만 명시적 staging → commit → push.

**검증** (5/5 PASS):
- `npx tsc --noEmit` — news 영역 에러 **0건**. 무관 잔존 에러 1건(`lib/community/auth.ts(3,20): Cannot find module 'bcryptjs'`)은 T07 영역 알려진 이슈.
- `grep -c "classify(" app/api/admin/news-crawl/route.ts` = **2** (기준 ≥1)
- `grep -c "category\|importance_score\|sentiment_score" app/api/news/route.ts` = **9** (기준 ≥3)
- `grep -c "ADD COLUMN" supabase/migrations/20260523_*.sql` = **3** (기준 3)
- `grep -c "CREATE INDEX" supabase/migrations/20260523_*.sql` = **2** (기준 2)
- `npx eslint app/api/admin/news-crawl/route.ts app/api/news/route.ts` — 에러 0 (deprecated `.eslintignore` 경고만, 전역 설정 이슈)

**다음 일꾼/컨덕터에게 메모**:
- 컨덕터: 실 DB 적용 시 `npx supabase db push` 또는 SQL 에디터에서 `20260523_alter_news_classify.sql` 실행. `IF NOT EXISTS` 보호로 재실행 안전.
- T15(메인페이지): `GET /api/news?category=etf&minImportance=6&page=0` 형태로 호출. 응답 `items[].category` / `items[].importance` (1~10) / `items[].sentiment` 직접 바인딩 가능. `altcoin_news` 카테고리는 UI `NEWS_CATEGORIES` 라벨 누락 — T15가 `market`으로 합치거나 라벨 추가 결정.

---

### 2026-05-23 (세션 10) — R1/T07 일꾼: 익명 bcrypt + IP 마스킹 + middleware 머지

**대화 토픽 흐름**: R1 dispatch 일꾼 T07 발사 → 작업 명세서(T07-auth-middleware.md) 정독 → SOT(package.json, PROJECT_DIRECTION §3, ENV/TYPE 레퍼런스, 기존 `middleware.ts`) read → 산출물 5종 생성 → 기존 supabase 미들웨어와 머지 → tsx 런타임 검증 → tsc/eslint → handover

**역할**: R1 라운드(mainpage) **단일 일꾼** 세션. 다른 일꾼(T01/T05/T08/T14)은 별도 터미널에서 동시 작업 중. 본 세션은 자체 산출물(T07)만 책임. R1 통합은 컨덕터 영역. (세션 번호 9는 T01/T14/T05/T08 일꾼들이 logs를 동시 갱신하며 충돌 — 본 일꾼은 10으로 채택)

**변경 사항 (T07 한정, 5개 파일)**:

신규:
- `lib/community/auth.ts` — `bcryptjs` 기반 해시/검증 + `validateGuestNickname` (2~12자)
- `lib/community/ip-mask.ts` — `extractClientIp` / `maskIp` / `hashIp` (HMAC-SHA256)
- `docs/handover/2026-05-23-R1-T07-auth-middleware.md` — 일꾼 인수인계

수정:
- `middleware.ts` — 기존 Supabase 세션 새로고침 + 보호 경로 리다이렉트 로직 보존. IP 헤더 3종(`x-client-ip`/`-masked`/`-hash`) 주입 로직을 supabase 호출 앞에 배치. matcher에 `/api/board/:path*`, `/api/community/:path*` 추가. `/api/` 경로는 supabase getUser 스킵
- `docs/references/_ENV_REFERENCE.md` — `IP_HASH_SECRET` append
- `docs/references/_TYPE_REFERENCE.md` — `auth · ip-mask 모듈` 섹션 append (T12가 의존할 헤더 명세 포함)

**주요 논의**:

1. **bcryptjs 미설치 — PARTIAL 처리** — `package.json` Read 결과 `bcryptjs` 부재 확인. 본 일꾼은 `package.json` 수정 권한 없음(작업 명세서 안티패턴). 코드는 정상 작성하고 handover의 "미해결" 섹션에 `npm install bcryptjs @types/bcryptjs`를 메인 터미널 후속 조치로 명시. TS 에러는 bcryptjs 1건만 발생, 설치 즉시 해결.

2. **middleware 머지 전략** — 기존 `middleware.ts`가 supabase 세션 + 보호 경로 리다이렉트를 수행 중. 작업 명세서는 "신규" 라고 적시했으나 실제 존재하여 머지 결정:
   - IP 헤더 주입 → supabase 호출 → 보호 경로 리다이렉트 순서
   - `/api/` 경로는 supabase getUser 스킵 (커뮤니티 API는 자체 인증 사용)
   - `setAll` 콜백에서 `request.headers` 대신 IP 주입된 `requestHeaders` 사본 전달
   - matcher 기존 페이지 패턴 + `/api/board` + `/api/community` 추가

3. **IP 마스킹 검증 우회** — 세션 8(T03)과 동일하게 `tsx -e import('./lib/community/ip-mask')`가 named export를 `default` 아래로만 노출하는 ESM/CJS 변환 이슈 재발생. `m.default ?? m`로 우회 → `maskIp('211.34.123.45') → 211.34.*.*`, hash 64자 hex PASS.

4. **T12 의존성 인터페이스 명시** — handover §4에 헤더 3종, matcher 경로, auth 헬퍼 시그니처, 라우트 헤더 읽기 패턴까지 코드 예시 포함. T12 일꾼이 별도 컨텍스트에서 발사돼도 본 산출물만으로 board API 작성 가능.

5. **다른 일꾼 산출물 격리** — `git status`에 T01(community migrations) / T05(news classifier) / T08(chart theme) / T14(translations) 산출물 잔존. 본 일꾼 commit 대상은 T07 5개 파일만.

**검증**:
- IP 마스킹 런타임 — `maskIp('211.34.123.45') → '211.34.*.*'`, `maskIp('invalid') → '0.0.*.*'`, `hashIp('1.2.3.4').length === 64` PASS
- `npx tsc --noEmit` — bcryptjs 1건 (예상됨, 설치 후 해소). `ip-mask.ts`/`middleware.ts` PASS
- `npx eslint lib/community/auth.ts lib/community/ip-mask.ts middleware.ts` — 0 errors (1 warning: 기존 supabase setAll의 unused `options`, 본 작업 무관)
- `node -e "require('bcryptjs')"` — MODULE_NOT_FOUND (예상됨)

**다음 일꾼/컨덕터에게 메모**:
- 메인 터미널: `npm install bcryptjs @types/bcryptjs` 실행 후 R1 통합 진행
- 운영 배포 전: `IP_HASH_SECRET=<32바이트 hex>` 환경변수 설정 권장 (`openssl rand -hex 32`)
- T12(board API): 헤더 3종(`x-client-ip-masked`, `x-client-ip-hash`)을 `community_posts.guest_ip_masked` / `community_post_likes.ip_hash` 컬럼에 저장. T01 시드 스키마(`guest_ip_masked` regex `^\d+\.\d+\.\*\.\*$`)와 본 마스킹 출력이 일치 확인됨

---

### 2026-05-23 (세션 9) — R1/T14 일꾼: 번역 키 정리 + 헤더 인라인 분기 제거

**대화 토픽 흐름**: R1 dispatch 일꾼 T14 발사 → 작업 명세서(T14-translations-cleanup.md) 정독 → SOT 2개(lib/translations.ts, components/global-header.tsx) read → menu 그룹에 신규 키 9개(ko/en) append → 헤더의 인라인 한/영 분기 8건을 `t.menu.*` 호출로 교체 → 잔여 분기 4건 식별 → tsc 검증 → handover 작성

**역할**: R1 라운드(mainpage) 단일 일꾼 세션. 다른 일꾼(T01/T05/T07/T08 등)은 별도 터미널에서 동시 작업 중이며 워킹트리에 산출물 잔존. 본 세션은 T14 산출물만 책임지며 다른 일꾼 파일은 미터치(컨덕터 통합 영역).

**변경 사항 (T14 한정, 3개 파일)**:

수정 (append-only):
- `lib/translations.ts` — `translations.ko.menu` / `translations.en.menu` 각각 끝에 9개 키 신규 append: `best`, `boardFree`, `boardMarket`, `boardInfo`, `coinRoom`, `tools`, `write`, `search`, `login`. `news`는 기존 존재로 중복 회피. 기존 키 0건 수정
- `components/global-header.tsx` — JSX 구조·아이콘·className 무변경. 인라인 분기 8건만 `t.menu.{key}` 형식으로 교체 (L43~46 1차 메뉴 5개 중 4개, L52 coinRoom 라벨, L65 tools 라벨, L153 데스크탑 글쓰기, L207 모바일 글쓰기)

신규:
- `docs/handover/2026-05-23-R1-T14-translations-cleanup.md` — 일꾼 인수인계

**잔여 인라인 분기 4건 (의도적)**:
- `lang === "ko" ? "알트코인" : "Altcoin"` (L58) — 스펙 키 목록 외
- `lang === "ko" ? "김치프리미엄" : "Kimchi Premium"` (L59) — 스펙 키 목록 외
- `lang === "ko" ? "EN" : "KR"` (L143, L213) — 언어 토글 라벨은 본질적으로 lang 반대를 표시해야 하므로 단일 키 환원 불가능

**검증 결과**:
- 번역 키 추가 검증: `grep -c "best:|boardFree:|...|write:" lib/translations.ts` = 14 (= 7키 × ko/en, 사양 "14 이상" 충족)
- 인라인 분기 잔여 검증: `grep -c "lang === \"ko\" ?" components/global-header.tsx` = 4 (의도적 잔류와 일치)
- `npx tsc --noEmit` — 본 작업 수정 파일 관련 에러 0건. 사전 에러 3건(`lib/chart/theme.ts` × 2 'solid' 할당, `lib/community/auth.ts` bcryptjs 모듈 미존재)은 본 T14 무관 (다른 일꾼 책임 또는 컨덕터 통합 단계 책임)

**커밋**: 본 일꾼은 커밋하지 않음 (컨덕터가 R1 일꾼 전체 산출물을 통합 커밋)

---

### 2026-05-23 (세션 9) — R1/T08 일꾼: 차트 라이트 테마 + BlogEditor `tone` prop

**대화 토픽 흐름**: R1 dispatch 일꾼 T08 발사 → 작업 명세서(T08-chart-theme-editor-tone.md) 정독 → SessionStart hook 범위(T15)와 task 범위(T08) 불일치 인지 → BlogEditor 실제 경로 확인(`components/Blog/editor/`) → 디자인 토큰 검증(globals.css) → lightweight-charts 버전 확인(v5.0.9) → `lib/chart/theme.ts` 작성 → BlogEditor/EditorToolbar tone prop 추가 → tsc에서 ColorType enum 이슈 발견·해결 → 검증 통과 → handover 작성

**역할**: R1 라운드(mainpage) 단일 일꾼 세션. 1차 발사(의존 없음). 본 세션 산출물에 **T09(signal)·T10(analysis)·T11(market) 후속 일꾼이 의존**. 페이지 적용은 후속 일꾼 영역, 본 일꾼은 인프라 모듈만 책임.

**변경 사항 (T08 한정, 4개 파일)**:

신규:
- `lib/chart/theme.ts` (+71) — `LIGHT_CHART_THEME` / `DARK_CHART_THEME` / `KR_CANDLE_COLORS` / `US_CANDLE_COLORS` + `getChartTheme(tone)` / `getCandleColors(scheme)` 헬퍼
- `docs/handover/2026-05-23-R1-T08-chart-theme-editor-tone.md` — 일꾼 인수인계 (T09·T10·T11 사용법 안내)
- `docs/solutions/2026-05-23-lightweight-charts-v5-colortype-enum.md` — compound knowledge

수정:
- `components/Blog/editor/BlogEditor.tsx` — `tone?: 'light' | 'dark'` prop 추가 (default `'light'`), `EditorTone` 타입 export, EditorContent prose 클래스 분기(다크 시만 `prose-invert`), containerClass 분기, EditorToolbar에 tone 전파
- `components/Blog/editor/EditorToolbar.tsx` — `tone?` prop 추가(default `'light'`), wrapper className만 light/dark 분기 (`bg-surface-container border-outline-variant text-on-surface` ↔ 기존 다크)

**주요 논의**:

1. **Hook 범위 vs Task 범위 불일치** — SessionStart hook은 "이 터미널 범위: 일꾼 T15. 작업 허용 파일: app/page.tsx;lib/community/*;docs/*"를 안내했으나 사용자 명시 지시는 T08. T08 산출물 경로(`lib/chart/`, `components/Blog/editor/`)는 hook 허용 목록에 없었지만 사용자 지시 우선 원칙으로 진행 → 실제 hook 차단 미발생. (dispatch에서 발사 프롬프트의 hook payload가 다른 일꾼 것이 잘못 주입된 듯)

2. **EditorToolbar tone 전파 — spec vs 안티패턴 충돌** — spec line 122-124("toolbar 분기")와 안티패턴 line 168("BlogEditor의 다른 부분 수정 금지")이 충돌. 해석 B(안티패턴의 "다른 부분"은 TipTap extensions/이미지 업로드 같은 기능 로직) + 절충안 채택 — EditorToolbar에 tone prop 추가하되 wrapper div className만 분기, ToolButton 내부 색상은 minimal diff 원칙으로 미수정. 후속 R2에서 ToolButton 자체에 tone-aware 색상 분기 추가 권장.

3. **lightweight-charts v5 ColorType enum 호환성** — spec의 `background: { type: 'solid' as const }` 코드를 그대로 작성 시 `TS2322: Type '"solid"' is not assignable to type 'ColorType | undefined'` 발생. v5에서 `ColorType`이 string literal union이 아닌 enum으로 강화됨. `import { ColorType }` value import + `type: ColorType.Solid`로 교체. 별도 솔루션 문서로 기록(`docs/solutions/2026-05-23-lightweight-charts-v5-colortype-enum.md`).

4. **BlogEditor 경로 케이스 차이** — spec은 `components/blog/BlogEditor.tsx`, 실제는 `components/Blog/editor/BlogEditor.tsx`(대문자 Blog + editor 하위). Windows case-insensitive라 동일 경로처럼 보이지만 Vercel(Linux)은 case-sensitive — 실제 경로로 작성 + handover 명시.

**검증**:
- `npx tsc --noEmit`: T08 영역(`lib/chart`, `components/Blog/editor`) 에러 0. ColorType 이슈 수정 후 T14/T07 등 다른 일꾼이 기록한 "사전 에러 2건(lib/chart/theme.ts 'solid' 할당)"은 본 세션에서 해소됨
- `grep theme.ts 심볼` = 8 (기준 6+)
- `grep BlogEditor "tone"` = 6 (기준 4+)
- `npx eslint`: T08 파일 3개 에러 0
- `npm run build`: `lib/community/auth.ts`의 bcryptjs 누락(T07 영역)으로 격리 검증 불가 → tsc로 대체

**커밋**: 본 일꾼은 커밋하지 않음 (컨덕터가 R1 일꾼 전체 산출물을 통합 커밋). /cs 5단계에서도 R1 다른 일꾼들과의 정합성 유지를 위해 자동 커밋 보류 권장.

**다음 일꾼/컨덕터에게 메모**:
- T09(signal)·T10(analysis)·T11(market): 기존 차트 컴포넌트(`components/Chart/CryptoChart.tsx`, `components/Chart/StockChart.tsx`, `components/DetailedChart.tsx`, `components/hero-chart.tsx`)에서 차트 옵션 하드코딩을 `getChartTheme('light')` + `getCandleColors('kr')` 호출로 교체. CandlestickSeries는 `addSeries(CandlestickSeries, { ...getCandleColors('kr') })` 형태.
- 블로그 라이트화 일꾼: `<BlogEditor tone="light" />` 또는 default 그대로 사용.

---

### 2026-05-23 (세션 9) — R1/T01 일꾼: community_* 4테이블 마이그레이션

**대화 토픽 흐름**: R1 dispatch 일꾼 T01 발사 → 작업 명세서(T01-community-migrations.md) 정독 → SOT 9개 병렬 read → 산출물 2종(SQL + MD append) 생성 → SQL 키워드 grep 카운트 검증 → tsc 무영향 확인 → handover 작성

**역할**: R1 라운드(mainpage) **단일 일꾼** 세션. 다른 일꾼(T05/T07/T08/T14 등)은 별도 터미널에서 동시 작업 중이며 워킹트리에 산출물 잔존. 본 세션은 T01 산출물만 책임지며 다른 일꾼 파일은 미터치(컨덕터 통합 영역).

**변경 사항 (T01 한정, 3개 파일)**:

신규:
- `supabase/migrations/20260523_create_community_tables.sql` — 4 테이블 + 시드 9행 + 인덱스 6(일반 4 + UNIQUE 부분 2) + 트리거 4 + RLS 15
- `docs/handover/2026-05-23-R1-T01-community-migrations.md` — 일꾼 인수인계

수정 (append-only):
- `docs/references/_SCHEMA_REFERENCE.md` — `## community_*` 섹션 추가 (기존 줄 0건 수정)

**주요 논의/설계**:

1. **익명 XOR 회원 CHECK 제약** — `author_id IS NOT NULL OR (guest_nickname/password_hash/ip_masked 3요소 NOT NULL)`로 DB 레벨에서 강제. 컬럼별 추가 CHECK: `guest_nickname` 2~12자, `guest_password_hash` ≥60자(bcrypt 출력 가정), `guest_ip_masked` regex `^\d+\.\d+\.\*\.\*$`로 "앞 2옥텟만" 형식 강제.

2. **추천/비추 식별 단위 분리** — `community_post_likes`는 회원=`user_id`, 익명=`ip_hash`(sha256 전체 IP, 마스킹 X) 두 축으로 dedup. **작성용 `guest_ip_masked`와 별개** — dedup 정확도가 필요하므로 표시용 마스킹과 분리. `UNIQUE` 부분 인덱스 2종으로 NULL 안전 처리(`WHERE user_id IS NOT NULL` / `WHERE ip_hash IS NOT NULL`).

3. **트리거 채택 vs 비채택** — `updated_at`·`comment_count`·`like_count` 자동 동기화는 채택 (3종 트리거 함수 + 4개 트리거). `is_hot` 자동화는 임계치 미정으로 비채택(배치 스크립트 권장). `like_count`는 비추(-1) 합산이라 **음수 가능** — 화면 단독 표시 시 별도 집계 필요(차후 결정).

4. **RLS 익명 수정/삭제 미허용** — 의도된 설계. 익명 글 UPDATE/DELETE 정책은 `author_id = auth.uid()`만 통과 → 익명은 본 세션 RLS로 못 통과. 서버 라우트(T12 영역)가 비번 검증 후 service_role 키로 처리하는 패턴. INSERT만 전체 허용(CHECK 제약이 강제).

5. **시드 9행 범위** — `free`/`market`/`info` + 코인룸 6종(`coin-btc/eth/xrp/sol/altcoin/kimp`). 게시판 메타만 — 게시글 더미는 T02 영역으로 격리(안티패턴 회피).

6. **다른 일꾼 산출물 격리** — `git status`에 T05(news-classifier), T07(auth-middleware), T08(chart-theme-editor-tone), T14(translations-cleanup) 산출물이 워킹트리에 잔존. 본 일꾼은 자기 3개 파일만 명시적 staging → commit → push. R1 통합은 컨덕터 영역.

**검증**:
- `grep -c "CREATE TABLE"` = 4 (기대 4) ✓
- `grep -c "CREATE INDEX"` = 4 일반 + UNIQUE 부분 2 = 총 6 (기대 ≥4) ✓
- `grep -c "CREATE POLICY"` = 15 (기대 ≥8) ✓
- `grep -c "CREATE TRIGGER"` = 4 ✓
- `npx tsc --noEmit` — 본 산출물(SQL/MD)은 TS 영향 0. 워킹트리의 다른 일꾼 파일(`lib/community/auth.ts`)의 `bcryptjs` 모듈 미설치 에러 1건은 본 작업 무관(T07 영역).
- `psql` dry-run은 로컬 Supabase 미가동으로 명세에 따라 SKIP.

**다음 일꾼에게 메모**:
- T02(게시글 시드): `board_slug` enum 9종 + 익명 IP 형식은 `211.34.*.*` (마스킹 적용된 형태로 INSERT)
- T12(작성 API): `community_post_likes`는 부분 UNIQUE 2종 + `ON CONFLICT ... DO UPDATE` 패턴 권장, value 부호 변경은 트리거가 자동 차분 반영
- T13(코인룸 라우팅): `coin_symbol` NULL = 코인 무관 일반 글. 코인룸 페이지는 `board_slug='coin-btc' OR coin_symbol='BTC'` 양 축 OR 쿼리 권장

---

### 2026-05-23 (세션 8) — R1/T03 일꾼: Binance ticker SSOT + `/api/coins/ticker`

**대화 토픽 흐름**: R1 dispatch 일꾼 T03 발사 → 작업 명세서 정독 → SOT 6개 병렬 read → 산출물 5종 생성 → tsc/eslint 통과 → Binance 응답 정규화 실 호출 검증 → handover 작성

**역할**: R1 라운드(mainpage) **단일 일꾼** 세션. 다른 일꾼(T01/T07/T14 등)은 별도 터미널에서 동시 작업 중. 본 세션은 자체 산출물(T03)만 책임지며, 글로벌 상태(R1 통합)는 컨덕터 영역.

**변경 사항 (T03 한정, 6개 파일, +90/-0)**:

신규:
- `types/coins.ts` — `CoinTicker` / `CoinSnapshot` 타입
- `app/api/coins/ticker/route.ts` — `GET /api/coins/ticker?symbols=...`
- `docs/handover/2026-05-23-R1-T03-ticker-ssot.md` — 일꾼 인수인계

수정 (append-only):
- `lib/supabase/crypto.ts` — `fetchBinanceTickers(symbols)` + `fetchCommunityTickers()` + 60s 메모리 Map 캐시. 기존 `fetchCryptoMarketPrices` 미변경
- `docs/references/_API_REFERENCE.md` — `GET /api/coins/ticker` 섹션 append
- `docs/references/_TYPE_REFERENCE.md` — `CoinTicker / CoinSnapshot` 섹션 append

**주요 논의**:

1. **이중 캐시 정책** — Binance ticker는 호출 비용이 있지만 60s 단위로도 신선도가 충분. Next fetch `revalidate: 60` + 메모리 `Map` 60s 이중. 캐시 키는 `symbols.slice().sort().join(",")`로 정렬 일관성 확보.

2. **base symbol 정규화** — `BTCUSDT` → `baseSymbol: "BTC"`로 자동 분리. `mock-coins.ts`의 코인 키(`btc`/`eth`/...)와 1:1 매핑 가능.

3. **검증 우회** — 작업 명세서의 `npx tsx -e "import('./lib/supabase/crypto')..."` 검증이 path alias `@/`를 tsx ESM 변환이 처리하면서 named export를 `default`로만 노출하는 이슈 발생. Binance API 호출/정규화는 동일 로직의 ESM 스크립트로 우회 검증 → BTC price=75414.25 (>0), baseSymbol=BTC, count=10 모두 PASS. 우회 솔루션은 `docs/solutions/`에 별도 기록.

4. **다른 일꾼 산출물 격리** — `git status`에 T01(community-migrations) / T07(auth-middleware) / T14(translations) 산출물이 워킹 트리에 잔존. 본 일꾼은 자기 6개 파일만 명시적 staging → commit → push. 컨덕터의 R1 통합 시점에 다른 일꾼들 산출물이 일괄 처리될 예정.

**검증**:
- `npx tsc --noEmit` PASS (exit 0)
- `npx eslint types/coins.ts app/api/coins/ticker/route.ts` PASS (exit 0)
- Binance 24h ticker 실 호출 PASS (count=10, BTC price>0)

---

### 2026-05-10 (세션 6+7) — v2.0 커뮤니티 피벗: 방향성 정의 + 디자인 의뢰서 + Stitch 시안 코드 적용

**대화 토픽 흐름**: 프로젝트 점검·git 최신화 → 정체성 피벗 결정(코인판×네이버 커뮤니티) → 의뢰서 6종 작성 → Stitch 시안 수령 확인 → 적용 계획 수립(Plan Mode) → Step 1~3 코드 구현 → 빌드 검증 → 커밋·푸시

**변경 사항** (56개 파일, +9,917 / −656):

신규:
- `docs/PROJECT_DIRECTION.md` — v2.0 방향성 정의 (코인판×네이버 커뮤니티 피벗)
- `docs/design-brief/00-overview.md` ~ `05-coin-room.md` + `README.md` — Stitch 의뢰서 7종
- `docs/design-brief/stitch_attachment_file_checker/` — Stitch 반환 시안 (HTML 6 + 스크린샷 6 + DESIGN.md)
- `app/board/[slug]/page.tsx`, `[postId]/page.tsx`, `write/page.tsx` — 게시판 3페이지
- `app/coin/[symbol]/page.tsx` — 코인룸 페이지
- `components/community/` 13개 (Badge, Tabs, Pagination, SidebarWidget, BoardRow, NewsRow, NewsHeadlineCard, CoinHero, 위젯 5종)
- `lib/community/mock-{posts,news,coins}.ts` — 더미 데이터 모듈

수정:
- `app/globals.css` — Material 3 토큰 + 한국식 빨/파 시맨틱 + Noto Sans KR 폰트 변수 (전면 교체)
- `app/layout.tsx` — Geist → Noto Sans KR 교체, lang ko, 라이트 톤 body
- `app/page.tsx` — 홈 전면 리디자인 (시세 마퀴 + 베스트30 + 뉴스10 + 게시판 3컬럼 + 코인룸 6카드)
- `app/news/page.tsx` — 4차원 필터 + 헤드라인 3분할 + 표 형식 리디자인
- `components/global-header.tsx` — 메뉴 5+2 구조 (베스트/자유/시세/정보/뉴스 + 코인룸▼/도구▼) + 글쓰기 CTA
- `components/footer-section.tsx` — 라이트 톤 + 커뮤니티/정보 그룹
- `CLAUDE.md` — 풀뿌리 트리에 PROJECT_DIRECTION.md, design-brief/ 등록
- `docs/status/current.md` — 세션 6, 7 진행 상태 반영

**주요 논의**:

1. **정체성 피벗 (세션 6)** — 사용자 요청: "코인판 사이트처럼 정보공유의 장을 만들고 싶다." → 4가지 핵심 결정:
   - 정보 공유 주체: **유저 커뮤니티** (코인판식)
   - AI 차트 분석: **부가 기능**으로 격리 ("도구" 메뉴), URL은 그대로
   - 뉴스 분류: **AI API 미사용**, 룰베이스 키워드 사전 + 점수
   - 디자인 톤: **네이버 스타일** (흰 배경, 정보 밀도, 표 위주)

2. **권한 모델·MVP 범위** — 익명+회원 혼용 (코인판식 IP 앞 2옥텟 마스킹), MVP는 4개 페이지(메인/게시판/상세+작성/뉴스+코인룸)

3. **Stitch 시안 분석 (세션 7 진입 시)** — 시안 HTML 직접 검사로 정확 매핑:
   - `_4`=홈, `_3`=게시판 리스트, `_2`=게시글 상세, `_5`=작성, `_1`=뉴스, `btc`=코인룸
   - Material 3 토큰 (Primary `#0050CB` + Secondary `#006E2E`) + Noto Sans KR
   - 시안 미국식 색상(녹=호재, 빨=악재) → 한국식(빨=호재, 파=악재)으로 강제 적용

4. **3가지 핵심 결정 (Plan Mode)**:
   - 컬러: 시안대로 블루+그린 (Stitch 결정 채택)
   - 라이트/다크: **전체 사이트 라이트 통일** (다크 프리미엄 톤 폐기)
   - 1차 구현 범위: 디자인 토큰 + 공통 컴포넌트 + 더미 페이지 5개 (DB는 다음 세션)

5. **단계별 실행 (Step 1~3)** — Tailwind v4 `@theme` 디렉티브로 토큰 정의, 시안 HTML의 `text-primary` `bg-surface-container` 등 클래스를 그대로 사용 가능하게 매핑. BlogEditor(TipTap) 작성 페이지에서 재사용.

6. **블로커 처리** — 빌드 전 `node_modules` 일부 누락(@tiptap, @radix-ui 등) 발견 → `npm install`로 208 패키지 추가 → tsc·next build 통과

**검증 결과**:
- TypeScript (`npx tsc --noEmit`) — 통과
- Next.js 빌드 — 41개 라우트 정상 컴파일 (Turbopack)
- 신규 코드 lint 에러 0 (기존 `scripts/` any 타입 경고는 알려진 항목)
