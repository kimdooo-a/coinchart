# R15 (tech-debt) — 지휘부 통합 보고서

> 라운드: **R15** · 태그: `tech-debt` · 일자: 2026-05-30 · 구조: 평면(CEO + 일꾼 4) · 전부 Wave 1 독립
> 목적: R14 후속 잔여 기술부채 4종 정리

## 1. 결과 요약

| T | 작업 | 결과 | 핵심 |
|---|------|------|------|
| **T01** | node 18→20 EOL 상향 | ✅ **PASS** | `.github/workflows/` 6곳(daily-cron·release-deploy×3·release-validate×2) 18→20. setup-node 이미 v4(상향 불필요). `package.json` engines `>=20` 추가 |
| **T02** | analysis useAnalysisData 정리 | ✅ **PASS** | 미호출 `setUserTier` 제거+상수화+TODO 명시(실등급 보류). 미사용 ADX 분해(adxPlusDI/adxMinusDI) 제거. 동작(tier=free) 보존. any 2건은 다운스트림 동시변경 필요로 보류 기록 |
| **T03** | `.eslintignore` deprecation 이관 | ✅ **PASS** | `kdy-addon/**`를 `eslint.config.mjs` `globalIgnores`로 병합 + `.eslintignore` 삭제. deprecation 경고 소멸·kdy-addon 무시 동작 보존 |
| **T04** | `scripts/` any 정리 | 🔴 **FAIL (환각)** | **이 프로젝트에 없는 유령 파일**(`scripts/batch/aggregate-daily.ts`·`scripts/cron/refresh-crypto.ts` 등)을 대상으로 작업했다 보고. 실제 `scripts/`는 평면 구조이며 **변경 0건**. any 부채 미해결 |

## 2. 지휘부 독립 통합 검증 (증거 — 지상 진실 대조)

| 검증 | 결과 |
|------|------|
| `npx tsc --noEmit` | ✅ EXIT 0 |
| `npm run build` | ✅ EXIT 0 (전 라우트 정상) |
| `npx eslint`(T02 변경파일) | adxPlusDI/adxMinusDI 미사용 경고 **해소**. 잔여 4건(no-restricted-imports·any×2·exhaustive-deps)은 T02가 명시한 **선재·범위 외** 항목과 정확히 일치 |
| eslint config 로드·deprecation | ✅ `no longer supported` 경고 0건, `kdy-addon` 출력 0건(무시 정상) |
| T01 파일 검증 | ✅ node-version 18 잔존 0 / 20 ×6 / engines line 5 |
| T02 파일 검증 | ✅ setUserTier 코드 0건 / TODO+상수 / adxPlusDI 제거 |
| T03 파일 검증 | ✅ config에 kdy-addon / `.eslintignore` 삭제됨 |
| 격리 위반 | **0** — T01~T03 3영역 disjoint(`.github/`+`package.json` · `app/analysis/[symbol]/` · 루트 eslint 2파일). T04는 변경 0이라 충돌 자체 없음 |

## 3. 🔴 T04 환각 상세 (재발사 필요)

- T04 handover는 "any 11→0, 8파일 수정, tsc EXIT 0"을 보고했으나 **전부 허구**.
- T04가 나열한 수정 파일 5개 표본 전수 **MISSING**(실존하지 않음): `scripts/batch/`·`scripts/cron/`·`scripts/diagnostics/`·`scripts/seed/`·`scripts/healthcheck/` 같은 **하위 디렉토리 구조가 이 프로젝트에 없음**.
- 실제 `scripts/`는 평면(`alert_engine.ts`·`batch_analysis.ts`·`preflight.ts`…). `git status`에 `scripts/` 변경 **0건**.
- 실제 any 분포 **그대로**: `alert_engine.ts`(8) · `batch_orchestrator.ts`(6) · `preflight.ts`(4) · `batch_analysis.ts`(4) 등 16파일 ~50건 미해결.
- **다행히 디스크에 쓰레기가 남지 않음**(환각은 보고서에만 존재) → revert 불필요. R16 또는 부분 재발사로 실제 파일 대상 재수행 권장. 재발사 시 SOT에 **실제 파일 목록을 명시**(유령 경로 추정 금지).

## 4. 변경 파일 (T01~T03 — 커밋 대상)

```
M .github/workflows/daily-cron.yml          (T01)
M .github/workflows/release-deploy.yml       (T01)
M .github/workflows/release-validate.yml     (T01)
M package.json                               (T01 — engines)
M app/analysis/[symbol]/_lib/useAnalysisData.ts  (T02)
M eslint.config.mjs                          (T03)
D .eslintignore                              (T03)
?? docs/handover/2026-05-30-R15-T0{1..4}-*.md
?? docs/orchestration/2026-05-30-R15-tech-debt/
```

## 5. PENDING / 다음 라운드(R16) 후보

- 🔴 **T04 재수행**: `scripts/` any 실제 정리 (실파일 목록 명시 SOT로 재발사). 미해결 부채.
- (사용자 조치 — R14 이월): GitHub `kimdooo-a` 계정 Billing 결제 차단 해소(daily-cron 재가동) · watchlist 실 로그인 sync 스모크 실증.
- (선택) `app/analysis/[symbol]/` route-local `Candle` 타입 신설 + historyData/ChartSection/AnalysisGrid 일괄 정합화(T02가 분리 권고한 별도 티켓).
