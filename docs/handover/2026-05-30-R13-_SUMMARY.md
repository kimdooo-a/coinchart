# R13 통합 보고서 — display-rollout (지휘자)

> 라운드: R13 · 태그: display-rollout · 2026-05-30 · CEO 세션
> 선행: R12 watchlist/settings 마감(`2d6593e`). 본 라운드는 R12 후속 4종 + 배포.
> 인덱스: `docs/orchestration/2026-05-30-R13-display-rollout/_INDEX.md`

## 개요

R12에서 구축한 표시설정 Context(`DisplaySettingsProvider` 루트 마운트)·`useWatchlist`·watchlist API·AuthButton 기반 위에, **전역 시세 표시 통일 + watchlist 후속 + 계정 드롭다운**을 4 외부 터미널 Wave1 동시 발사로 분산 처리하고 회수·통합했다. 통합 검증 중 **R13 문서가 유발한 Tailwind content 오염(dev 500)을 규명·핫픽스**했다.

## 매트릭스 & 결과

| 코드 | 작업 | 결과 | 격리 |
|------|------|------|------|
| T-A1 | 코인룸·사이드바 시세 구독 | ✅ CoinHero(서버→클라)·PriceTickerWidget 전환. BoardSidebar 무변경(정당) | `components/community/` |
| T-A2 | 티커·김프 시세 구독 | ✅ Ticker·StockTicker 전환. KimchiPremium 무변경(단위고정·구간의미색 정당) | `components/Chart/`·`components/Market/` |
| T-B | watchlist 후속 | ✅ notice 배너·reorder `PATCH`·clear `DELETE?all`·SSOT 확장 | `components/Watchlist/`·`hooks/`·`api/watchlist/` + `lib/supabase/watchlist.ts`(정당 일탈) |
| T-C | AuthButton 계정 드롭다운 | ✅ AccountMenu 신규·AuthButton 109→60줄·접근성 완비 | `components/AuthButton.tsx`·`components/account/` |
| 배포 | Release 게이트 | (지휘자 Wave2) | — |

## 지휘자 독립 검증

- 통합 `npx tsc --noEmit` → **exit 0**
- 통합 `npm run build` → **exit 0**, `● /coin/[symbol]` SSG·`○ /watchlist`·`○ /settings`·`ƒ /api/watchlist` 정상
- 통합 `npx eslint`(변경 11파일) → **error 0**
- 파일 중복 수정 **0** (disjoint)
- 격리: T-B `lib/supabase/watchlist.ts` 일탈은 지휘자 SOT의 "RLS 우회 금지+SSOT 경유" 지시 결과 — watchlist 단일 도메인 SSOT 확장(무충돌). allowed_dirs 누락은 SOT 작성 측 미스로 인정.

## 🔧 dev 500 블로커 — 규명 + 핫픽스 (지휘자 단독)

- **증상**(T-B 보고): dev 모드 전 페이지 500, Turbopack CSS 파서 `Unexpected token Delim('*')`.
- **T-B 진단(오진)**: `app/globals.css:3960` `var(--color-kr-*)` 와일드카드. → **소스 globals.css(274줄)에 해당 패턴 없음**, R13 미변경 확인.
- **실제 원인(지휘자 규명)**: R13 문서(`docs/orchestration/.../T-A1-*.md`·`docs/handover/.../R13-T-A1·T-B-*.md`)에 적힌 `text-[var(--color-kr-*)]` 문자열을 **Tailwind v4 자동 content 감지가 docs/까지 스캔**해 클래스 후보로 추출 → `.text-[var(--color-kr-*)]{ color: var(--color-kr-*) }` 불량 유틸 생성 → dev CSS 파서 붕괴. 프로덕션 빌드는 경고로만(통과).
- **핫픽스**: `app/globals.css`에 `@source not "../docs";` 추가(Tailwind v4 negative source) — docs 문서를 클래스 추출에서 제외.
- **검증**: 재빌드 CSS 경고 **0**, dev 서버 `/`·`/watchlist`·`/coin/btc`·`/settings` 전부 **HTTP 200**, CSS 파서 에러 0.
- **의의**: 워커 단독 검증으론 잡히지 않고(각자 영역 내 PASS), 지휘자 통합에서만 포착·정정된 사례.

## 수정/신규 파일 (지휘자 통합 포함)

**신규**: `components/account/AccountMenu.tsx`·`components/Watchlist/WatchlistNotice.tsx`
**수정(일꾼)**: `components/community/CoinHero.tsx`·`components/community/widgets/PriceTickerWidget.tsx`·`components/Chart/Ticker.tsx`·`components/Chart/StockTicker.tsx`·`components/Watchlist/WatchlistView.tsx`·`components/hooks/useWatchlist.ts`·`app/api/watchlist/route.ts`·`lib/supabase/watchlist.ts`·`components/AuthButton.tsx`
**수정(지휘자)**: `app/globals.css`(핫픽스)·`docs/references/_API_REFERENCE.md`·`docs/references/_COMPONENT_MAP.md`·checkpoint

## 신규 API (R13)
- `PATCH /api/watchlist` — `{ order:[{assetType,symbol,sortOrder}] }`(≤500) → `200 {ok,updated}` (reorder 영속화)
- `DELETE /api/watchlist?all=true` — → `200 {ok,cleared}` (벌크 clear)

## 알려진 이슈 / 후속
- **watchlist 회원 sync 런타임**: 신규 API 401 가드만 검증됨. 실 OAuth 로그인 전체 경로(sync→DB→reorder/clear 반영→타세션 복원)는 미검증 — T-B handover 수동 스모크 절차서 참조(후속 1회 권장).
- **시세 구독 범위 밖(의도)**: FngGaugeWidget·HotIssueWidget(심리/트렌드 색)·KimchiPremium(구간 의미색)·StockTicker 다통화(한국주식 확장 시 `currency` 필드 필요).
- `/stock-market` nav 진입점 소실 잔존(Minor).

## 다음(Wave2): 배포
- Release 게이트로 R10~R13 누적분 프로덕션 반영.
