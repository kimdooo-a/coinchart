# R9 통합 보고서 — tree-reconcile (정합·정리)

> 작성일: 2026-05-29 · 지휘부(CEO) 세션
> 라운드: R9 (tree-reconcile) · 평면 3터미널 · 이전: R8(세션 34)
> 인덱스: [_INDEX.md](../orchestration/2026-05-29-R9-tree-reconcile/_INDEX.md)

## 목적

R8 cs가 인계한 R9 후보 #1(홈 컴포넌트 트리 재감사 + 레퍼런스 정합)을 중심으로 `/history` 메뉴 정합을 묶어 **정합·정리**로 완결. 신규 기능 없음. (807줄 리팩토링·Giscus App 설치·`.env.local` BOM[이미 해소]은 범위 밖.)

## 결과 — 3/3 완료·검증 PASS

| T | 작업 | 산출 | 검증 |
|---|------|------|------|
| **T01** | 홈 트리 재감사 + dead 삭제 | 홈 트리 9종 확정 + 닫힌 dead 트리 3종 `git rm`(`hero-section`·`hero-chart`·`news-rotator`) + 보존 2종(`ErrorState`·`InsufficientData` 재사용 프리미티브) | tsc 0·build green·잔존 import 0·footer 보존 |
| **T02** | `/history` 메뉴 정합 | 방안 A — 도구 드롭다운에 `/history`("코인 역사") 1줄 추가(7→8항목). `/history`는 실데이터 완성 페이지(`lib/history-data.ts`), 진입 경로만 누락이었음 | tsc 0·build green·라우트 54 유지·`○ /history` |
| **T03** | 레퍼런스 전수 정합 | `_COMPONENT_MAP`·`_WEB_CONTRACT` 정합 — dead 3종 제거·홈 트리 9종 갱신·`InvestmentQuotes` 홈 stale 제거·GNB v2.0 실구조(primary 5/coinRoom 6/tools 8) 재작성·계약 v3→v4 | docs 전용·교차 검증 5종 PASS·격리 2파일 |

## 지휘부 독립 검증 (verification-before-completion)

```
[1] grep dead 3종 (hero-section|hero-chart|news-rotator) in app/components/lib (.ts/.tsx) → 0건 (OK)
[2] global-header /history 추가 → L72 { label: t.menu.history, href: "/history" } (OK)
[3] git diff --stat → global-header(+1)·hero-chart(-148)·hero-section(-88)·news-rotator(-107)·_COMPONENT_MAP·_WEB_CONTRACT (6파일, 격리 정확)
[4] 삭제 3종 파일 부재 (OK) / [5] footer-section 보존 (OK)
[6] npx tsc --noEmit → exit 0
[7] npm run build → exit 0 (green, ○ /history 정적, 54 라우트)
```

## 검증 메트릭
- 완료율: **3/3 (100%)**
- 자가 검증 PASS율: **3/3** (지휘부 독립 재확인)
- 안티패턴 위반: **0건** (쓰기 영역 매트릭스 정확 일치 — T01 components 루트 dead만·T02 global-header만·T03 docs/references만)
- 시각/런타임 회귀: 없음 (dead 삭제 + 메뉴 1줄 + 문서 — 빌드 green·라우트 불변)

## 잔여 stale (T03 플래그 → 다음 라운드 후보)
T03이 확정 입력(홈 트리+`/history`) 밖이라 **추측 금지** 원칙으로 미수정·플래그만:
1. `_WEB_CONTRACT` 라우트 레지스트리에 v2.0 커뮤니티 라우트(`/board/[slug]`·`/board/[slug]/write`·`/board/[slug]/[postId]`·`/coin/[symbol]` 6종) **미등록** — 레지스트리 30행 ↔ 빌드 54 라우트 괴리
2. §8 연결성 검증 카운트 stale("23개/19개", 최종 2026-02-20)
3. 라우트 레지스트리 진입점 컬럼이 과거 4그룹 기준 — `/blog`(GNB·Footer 진입점 없음, 고아 가능성)·`/contact`·`/terms`·`/settings`
4. Footer 정합 미검증(`footer-section.tsx` 실내용 본 라운드 미열람 — §3-3 Footer 레지스트리 실코드 대조 권장)

## 커밋 후보
- 변경 6파일: `components/global-header.tsx`(+1) · `components/{hero-section,hero-chart,news-rotator}.tsx`(삭제) · `docs/references/{_COMPONENT_MAP,_WEB_CONTRACT}.md` + handover/orchestration 산출물
- 제안 메시지: `feat: 세션 35 — R9 마감: 홈 dead 트리 3종 삭제 + /history 메뉴 정합 + 레퍼런스 정합`
