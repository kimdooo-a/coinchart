# R16 (type-cleanup) — 지휘부 통합 보고서

> 라운드: **R16** · 태그: `type-cleanup` · 일자: 2026-06-02 · 구조: 평면(CEO + 일꾼 3) · 전부 Wave 1 독립
> 목적: **R15-T04 환각 FAIL 재수행**(scripts any 실제 정리) + analysis 라우트 Candle 타입 정합

## 1. 결과 요약

| T | 작업 | 결과 | 핵심 |
|---|------|------|------|
| **T01** | scripts any 엔진류(4파일/22건) | ✅ **PASS** | alert_engine 8·batch_orchestrator 6·batch_analysis 4·preflight 4 → **21건 타입화, 1건 정직 보류**. catch `any`→`unknown`+narrowing, 로컬 `AlertSnapshot` 도입. 동작 보존 |
| **T02** | scripts any 보조류(12파일/23건) | ✅ **PASS** | **23건 전건 타입화, 보류 0**. 로컬 `YahooCandle`/`YahooPriceRow`/`BinanceKline` 선언 + 기존 `MarketRegime`·`ConfidenceGrade`·`CandleData` import 재사용. catch `unknown`+`instanceof Error` |
| **T03** | analysis Candle 타입(5건) | ✅ **PASS** | `historyData: any[]` 5건 → route-local `Candle`(time:string+OHLCV) 신설·일괄 정합. 기존 캔들 타입(`time:number`)과 shape 불일치라 신설이 정답. **5→0** |

## 2. 지휘부 독립 통합 검증 (증거)

| 검증 | 결과 |
|------|------|
| `npx tsc --noEmit` | ✅ **EXIT 0** |
| `npm run build` | ✅ **EXIT 0** (전 라우트 정상, `ƒ /analysis/[symbol]` 유지) |
| eslint(변경 21파일) | ✅ 베이스라인 **58 problems(55 err) → 7 problems(4 err)**. 감소 51건=any 해소. **신규 위반 0**(잔여 7건 전부 pre-existing: no-restricted-imports×2[R11 분해 orchestrator import]·prefer-const·no-require-imports·unused-vars×2·exhaustive-deps) |
| **환각 검증** | ✅ **재발 없음** — 디스크 실제 변경 21파일 존재, 각 handover의 `git diff --stat`이 실제와 일치. 착수 전 게이트(`ls scripts/`=fixtures·smoke만) 3 일꾼 모두 통과 보고 |
| 격리 위반 | ✅ **0** — T01(alert_engine·batch_orchestrator·batch_analysis·preflight) ∩ T02(나머지 12) = ∅, T03은 `app/analysis/[symbol]/`만. 새 공유 타입 파일 신설 0(전부 로컬/import) |
| scripts any | ✅ 45 → **1**(batch_analysis `result` 보류) |
| analysis any | ✅ 5 → **0** |

## 3. T01·T02 상충 보고 해소

- T02 handover가 "글로벌 tsc EXIT 2, alert_engine.ts possibly-undefined 12건"을 보고했으나, 지휘부 최종 `tsc --noEmit`는 **EXIT 0**.
- 원인: 공유 작업트리에서 T02가 tsc 실행한 시점이 **T01의 alert_engine.ts 작업 중간 스냅샷**(옵셔널 체이닝/`?? 0` 정리 전)이었음. T01이 마감하여 최종 EXIT 0 달성. **상충 아님, 타이밍 차**.

## 4. 보류 1건 (R17 후보 — 정직 기록)

- **`scripts/batch_analysis.ts` `AnalysisRecord.result?: any`**: union(`AnalysisResult | StockAnalysisResult`)으로 좁히면 **T02 영역 `report_generator.ts`**가 union에 없는 `.signals`를 접근해 tsc 에러 3건 발생. 격리(타 영역 수정 금지)+동작보존 원칙상 보류 + eslint-disable + 사유 주석. 후속: report_generator 소비처 정리와 **동시 변경**해야 하는 교차 파일 결합 사안 → R17 후보.

## 5. 변경 파일 (통합 커밋 대상)

```
scripts/ 16파일 (T01 4 + T02 12)  — any 45→1
app/analysis/[symbol]/ 5파일 (T03) — any 5→0, _lib/types.ts에 Candle 추가
```

## 6. PENDING / 다음 라운드(R17) 후보

- (R17 후보) `batch_analysis.result` + `report_generator.signals` 교차 결합 타입화(동시 변경 필요).
- (사용자 조치 — R14부터 이월): GitHub `kimdooo-a` 계정 Billing 결제 차단 해소(daily-cron 재가동) · watchlist 실 로그인 sync 스모크 실증(`docs/db/R14-watchlist-sync-smoke.md`).
