# R2 라운드 통합 보고서 — realdata-finish

> 지휘자(CEO) Phase 4 통합 검증. 작성: 2026-05-23 (R2 회수 세션).
> 주제: R1 잔여 과제 소진 — board/news/coin 실데이터 전환 + 차트 라이트화 + 인프라 마무리.

## 라운드 메타

| 항목 | 값 |
|---|---|
| 라운드 | R2 (realdata-finish) |
| 터미널 수 | 5 (평면, 동시 발사) |
| **완료율** | **5 / 5 (100%)** ✅ |
| 검증 | `tsc --noEmit` 0 에러 · `npm run build` Compiled successfully |
| 격리 위반 | 1건 (BoardSidebar.tsx, 신규·무충돌, 허용) |

## 일꾼별 최종 상태

| ID | 작업 | handover | 산출물 검증 | 커밋 |
|---|---|---|---|---|
| R2-T01 | board-realdata | ✅ | `lib/community/board-queries.ts`(446줄, fetch 10) + 게시판 3페이지 T12 API 전환 + `BoardSidebar.tsx`(공용). mock import=BOARD_META/BoardSlug(정적)만 | 지휘자 통합 |
| R2-T02 | news-realdata | ✅ | `news-queries.ts`(248줄, fetch 5) + `/api/news` 4차원 필터 4호출. mock import=NEWS_CATEGORIES/COIN_FILTERS(라벨)만 | 지휘자 통합 |
| R2-T03 | coin-realdata | ✅ | `coin-queries.ts`(444줄, fetch 6) + 코인룸 시세/게시글/뉴스 전환. mock import **0** | 지휘자 통합 |
| R2-T04 | chart-lightify | ✅ | 차트 4종(`CryptoChart`/`StockChart`/`DetailedChart`/`hero-chart`) `getChartTheme`/`getCandleColors` 적용, 하드코딩 다크 0 | cs `1ae0cd6` |
| R2-T05 | infra-finish | ✅ | `/` 정적 ISR 전환(ƒ→○), middleware `runtime='nodejs'`(node:crypto 경고 0), Giscus `light` | cs `8fcadb3` |

## 데이터 흐름 패턴 (R2 확립)

- board/news/coin은 `"use client"` 유지 + **클라이언트 `fetch()`** 로 기존 API 라우트 호출 (`*-queries.ts` 모듈에 fetch+매퍼 격리, server-only import 0).
- 사이드바 위젯(ticker/hot/fng/공식글)은 `/api/coins/ticker`·`/api/coins/hot-issues`·`/api/fng`·`/api/blog` 공통 fetch. `BoardSidebar.tsx`로 공용화.
- 메인 `/`(T05)만 SSR + cookies 비의존 anon 클라이언트로 정적 ISR.

## 격리 분석

- 위반 1건: `components/community/BoardSidebar.tsx` — R2-T01이 자기 allowed_dirs(`app/board/`, `lib/community/board-queries.ts`) 밖에 신규 생성. write-guard 소프트(env 비전파)라 차단되지 않음. **신규 파일·충돌 없음·재사용 합리적** → 허용, 통합 커밋 포함. R3 발사 시 사이드바 공용 컴포넌트도 allowed_dirs에 명시 권고.

## mock-* 정리 상태 → R3 이월

- 잔여 mock import: **정적 메타/타입뿐** (데이터 배열·getter·`mock-coins` 전부 unused). 빌드 무해.
- R3 계획: `mock-coins.ts` 삭제 + 정적 메타 별도 파일 이전 후 `mock-posts`/`mock-news` 데이터부 삭제.

## R3 후보

1. **mock-* 완전 정리** (위)
2. **관리자 게시판 라우트** (`is_notice` 생성 admin 전용) — T12 §6
3. **댓글 추천 토글** + **dislikeCount 분리 RPC** — T12 §6
4. **추천 dedup 회원 전이 케이스** 정책 — T12 §6
5. (선택) board/news/coin SSR 전환 검토 (현재 client fetch — SEO 강화 시)
