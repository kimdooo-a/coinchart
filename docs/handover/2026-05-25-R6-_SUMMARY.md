# R6-polish 통합 보고서 (지휘부)

> 작성일: 2026-05-25 · 지휘부(CEO) 세션 31
> orchestration: [2026-05-25-R6-polish](../orchestration/2026-05-25-R6-polish/_INDEX.md)
> 이전 라운드: R5(단독 세션) [session30-r5](./2026-05-25-session30-r5.md) · R4 [R4_SUMMARY](./2026-05-25-R4-_SUMMARY.md)

## 개요

세션 30(R5) cs가 인계한 **R6 후보 5건**(정합·정리·일관성)을 평면 5터미널로 분산. **외부 일꾼 터미널들이 지휘부 발사 프롬프트와 무관하게 선행 완료**한 상태로 회수·통합·검증·핫픽스를 수행했다.

- Wave1(T01~T04 독립) 통합 커밋 `94ff3a5` → AD1 활성화 중 라이브 버그 적발·핫픽스 `a1983a3` → Wave2(T05) 통합.
- 전 구간 tsc 0 / build green. 라이브 운영 DB 직접 쓰기는 AD1 검증(자체 데이터 자기정리, 잔여 0)뿐.

## 터미널별 결과

| T | 작업 | 결과 | 핵심 |
|---|------|------|------|
| **T01** | 관리자 AD1 storageState | ✅ 활성화·통과 | OAuth 전용 관리자 → `@supabase/ssr` 프로그래밍 로그인. config projects 3분리. **AD1이 `/admin/board` 라이브 버그 적발**(아래 핫픽스) |
| **T02** | 마이그 파일명 14자리 정규화 | ✅ 완결 | 14개 `git mv` 정규화 + `config.toml` + `backfill_schema_migrations.sql`. **운영 DB 미적용(코드만)** |
| **T03** | queries.ts SSOT 환원 | ✅ 완결 | `fetchMainPageData` anon 단일화 + page.tsx `loadMainPageData` 제거. `/` 정적 ISR ○ 유지 |
| **T04** | 차트 방향색 KR 정렬 | ✅ 완결 | 볼륨·MACD 히스토그램 녹↑/빨↓ → 빨↑/파↓(theme.ts 헬퍼). 대상은 Crypto·Stock 2종(Detailed·hero는 히스토그램 없음) |
| **T05** | 토큰계 통일 | ✅ 완결 | `muted-foreground` → `on-surface-variant` 137건/29파일. `components/ui/` 원본·globals.css 별칭 보존. 시각 회귀 0 |

## 핫픽스 — AD1이 적발한 라이브 버그 (`a1983a3`)

- **증상**: 관리자 로그인 상태로 `/admin/board` 진입 시 TipTap `"SSR detected"` throw → 페이지 전체 클라 예외 크래시.
- **원인**: `BlogEditor`의 `useEditor`에 `immediatelyRender: false` 누락. Next `dynamic(ssr:false)` 마운트에서도 첫 렌더 throw.
- **미발견 경위**: S-AD1(비로그인 리다이렉트)은 이 페이지를 렌더하지 않아 여태 안 잡힘 → AD1이 처음 관리자 상태로 렌더하며 적발. **blog 작성/편집(`/admin/blog/*`)에도 잠재했던 동일 버그 동시 해소**.
- **수정**: `BlogEditor.tsx` 1줄(`immediatelyRender: false`, TipTap 공식 SSR 권장). tsc 0·build green·blog 회귀 0.

## 라이브 환경 처리 (사용자 고지: R1~R5 실제 배포됨)

- **배포 모델**: 프로덕션 = GitHub Release(`release: published`) 게이트. main push만으로 자동 배포 안 됨. 마이그 자동 적용 없음 → T02 코드 정규화는 프로덕션 DB 영향 0.
- **AD1 라이브 DB 쓰기**: §2-1 안전장치(`afterAll` 정리·`E2E 공지대상` 자체 생성·잔여 0 증거). 검증 후 **라이브 잔여 0** 확인.
- **관리자 임시 비번**: 지휘부가 service_role로 주입해 AD1 검증 → 완료 후 **무력화**(새 랜덤 재설정 후 값 폐기, 비번 로그인 불가/OAuth 유지). `e2e/.auth/` 전량 폐기. 라이브 인증 원복.

## 검증

- `npx tsc --noEmit` — 0 (Wave1·핫픽스·T05 각 단계)
- `npm run build` — green (54 라우트, `/` ○ ISR·`/coin/[symbol]` ● 6종·board/news ƒ 모드 회귀 없음)
- AD1 — `2 passed`(setup+AD1), 운영 DB 잔여 0
- T05 — muted-foreground(ui/ 제외) 0건, globals.css 별칭 1건 유지

## 커밋

| 커밋 | 내용 |
|------|------|
| `94ff3a5` | R6 Wave1 통합 (T01~T04) |
| `a1983a3` | BlogEditor immediatelyRender 핫픽스 (AD1 적발) |
| (통합 커밋) | T05 토큰통일 + cs 문서 + 체크포인트 + 마커 archive |

## 미해결 / 다음 라운드(R7) 후보

1. **마이그 정합 마무리(운영 DB)**: T02가 코드(파일명 14자리·config.toml·backfill SQL)까지만 — 사용자가 `supabase link` + `db push --dry-run` + backfill 실행으로 정합 완료(런북 §9-4).
2. **AD1 CI 통합**: 현재 비번 무력화로 로컬 재실행 시 비번 재주입 필요. generateLink(magiclink) fallback을 `auth.setup`에 구현하면 비번 영구설정 없이 CI에서 AD1 자동화 가능(T01 handover §6 대안).
3. **차트 라인/오버레이 색**: T04는 히스토그램 방향색만. RSI/MACD/MA/BB 라인·CSS 오버레이는 범위 밖 보존(후속 정렬 검토).
4. **토큰계 잔여**: `bg-`/`border-` 변형은 사용처 거의 없었으나(T05는 text- 중심) 전수 점검 1회 가치.

---
[← handover/_index.md](./_index.md)
