# R12 / T-D — nav 진입점 (handover)

> 작성일: 2026-05-30
> 일꾼: T-D (4명 중 1) · 역할: 헤더 nav 진입점 연결
> 상태: ✅ 완료 (cs 금지 — 본 handover만)

---

## 0. 요약

settings 진입점 **2건**(익명 도구▼ + 회원 계정영역)을 신규 연결하고, watchlist 진입점이 이미 실경로로 연결돼 있음을 확인했다. 헤더 전체 링크 죽은 링크 0 검증. tsc PASS.

---

## 1. ⚠️ 명세 가정과 실제 코드베이스 불일치 (지휘관 인지 필요)

T-D 명세(`docs/orchestration/2026-05-29-R12-watchlist-settings/T-D-nav.md`)는 다음을 가정했으나 실제와 달랐다:

| 항목 | 명세 가정 | 실제 |
|------|----------|------|
| 헤더 위치 | `components/Common/` 내 | `components/global-header.tsx` (layout.tsx가 직접 import) |
| 회원 영역 | "계정 드롭다운" 존재 | `components/AuthButton.tsx` — 드롭다운 없음(아바타+로그아웃 아이콘 버튼 그룹) |
| `/watchlist` 진입점 | "준비 중" 스텁 교체 필요 | **이미 도구▼ 드롭다운에 실경로 연결됨** (데스크톱+모바일) |
| `/settings` 진입점 | 둘 다 추가 | 헤더 어디에도 없음 (단 `t.menu.settings` translations 키는 이미 존재) |
| 헤더 내 "준비 중"/죽은 링크 | 잔존 가정 | 없음 (전 링크 실페이지) |

→ 쓰기 허용 영역(`components/Common/`만)에 실제 헤더가 없어 명세대로 수행 불가했고, **지휘관이 쓰기 영역 확장(`components/global-header.tsx` + `components/AuthButton.tsx` 직접 수정)을 승인**하여 진행함. PreToolUse hook 차단은 발생하지 않음.

---

## 2. 추가/교체한 진입점 목록 (레퍼런스 갱신용 — `_WEB_CONTRACT.md`는 지휘관 통합 시 일괄)

### 신규 추가

| # | 위치 | 파일 | 대상 | 노출 대상 |
|---|------|------|------|----------|
| 1 | 도구 ▼ 드롭다운 (데스크톱+모바일) | `components/global-header.tsx` (tools.items 말미) | `/settings` (`t.menu.settings` = "설정"/"Settings") | 익명·회원 공통 |
| 2 | 회원 계정 영역 (AuthButton, 로그인 시) | `components/AuthButton.tsx` (portfolio 아바타 앞 ⚙️ 아이콘 링크) | `/settings` | 회원 전용 |

> taste #5(settings 진입점 = 둘 다: 익명=도구▼ / 회원=계정 영역) 충족.

### 기존 확인(변경 없음)

| 위치 | 대상 | 비고 |
|------|------|------|
| 도구 ▼ 드롭다운 (데스크톱+모바일) | `/watchlist` (`t.menu.watchlist` = "관심종목") | 선행 라운드에서 이미 실경로 연결됨. 익명·회원 공통(명세 3-2 충족). 추가 작업 불필요 |

---

## 3. 변경 파일 (격리 확인)

```
components/global-header.tsx   | +1   (tools.items 에 settings 1줄 추가)
components/AuthButton.tsx      | +12  (회원 ⚙️ 설정 링크 블록)
```

- 위 2개 파일 외 본 작업의 코드 변경 없음. (git diff 상의 layout.tsx·WatchlistTable.tsx·useWatchlist.ts·_DISPATCH_CHECKPOINT.md 등은 타 일꾼 T-E·T-F 작업으로 본 작업과 무관.)
- 브랜드 그린: 설정 항목 강조/hover는 기존 `text-primary` 토큰 패턴을 그대로 사용해 헤더 전체와 색 일관 유지(00-overview §3-3 그린). ⭐ 강조는 watchlist 페이지(T-A) 책임이며 헤더 드롭다운은 라벨 텍스트 통일(타 항목 이모지 미사용과 일관) 유지.

---

## 4. 검증

| 항목 | 결과 |
|------|------|
| `npx tsc --noEmit` | ✅ PASS (exit 0) |
| `npx eslint` (변경 2파일) | global-header.tsx ✅ clean / AuthButton.tsx — error 1·warning 1 **모두 기존 코드**(line 10 `useState<any>`, line 28 useEffect deps), 본 작업 추가분(설정 링크)과 무관·diff 미포함 |
| 죽은 링크 0 | ✅ 헤더 전 링크 라우트 존재 확인 — 정적(`/analysis` `/stock` `/signal` `/market` `/calendar` `/history` `/watchlist` `/secure-memo` `/settings` `/news` `/portfolio` `/admin` `/auth/login`) + 동적(`/coin/[symbol]`, `/board/[slug]`, `/board/[slug]/write`) |
| 선행(T-A `/watchlist`·T-B `/settings`) | ✅ 실페이지 교체 완료 확인 후 착수 |

### 수동 검증 가이드 (지휘관/후속)
- 익명: 도구▼ → "설정"·"관심종목" 진입 동작 (데스크톱 hover 드롭다운 + 모바일 햄버거 메뉴 양쪽).
- 회원: 헤더 우측 ⚙️ 아이콘 → `/settings` 진입.
- 모바일 메뉴: 도구 그룹 펼침에 설정·관심종목 포함됨(tools.items 공유 렌더링).

---

## 5. 미결(지휘관 통합 시)

- `_WEB_CONTRACT.md` 라우트 레지스트리에 `/settings` 진입점 2건 반영 (명세 §4 지시대로 일꾼이 직접 갱신하지 않음).
- (선택) AuthButton.tsx 기존 lint 에러 2건(line 10·28)은 본 작업 범위 밖. 정리 필요 시 별도 태스크.
- (참고) 명세가 가정한 "헤더 = components/Common/"은 실제와 불일치 → 차후 nav 작업 명세는 `components/global-header.tsx`·`components/AuthButton.tsx`를 소유 대상으로 명시 권장.
