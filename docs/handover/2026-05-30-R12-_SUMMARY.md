# R12 통합 보고서 — watchlist/settings (Wave1 + Wave2 마감)

> 작성: 2026-05-30 세션39 지휘자 (kdydispatch CEO)
> 라운드: R12 / tag: watchlist-settings / 평면(flat) 6일꾼

---

## 1. 라운드 개요

v2.0 커뮤니티 피벗의 watchlist(관심종목)·settings(표시 환경설정) 기능을 2 Wave로 구현.

| Wave | 일꾼 | 작업 | 회수 |
|------|------|------|------|
| 1 | T-A | watchlist (useWatchlist + 표UI + 병렬폴링) | ✅ PASS (세션38) |
| 1 | T-B | settings (표시설정 Context + localStorage + 초기화 + 계정) | ✅ PASS (세션38) |
| 1 | T-C | DB·API (user_watchlist 마이그레이션·RLS + API 4) | ✅ PASS (세션38) |
| 2 | T-D | nav 진입점 2건 (도구▼>설정 + AuthButton ⚙️) | ✅ PASS (세션39) |
| 2 | T-E | S2 표시설정 전역 적용 (Provider 루트 + WatchlistTable 구독) | ✅ PASS (세션39) |
| 2 | T-F | D3 회원 동기화 (useWatchlist 내부 sync·DB소스전환) | ✅ PASS (세션39) |

- Wave1: 190356e(세션38) 커밋 완료 + 통합 핫픽스 2건(localStorage 키 통일·eslint 화이트리스트).
- Wave2: 세션39에서 외부 3터미널 분산 발사 → 회수·통합·커밋(본 라운드 마감).

## 2. Wave2 회수 검증 (Phase 4)

### 완료율 / 검증
- handover 3/3 작성. 자가검증 전부 PASS 보고.
- 지휘자 독립 재검증: `npx tsc --noEmit` **exit 0**, `npx eslint`(변경 8파일) **error 0**.
- 파일 중복 수정 **0** — 머지 충돌 없음.

### 격리 점검
| 일꾼 | 판정 | 비고 |
|------|------|------|
| T-E | ✅ 영역 내 | `app/layout.tsx`·`components/Watchlist/WatchlistTable.tsx` |
| T-F | ✅ 영역 내 | `components/hooks/useWatchlist.ts` 1파일, SSOT 교차임포트 0 |
| T-D | ⚠️ **격리 일탈(정당)** | SOT가 헤더를 `components/Common/`으로 가정했으나 실제는 `components/global-header.tsx`·`AuthButton.tsx` → 거기 수정. 타깃이 Common에 없어 불가피, 변경 최소(+1/+12줄), 타 일꾼과 disjoint·충돌 0. **차후 nav SOT는 실제 경로 명시 필요** |

## 3. 🔴 통합 중 신규 블로커 발견·해소

**middleware.ts가 `/settings`·`/watchlist`를 인증 보호** → 익명 우선 MVP(taste #3, T-A/B/D/F 설계)와 정면 모순. 어느 일꾼도 `middleware.ts`를 소유하지 않은 교차 영역이라 누락됨.
- 증상(미해소 시): 익명 사용자가 두 페이지 도달 불가(로그인 리다이렉트), T-D 익명 진입점 무용지물.
- 조치(사용자 승인): `protectedPaths`에서 둘 제거(`/portfolio`·`/secure-memo`·`/admin`은 유지). 회원 전용 기능은 페이지 내부 auth 분기.

## 4. 지휘자 통합 작업 (세션39)

1. settings 로컬 `DisplaySettingsProvider` 래퍼 제거 (`app/settings/page.tsx`) — T-E 루트 단일마운트 정석화.
2. `format.ts` `formatPrice` dead export 제거 (Context로 이관됨).
3. `AuthButton.tsx` 기존 lint 2건 수정: `useState<any>`→`useState<User|null>`, useEffect deps(`useMemo` 안정화 + `[supabase]`).
4. `_WEB_CONTRACT.md` 라우트 레지스트리 정합: R-020/R-021 인증'아니오'·활성, GNB 도구▼ 9번째(설정) 등재 + AuthButton 진입점 주석, 비인증 표·§8 요약·이력 갱신.
5. `_SCHEMA_REFERENCE.md`: user_watchlist 운영 DB 적용 기록 추가.
6. **user_watchlist 운영 DB 적용**(outward-facing, 승인 후): Management API `database/query` + `SUPABASE_ACCESS_TOKENS`. 테이블 6컬럼·인덱스·RLS 4정책·RLS활성 검증 + `schema_migrations` 정합(version `20260529000001`).

## 5. taste 준수 (7확정)
1 클라이언트병렬 ✅ · 2 로컬우선병합 ✅(T-F sync 합집합) · 3 익명30/회원100 ✅ · 4 한국식고정→S2전환 ✅ · 5 진입점둘다 ✅(T-D) · 6 다크v2.1미룸 · 7 그린 ✅

## 6. 변경 파일 (Wave2 + 지휘자 통합)

| 파일 | 담당 | 변경 |
|------|------|------|
| `components/global-header.tsx` | T-D | 도구▼ 설정 항목 +1 |
| `components/AuthButton.tsx` | T-D + 지휘자 | ⚙️ 설정 링크 +12 / lint 2건 수정 |
| `app/layout.tsx` | T-E | DisplaySettingsProvider 루트 마운트 |
| `components/Watchlist/WatchlistTable.tsx` | T-E | useDisplaySettings 구독(통화·등락색) |
| `components/hooks/useWatchlist.ts` | T-F | D3 동기화 캡슐화 |
| `app/settings/page.tsx` | 지휘자 | 로컬 Provider 래퍼 제거 |
| `components/Watchlist/format.ts` | 지휘자 | formatPrice dead export 제거 |
| `middleware.ts` | 지휘자 | /settings·/watchlist 보호 해제 |
| `docs/references/_WEB_CONTRACT.md`·`_SCHEMA_REFERENCE.md` | 지휘자 | 레퍼런스 정합 |

## 7. 미해결 / 후속 (R13 후보)

- **런타임 스모크 미수행**: 회원 sync→DB반영→새기기 복원은 dev서버+회원로그인 필요. 정적 검증만 완료(PASS 위장 아님). 운영 DB 적용됐으므로 후속 수동 1회 권장.
- **R13 전역 시세 롤아웃**: S2는 MVP 표면(watchlist 표)만 구독. 시세 스트립·코인룸·/analysis 등은 R13에서 `useDisplaySettings()` 동일 패턴 확장(Provider 이미 루트).
- **reorder DB 영속화**·**clear 벌크 삭제 엔드포인트**: T-F 스코프 외, 후속.
- **notice UX 표면화**: T-F가 `notice`/`dismissNotice` 제공, WatchlistView 토스트 구독은 후속.
- **AuthButton 드롭다운 UX**: 현재 아이콘 버튼 그룹. 계정 드롭다운 패턴은 후속 디자인.

## 8. 검증 증거
- `npx tsc --noEmit` → exit 0
- `npx eslint app/layout.tsx app/settings/page.tsx components/AuthButton.tsx components/Watchlist/WatchlistTable.tsx components/Watchlist/format.ts components/global-header.tsx components/hooks/useWatchlist.ts middleware.ts` → error 0 (middleware:47 기존 경고 1, 무관)
- 운영 DB: information_schema 6컬럼 + pg_policies 4정책 + relrowsecurity=true + schema_migrations RETURNING 20260529000001
