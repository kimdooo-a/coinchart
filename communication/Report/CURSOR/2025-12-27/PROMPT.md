✅ PROMPT → TO_CURSOR

2) Cursor — "실제 구현(3개 핵심 페이지: 철학 디자인 + 성능/신뢰성)"

1) 먼저 확인(ACK):
네가 받은 모든 메시지를 읽고, "현재 구현 상태 + 가장 큰 병목 3개"를 10줄로 요약해라.

2) 즉시 UI 정리(브랜딩/신뢰성):
GlobalHeader 로고 텍스트 사랑하는 마누라 즉시 변경.

3) "Classic Masters Design System" 구현 방식(토큰 API 금지 준수):
방법 A: 사용자가 Midjourney로 만든 이미지 → public/art/*에 넣고 Tailwind 배경/오버레이로 사용
방법 B: Public domain(고전 명화) 이미지를 사용자가 직접 다운로드하여 정적 에셋으로만 사용
어떤 방법이든 "외부 토큰 API 호출"은 금지.

4) 적용 대상 3페이지(돈 되는 3페이지)만 집중 구현:
홈 /
코인 분석 /analysis/[symbol] (현재 페이지는 "AI" 배지/프랙탈 영역이 존재)
주식 분석 /stock (Twelve Data 라벨 표시)

5) 성능/확장성 1차 개선(큰 것만):
코인 분석 페이지는 현재 클라이언트에서 많은 작업을 하고 있고(차트/지표/프랙탈 등), 더 커지면 위험하다.
"분석 결과 캐싱(서버) + 클라에서는 렌더만" 구조로 1차 이전 설계를 제안하고, 가능한 범위에서 구현해라.
(단, Redis 유료/복잡하면 Supabase KV/테이블 캐시로 시작)

6) 산출물 저장:
공통 지시 0의 Report 규칙대로 저장.
