# CLAUDE CODE TECHNICAL ANALYSIS REPORT

**ID**: `RPT-20251227-001`
**DATE**: `2025-12-27T12:00:00+09:00`
**PHASE**: `0 (Bootstrap)`
**OWNER**: `Claude Code`
**STATUS**: `COMPLETED`

---

## A. Prompts Received Log

### Session 1: Bootstrap Acknowledgment
```
[User Request]
kdy-addon\poly-tech2\readme.md

[Action Taken]
- Read README.md (Bootstrap Contract)
- Read SHARED_CONTEXT.md (Current Phase: BOOTSTRAP → ONBOARDING)
- Read CONSTITUTION.md (Supreme Law)
- Read ROLE_MODEL.md (Claude Code Role Definition)
- Read COMMUNICATION.md (Protocol)
- Read WORKFLOW.md (Phase Gates)
- Acknowledged Constitution compliance
```

### Session 2: Project Analysis Request
```
[TO_CLAUDE_CODE]

Analyze:
- 코인 차트분석 프로젝트 전체를 기술/논리 관점에서 평가하라.

Focus:
1. 현재 아키텍처의 강점 / 병목 / 기술부채
2. "AI 분석"이 실제로 어디까지 구현되어 있고, 어디가 껍데기인지
3. 고도화 시 가장 파괴력이 큰 지점 TOP 5
   (정확도, 차별성, 확장성 기준)

Deliver:
- 기술 고도화 제안 목록 (우선순위 포함)
- 지금 당장 하면 안 되는 것 (위험요소)
- Commander(나)가 전략 설계 시 반드시 알아야 할 함정

Constraints:
- 코드 수정 금지
- 실행 금지
```

### Session 3: Report Documentation Request (Current)
```
[TO_CLAUDE_CODE]

Phase 0 Mission:
1) 너에게 전달된 모든 지시/프롬프트와 네 결과물을 "정리해서 기록"하라.
2) 기술/논리 측면에서 현재 프로젝트의 "AI 실체"를 정직하게 정리하고,
   유료 SaaS 수준으로 끌어올릴 설계를 제시하라.
3) Watcher는 사용하지 않는다.

Report Folder: F:\11 dev\251206 코인 차트분석\communication\Report
Report File: CLAUDE_CODE_REPORT_2025-12-27.md

Sections Required:
A) Prompts Received Log
B) AI Reality Check
C) Commander Warnings
D) Upgrade Blueprint (No paid AI API)
```

---

## B. AI Reality Check

### B-1. 시스템 분류표

| 기능 | 분류 | 파일 위치 | 실체 설명 |
|------|------|-----------|-----------|
| RSI/MACD/BB 등 7개 지표 | **Rule-Based** | `lib/indicators.ts` | 수학 공식 구현. AI 아님. |
| 멀티 지표 스코어링 | **Rule-Based** | `lib/analysis.ts` | if/else 가중치 조합. AI 아님. |
| 프랙탈 패턴 매칭 | **Rule-Based** | `lib/fractal_engine.ts` | 피어슨 상관계수. 통계, AI 아님. |
| 백테스트 승률 | **Rule-Based** | `lib/backtest.ts` | 과거 가격 비교. AI 아님. |
| 시그널 스캔 | **Rule-Based** | `lib/signal_engine.ts` | RSI + 가격변화율만 체크 |
| 공포탐욕 지수 | **Hybrid** | `app/market/page.tsx` | alternative.me API + 자체 계산 |
| RSI Heatmap | **Real Data** | `components/Market/RSIHeatmap.tsx` | Binance API 실시간 |
| **고래 경보** | **MOCK** | `components/Signal/WhaleAlert.tsx` | `Math.random()` 100% 시뮬레이션 |
| **주식 차트** | **MOCK** | `lib/api/binance.ts:27-51` | `generateMockCandles()` 가짜 |
| **AI Scan 애니메이션** | **MOCK** | `app/signal/page.tsx:26-31` | 3초 대기 후 빈 배열 반환 |

### B-2. 기술 스택 분류

```
┌─────────────────────────────────────────────────────────┐
│                    현재 시스템 구성                       │
├─────────────────────────────────────────────────────────┤
│  [REAL]                                                 │
│  ├── Binance API (Crypto 가격/캔들)                      │
│  ├── alternative.me API (공포탐욕지수)                   │
│  ├── Supabase (인증/데이터 저장)                         │
│  └── 기술적 지표 계산 (RSI, MACD 등)                     │
├─────────────────────────────────────────────────────────┤
│  [RULE-BASED - "AI" 아님]                               │
│  ├── 지표 기반 매수/매도 판정                            │
│  ├── 시장 상태 분류 (Trending/Ranging/Volatile)          │
│  ├── 가중치 스코어링                                     │
│  └── 패턴 상관분석                                       │
├─────────────────────────────────────────────────────────┤
│  [MOCK/FAKE]                                            │
│  ├── 고래 경보 (100% 랜덤)                               │
│  ├── 주식 가격 (100% 랜덤)                               │
│  └── Signal Scan (빈 결과)                               │
├─────────────────────────────────────────────────────────┤
│  [없음 - 0%]                                            │
│  ├── Machine Learning 모델                              │
│  ├── Deep Learning (LSTM/Transformer)                   │
│  ├── 자연어 처리 (뉴스 감성분석)                         │
│  └── 온체인 데이터 분석                                  │
└─────────────────────────────────────────────────────────┘
```

### B-3. "AI" 라벨 vs 실제 코드

| UI 표기 | 코드 실체 |
|---------|-----------|
| "⚡ 인공지능 정밀 분석" | `if (rsi < 30) return 'BUY'` |
| "🧠 AI 패턴분석 (BETA)" | `calculateCorrelation(x, y)` (피어슨 상관계수) |
| "AI Commentary" | 하드코딩된 한글/영어 문자열 조건 분기 |
| "AI SCAN" | `setTimeout(() => setSignals([]), 3000)` |

**결론**: ML/DL 모델 없음. 현재 시스템은 **규칙 기반 기술적 분석(Technical Analysis)**임.

---

## C. Commander Warnings (함정 요약)

### C-1. 마케팅 함정
| 함정 | 설명 | 위험도 |
|------|------|--------|
| "AI" 용어 오용 | ML 모델 없이 AI 마케팅 → 기대치 GAP, 법적 리스크 | **CRITICAL** |
| 정확도 수치 홍보 | 백테스트 승률 ≠ 실전 수익률 (수수료/슬리피지 미반영) | **HIGH** |
| "실시간 고래 추적" | 100% 시뮬레이션 데이터 | **HIGH** |

### C-2. 기술적 함정
| 함정 | 설명 | 위험도 |
|------|------|--------|
| 백테스트 과적합 | 단순 "가격이 올랐나?" 체크. 리스크 지표 없음 | **HIGH** |
| 데이터 이중구조 | Crypto=Real, Stock=Fake, Whale=Fake 혼재 | **HIGH** |
| 클라이언트 연산 | 모든 분석이 브라우저에서 실행 → 모바일 성능 저하 | **MEDIUM** |
| API 캐싱 부재 | 페이지 로드마다 API 호출 → Rate Limit 위험 | **MEDIUM** |

### C-3. 확장성 함정
| 함정 | 설명 | 위험도 |
|------|------|--------|
| 테스트 0% | 리팩토링/확장 시 회귀 버그 위험 | **HIGH** |
| any 타입 남용 | 타입 안정성 부재 → 런타임 에러 | **MEDIUM** |
| 에러 핸들링 미흡 | 대부분 `console.error` 후 무시 | **MEDIUM** |

### C-4. 전략적 함정
| 함정 | 설명 |
|------|------|
| 프랙탈 엔진 과신 | 피어슨 상관계수 ≠ 미래 예측력. 과거 유사 ≠ 미래 동일 |
| 승률 착시 | 60% 승률도 손익비 1:1이면 수수료로 손실 |
| 스케일 환상 | 동시 100명 접속 시 Binance API 1만+ 호출 → 차단 |

---

## D. Upgrade Blueprint (No Paid AI API)

> **제약조건**: OpenAI, Anthropic, Google AI 등 유료 AI API 사용 금지

### D-1. 즉시 가능한 개선 (Week 1-2)

#### 1. 데이터 정직성 확보
```
[현재]
WhaleAlert.tsx → Math.random() 시뮬레이션
Stock prices → Mock 랜덤 생성

[개선]
- WhaleAlert: "(Simulation)" 라벨 강조 또는 기능 비활성화
- Stock: "Demo Mode" 명시 또는 페이지 숨김
- "AI" → "Smart Analysis" 또는 "Technical Analysis" 용어 변경
```

#### 2. 서버사이드 캐싱 도입
```typescript
// 현재: 클라이언트에서 매번 API 호출
const res = await fetch(`https://api.binance.com/...`);

// 개선: Next.js API Route + 캐시
// app/api/analysis/[symbol]/route.ts
export async function GET(request, { params }) {
  const cached = await redis.get(`analysis:${params.symbol}`);
  if (cached) return NextResponse.json(JSON.parse(cached));

  const data = await binance.getKlines(params.symbol);
  const analysis = analyzeMarket(data);

  await redis.setex(`analysis:${params.symbol}`, 300, JSON.stringify(analysis));
  return NextResponse.json(analysis);
}
```

### D-2. 단기 고도화 (Month 1)

#### 1. 분석 품질 강화 (AI API 없이)
```
[추가 가능한 지표 - 모두 수학 공식]
- 볼륨 프로파일 (Volume Profile)
- VWAP (Volume Weighted Average Price)
- 피보나치 리트레이스먼트 자동 계산
- 엘리엇 웨이브 패턴 감지 (규칙 기반)
- 다이버전스 자동 감지 (RSI/MACD vs Price)
```

#### 2. 백테스트 엔진 고도화
```typescript
// 현재
const isWin = exitPrice > entryPrice;

// 개선
interface BacktestMetrics {
  winRate: number;
  profitFactor: number;      // 총이익 / 총손실
  sharpeRatio: number;       // 위험조정수익률
  maxDrawdown: number;       // 최대 낙폭
  avgWin: number;
  avgLoss: number;
  expectancy: number;        // 기대수익
}
```

#### 3. 온체인 데이터 통합 (무료 티어)
```
[무료 API 옵션]
- Whale Alert API: 무료 티어 (월 10회)
- Glassnode Free: 기본 온체인 지표
- CryptoQuant Free: 거래소 유출입
- Dune Analytics: 커스텀 쿼리 (무료)
```

### D-3. 중기 고도화 (Quarter 1)

#### 1. 로컬 ML 모델 (AI API 없이)
```
[TensorFlow.js 활용]
- 브라우저/Node.js에서 실행
- 모델 학습: 로컬 Python으로 훈련 → TFJS 변환
- 추론: 클라이언트 또는 서버에서 무료 실행

[가능한 모델]
- LSTM 가격 예측 (시계열)
- Random Forest 분류 (매수/매도/관망)
- XGBoost 앙상블
```

```typescript
// 예시: TensorFlow.js 가격 예측
import * as tf from '@tensorflow/tfjs';

const model = await tf.loadLayersModel('/models/price-predictor/model.json');
const input = tf.tensor2d([normalizedPrices], [1, 60]);
const prediction = model.predict(input);
```

#### 2. 뉴스 감성분석 (무료)
```
[무료 NLP 옵션]
- Hugging Face Transformers.js (브라우저 실행)
- 사전학습된 FinBERT 모델 (금융 특화)
- 로컬 실행이므로 API 비용 없음

[데이터 소스]
- CryptoPanic API (무료 티어)
- RSS 피드 (무료)
- Twitter/X API (제한적 무료)
```

### D-4. 아키텍처 개선 제안

```
┌─────────────────────────────────────────────────────────┐
│                    목표 아키텍처                          │
├─────────────────────────────────────────────────────────┤
│  [Client Layer]                                         │
│  ├── Next.js App (UI Only)                             │
│  ├── TensorFlow.js (경량 추론)                          │
│  └── WebSocket (실시간 가격)                            │
├─────────────────────────────────────────────────────────┤
│  [API Layer - Next.js API Routes]                       │
│  ├── /api/analysis/[symbol] (캐싱된 분석 결과)           │
│  ├── /api/signals (스캔 결과)                           │
│  └── /api/onchain (온체인 지표)                         │
├─────────────────────────────────────────────────────────┤
│  [Cache Layer]                                          │
│  ├── Vercel KV / Upstash Redis (5분 TTL)               │
│  └── Supabase Edge Cache                               │
├─────────────────────────────────────────────────────────┤
│  [Data Layer]                                           │
│  ├── Binance API (Crypto)                              │
│  ├── TwelveData Free (Stocks - 800 req/day)            │
│  ├── Whale Alert Free (10 req/month)                   │
│  └── CryptoPanic RSS (News)                            │
├─────────────────────────────────────────────────────────┤
│  [ML Layer - 로컬/서버]                                  │
│  ├── TensorFlow.js (추론)                               │
│  ├── Python Scripts (학습 - 오프라인)                   │
│  └── ONNX Runtime (고성능 추론)                         │
└─────────────────────────────────────────────────────────┘
```

### D-5. 우선순위 로드맵

| 우선순위 | 작업 | 난이도 | 효과 |
|----------|------|--------|------|
| **P0** | "AI" 마케팅 용어 수정 | ★☆☆☆☆ | 리스크 제거 |
| **P0** | Mock 데이터 명시/비활성화 | ★☆☆☆☆ | 신뢰성 확보 |
| **P1** | API 캐싱 레이어 추가 | ★★★☆☆ | 성능 10x |
| **P1** | 서버사이드 분석 이전 | ★★★☆☆ | 확장성 확보 |
| **P2** | 백테스트 고도화 | ★★★☆☆ | 프로 유저 유입 |
| **P2** | TensorFlow.js 통합 | ★★★★☆ | 진짜 AI |
| **P3** | 온체인 API 연동 | ★★★☆☆ | 차별화 |
| **P3** | 실제 주식 API | ★★☆☆☆ | 자산군 확장 |

---

## E. 파일 분석 목록

### 읽은 핵심 파일
```
lib/analysis.ts          - 멀티 지표 스코어링 엔진
lib/indicators.ts        - 기술적 지표 라이브러리
lib/fractal_engine.ts    - 패턴 매칭 엔진
lib/backtest.ts          - 백테스팅 로직
lib/signal_engine.ts     - 시그널 스캐너
lib/api/binance.ts       - Binance API + Mock 생성
lib/constants.ts         - 지원 코인/주식 목록
app/api/signals/route.ts - 시그널 API 엔드포인트
app/api/price/route.ts   - 가격 API 엔드포인트
app/analysis/[symbol]/page.tsx - 상세 분석 페이지
app/market/page.tsx      - 시장 심리 페이지
app/signal/page.tsx      - 시그널 페이지
components/Analysis/AnalysisPanel.tsx - 분석 패널 컴포넌트
components/Analysis/TradingStrategyGuide.tsx - 전략 가이드
components/Signal/WhaleAlert.tsx - 고래 경보 (Mock)
components/Market/RSIHeatmap.tsx - RSI 히트맵
package.json             - 의존성
project_structure.txt    - 프로젝트 구조
```

---

## F. 결론 및 권고사항

### 현재 상태 요약
- **강점**: 잘 구조화된 Next.js 앱, 완전한 기술적 지표 라이브러리
- **약점**: AI 없음, Mock 데이터 혼재, 캐싱 없음, 테스트 없음
- **기회**: 로컬 ML, 온체인 데이터, 서버사이드 아키텍처
- **위협**: AI 마케팅 오용, 스케일링 병목, 신뢰성 이슈

### Commander 핵심 결정 사항
1. **"AI" 용어 유지 vs 변경** - 법적/마케팅 리스크 평가 필요
2. **Mock 데이터 처리** - 비활성화 vs Demo 명시 vs 실제 API 연동
3. **로드맵 우선순위** - 품질 vs 기능 확장 vs 성능

### 제안
"Smart Technical Analysis Platform"으로 포지셔닝하고, 단계적으로 로컬 ML을 도입하여 "진짜 AI"로 진화하는 전략 권장.

---

**Report Status**: COMPLETED
**Next Phase**: Commander 결정 대기
**Agent**: Claude Code (Reasoning/Analysis)
