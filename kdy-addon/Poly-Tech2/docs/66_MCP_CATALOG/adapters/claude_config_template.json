{
    "mcpServers": {
        "{{ID}}": {
            "command": "{{COMMAND}}",
            "args": [
                "{{ARGS}}"
            ],
            "env": {
                "{{ENV_KEY}}": "YOUR_KEY_HERE"
            }
        }
    }
}